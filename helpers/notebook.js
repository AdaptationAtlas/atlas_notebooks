const e="atlas-notebook-config",r=["en","fr"];export const SITE_ROOT=new URL("../",import.meta.url);async function s(o){const t=await fetch(o);if(!t.ok)throw new Error(`Unable to load ${o}: HTTP ${t.status}`);return t.json()}export function readNotebookConfig(o=document){const t=o.getElementById(e);if(!t?.textContent)throw new Error(`Missing #${e}; check the notebook's nb-config`);return JSON.parse(t.textContent)}export async function loadNotebookText(o){const t=await Promise.all(r.map(async n=>[n,await s(new URL(`${o.textDir}/${n}.json`,SITE_ROOT))]));return Object.fromEntries(t)}
