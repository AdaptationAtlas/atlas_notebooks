const e="atlas-notebook-config",r=["en","fr"],s=new URL("../",import.meta.url);async function a(n){const t=await fetch(n);if(!t.ok)throw new Error(`Unable to load ${n}: HTTP ${t.status}`);return t.json()}export function readNotebookConfig(n=document){const t=n.getElementById(e);if(!t?.textContent)throw new Error(`Missing #${e}; check the notebook's nb-config`);return JSON.parse(t.textContent)}export async function loadNotebookText(n){const t=await Promise.all(r.map(async o=>[o,await a(new URL(`${n.textDir}/${o}.json`,s))]));return Object.fromEntries(t)}
