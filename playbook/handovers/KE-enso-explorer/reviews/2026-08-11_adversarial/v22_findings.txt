
===============================================================================================
REVIEWER: checklist-compliance auditor — v2.2 @ 4f2ddd1 vs V2_CYCLE3_CHECKLIST.md
VERDICT: DO NOT SHIP: one critical structural break undoes two checklist items the build claims as done. A missing ```{ojs} fence at line 3617 means the entire Fig 3.8 flagship machinery (D8 — Pete's 'most important plot') and, via the orphaned closing fence, the GESI data cell (B7) never enter the OJS module: verified against the rendered HTML's ojs-module-contents (13 definitions absent, all referenced). Shipped page = Fig 3.8 permanent loader + dead controls, Fig 1.4 GESI OJS error (and since v2.1's annex A5.1 full GESI table was removed as 'subsumed', NO GESI survives anywhere — a regression), plus a likely 60 s boot-spinner hang from ≥5 persistent RuntimeErrors. The commit's QA claim was false assurance: ojs_node_check.py's regex fence-pairing PASSES the broken file. Item-by-item: A1 delivered (toc:false beats project toc:true — native TOC absent in HTML; .atlas-toc left-anchor !important beats the helper's injected right:22px; collapse toggle present; tocMobile.js null-guards safely); A2 delivered (About <details> sits in the same .plot-footer-row flex as chartDownloadButton); A3 delivered-with-deviation (one 'Data downloads' section of per-dataset cards rather than per-section links; dual citation + copy button ✓; all registry files verified present in _site/data, incl. gesi_v2.parquet); A4 delivered (NDVI from data start, ReliefWeb ≥2010, prices/ToT at data start, annotation clamps; MAM-2026 = known data task); A5 delivered; A6 largely delivered (channel tooltips; small gaps: A1.1 monthly line, A3.2 trend, maps use native <title>); A7 PARTIAL (NDVI multiselect ✓ but the multi-county rainfall compare was deleted with no replacement — finding); A8 delivered except (d) similar-seasons filter (missed, undocumented — finding). B1–B6 delivered (B5's treemap uses the range's end year rather than a dedicated single-year selector — defensible; B6's coverage explanation is in produceIntro); B7 delivered in code, dead in the shipped page (critical finding). C1–C4 delivered; C5 delivered (per-panel drivers incl. composite default, %-labels, All-years rows, 1991–2025 from/to; OND panel deliberately omits W-V per the R2 cross-season guard; but its CSV export doesn't match the redesign — finding); C6 correctly deferred. D1–D6, D9 delivered; D7 partial (points not phase-coloured — finding); D8 broken as above (in-source implementation is otherwise faithful: 3 views, preceding-OND bar strength, both-season strip, detrend explicitly declined at n=6, rainfall/SPEI drivers; backlink one-way — minor finding); D10 delivered-with-deviation (4.3 removed/subsumed by 2.3's MAM panel, §2 not §3 — defensible). E1 delivered; E2 delivered (sticky-bar KMD strip states no machine-readable feed + links, as the checklist allows); E3 missed (finding). F1 correctly deferred AND documented (tradeIntro: 'no cross-border price series is served yet'); F2 delivered (Season rainfall + SPEI-3 are first-class drivers in phaseDefs/eventDriver). Regression spot-check: all 12 R2 critical fixes held (picker gaul mapping, corrMap BrBG sign, ACLED week-substr, full-band year domains, is_county filter, xbt dominant-unit, off-diagonal guard, RONI-based ensoPhaseOND, DMI coalesce, modalNull enumeration, base-rate All-years rows, 92/62-day staleness tiers), and the driver framework is genuinely verbatim with dev_rainfall_maps (Z_BANDS 0.5/1.0/1.5, identical PRGn cardColor constants, composite = mean of member z re-standardised). Fix is one line (insert the fence), then re-render and real-browser-verify 1.4 and 3.8 — and harden the node-check tool so this class of break can't pass QA again.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:3617 (block 3618–3709; stray closing fence 3710) — Missing ```{ojs} fence kills Fig 3.8 (D8 flagship) AND Fig 1.4 (B7 GESI) on the shipped page
  PROBLEM: The '// ---- 3.8 flagship machinery ----' block has no opening ```{ojs} fence: the produce-machinery cell closes at 3615 and the next opener is the (now-orphaned) ``` at 3710. Verified in _site HTML: the block renders as literal <p> text (inside the .hidden appendix, so invisible, with live inline `${county}` OJS spans), the stray fence at 3710 then swallows the GESI cell into a literal <pre><code>```{ojs} // ---- GESI ----…</code></pre>, and the decoded ojs-module-contents confirms 13 definitions never execute: naprCtyFull, prodEvtCrops, prodEvtDefault, prodEvtRowsAll, prodEvtYears, prodEvtStrip, prodEvtBars, prodEvtLines, prodEvtTable, dbGesi, gesiAll, gesiSeries, gesiExport — all still referenced by executed cells (renderToDiv('plotProdEvents', …) → prodEvtBars(); viewof prodEvtPick → prodEvtCrops; plotFooter 3.8 → prodEvtRowsAll; gesiRiskCard → gesiSeries; plotFooter 1.4 → gesiExport). Shipped result: Fig 3.8 = permanent loader + OJS errors, Fig 3.8 controls dead, Fig 1.4 GESI = OJS error — and since v2.2 also deleted v2.1's annex A5.1 full GESI table, the page ships with NO working GESI at all (a net regression from v2.1). Per the project's _include.html behaviour, ≥5 persistent OJS RuntimeErrors likely hold the boot spinner to its 60 s cap.
  FIX: Insert a ```{ojs} opener at line 3617 (immediately before '// ---- 3.8 flagship machinery ----'). That re-pairs 3710 as this cell's closer and restores the GESI cell (3712–3743). Re-render and re-verify Fig 1.4 and Fig 3.8 in a real browser (headless mis-reproduces this stack).

[MAJOR] playbook/handovers/KE-enso-explorer/tools/ojs_node_check.py:57 — QA gate false-passes the broken file — regex fence pairing diverges from Quarto's extraction
  PROBLEM: The checker extracts cells with re.findall(r"```\{ojs\}\n(.*?)```", qmd, re.S), which happily pairs the swallowed ```{ojs} GESI opener (literal text inside the orphaned plain block) with a later fence. It reports 390 cells / NODE CHECK: PASS on the current file even though Quarto's rendered module is missing 13 definitions. This is exactly why the commit's 'all OJS cells node-checked' claim shipped a dead flagship figure.
  FIX: Make the checker parse fences stateful, line-by-line, the way Quarto's breakQuartoMd does (plain ``` opens a non-executable block that only a bare ``` closes), and additionally cross-check its cell inventory against the rendered HTML's ojs-module-contents (base64 JSON) — flag any name defined in the qmd's apparent cells but absent from the executed module.

[MAJOR] notebook_v2.qmd §2.1/§3.2 (rain figures) vs v2.1 Fig 5.1 (removed); checklist A7 [P27, P28] — A7 partial: multi-county RAINFALL compare deleted with no per-figure replacement
  PROBLEM: Pete's A7 says 3.1↔5.1 AND 3.3↔5.2 duplications dissolve via county multi-select as a per-figure capability. v2.2 dissolved §5 and gave the NDVI figure (3.3) a per-county-panel multiselect (verified, viewof ndviCounties), but v2.1's Fig 5.1 multi-county seasonal-rainfall compare was simply deleted: neither Fig 2.1 nor Fig 3.2 accepts multiple counties. The rainfall-compare capability Pete explicitly asked to integrate is now gone from the notebook entirely.
  FIX: Add the same enhancedMultiSelect + per-county-panel pattern (already proven on 3.3) to Fig 2.1's rainPanel or Fig 3.2's season strip — the rainSeasonData loader already parameterises on county, so a county-list loop mirroring ndviChart is the smallest change.

[MAJOR] checklist E3 [P30.1b] / A8(d) — no implementation anywhere in notebook_v2.qmd — E3 'similar seasons' highlight/filter missed and not documented as deferred
  PROBLEM: A8(d)/E3 ask for a 'find/highlight similar seasons (by rainfall tercile / driver state)' cross-notebook filter. Grep of the source and nbText finds only the driver-strength background shading and the outlook analogue chips — there is no control to select a driver state / tercile and have matching seasons highlighted across figures 3.2–3.9. Unlike C6/F1 (deferred with an explicit note) this item is silently absent: the commit message doesn't claim it, and no deferral is recorded in the checklist, ISSUES.md, or nbText.
  FIX: Either implement a minimal version (a 'highlight seasons like: [Strong La Niña / Dry tercile / …]' select that re-uses zSeries + categoryOf to emphasize matching years in annotationMarks and makeYearStrip), or record an explicit deferral with rationale in V2_CYCLE3_CHECKLIST.md / ISSUES.md so the item isn't lost.

[MINOR] notebook_v2.qmd:1268–1285 (totChart); checklist D7 [P22] — D7 half-done: ToT keeps faint background bands; points not coloured by phase
  PROBLEM: Pete asked to 'replace faint background bands with bars/points coloured by phase'. totChart still draws annotationMarks background bands behind a fixed-purple line and dots (fill: '#7b3294'). The x-start half of D7 is fine (data extent), and the global Driver-strength toggle mitigates, but the specific requested encoding — the series marks themselves carrying the phase colour — was not implemented.
  FIX: Colour the Plot.dot fill by the concurrent season's phase/strength (e.g. fill: d => cardColor(zSeries(eventDriver, d.month>=10?'OND':'MAM').get(d.year)) or the discrete PALETTE.phase colour), keeping the line neutral.

[MINOR] notebook_v2.qmd:830 (fig 2.3 data: countyContingency) and :1102 (fig 3.4 data: ndviOndByPhase) — 2.3 and 3.4 CSV downloads export the OLD phase structure, not what the redesigned panels show
  PROBLEM: Fig 2.3's download data is countyContingency (fixed 3-phase ENSO/W-V split, all years, no intensity classes, ignoring contOndDriver/contMamDriver and the from/to period control); the chart shows per-panel driver selections with Strong/Moderate/Weak classes over the chosen period. Fig 3.4's download is ndviOndByPhase (OND-only, ENSO phase) while the chart shows OND+MAM composite/W-V intensity bins; its filename is still '..._ndvi_ond_by_enso_phase'. A user downloading 'the data behind this figure' gets different groupings than displayed.
  FIX: Export the panels' actual working rows: for 2.3 emit the {group, outcome, count, total, share, driver, period} rows produced by contingencyPanel for both panels; for 3.4 emit the per-year {season, year, ndvi, z, bin} rows from ndviSwarmPanel and update the filename.

[MINOR] notebook_v2.qmd:625, 629–631, 741–758, 873–876, 1052, 374–379, 517 (new v2.2 controls) — New cycle-3 control labels hard-coded in English, bypassing nbText/_lang
  PROBLEM: The controls added in v2.2 — 'Show county spread (±1 sd)', 'Ocean strips', 'Chart background', 'Short-rains driver', 'Long-rains driver', 'From year'/'To year' (produce + contingency), 'Also show the long rains (MAM)', gesiFilter 'Show' — are literal strings, breaking the notebook's convention that all UI text resolves through _lang(nbText...). FR fallback currently masks it, but these strings will be invisible to the future FR pass.
  FIX: Move the labels into nbText_v2.json sections.controls and wrap with _lang(...), matching the existing controls (county, season, climateVar etc.).

[MINOR] notebook_v2.qmd §3.8 intro (nbText b3.prodIntro/prodAbout) and annex A5 figs A5.2/A5.3 — D8(b) cross-link is one-directional, and the annex figure numbering has a hole at A5.1
  PROBLEM: D8(b) asked for cross-links to/from the §1 produce figure: 1.2 → 3.8 exists (line 442) but nothing in the 3.8 section links back to Figure 1.2/#stakes. Separately, removing v2.1's A5.1 (full GESI table) left annex A5 starting at fig 'A5.2' (line 2031) with no A5.1 — a visible numbering gap.
  FIX: Add a '← the county's full production profile is Figure 1.2' line to b3.prodIntro, and renumber A5.2/A5.3 → A5.1/A5.2 (fig numbers live in the call sites, so nbText is untouched).


===============================================================================================
REVIEWER: hostile-ojs-plot-sql-adversary (cycle-3 challenge on notebook_v2.2 @ 4f2ddd1)
VERDICT: DO NOT SHIP. One missing ```{ojs} fence opener silently kills TWO figures (1.4 GESI and the flagship 3.8 production-by-events) and — per this project's boot contract — holds the whole-page loading overlay to the 60 s cap on every visit, because 13 cell definitions never compiled while all their consumers did. `quarto render` passed and the ojs_node_check tool passed (its regex pairs fences without markdown state, so it re-paired the orphan fence with the GESI cell's closer — a false negative to fix in the tool). Beyond that: the ReliefWeb chart's bars are drawn half a year off their own year axis/annotation bands with the last bar overflowing the frame; the left-TOC toggle is buried under the sticky controls bar on narrow viewports; and Fig 3.2's MAM bars are one month off their driver-strength shading. Everything else attacked held up: loader dep arrays are data-only and the render-cell reactive chains (prodView/vopView/prodEvtView + annMode via prodEvtLines→render function identity) are correct; object-valued Inputs.select defaults and enhancedMultiSelect identity matching are sound (prodDefaultPick reuses prodItemsAvail refs); no stale prodGroups/prodPick window (topological); all SQL verified green against the real parquets (NAPR∪AFA casts, SPEI-03/12 strings, is_county BOOLEAN, goat/maize products, county-name harmonization across all 10 served county datasets incl. Murang'a/Elgeyo); treemap svg templates are single-root with tips rewired per render; all 19 dataset-card files exist under _site/data/KE-enso-explorer/; all 178 nbText key refs and events.json shapes validate; all new section ids present in the rendered HTML.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:3615-3712 (missing opener at ~3617); visible damage at Fig 1.4 (qmd:524-577) and Fig 3.8 (qmd:1324-1369) — Missing ```{ojs} fence opener: 13 appendix definitions never compile — Figures 1.4 and 3.8 are dead and boot degrades to the 60 s overlay cap
  PROBLEM: The appendix cell that opens at line 3351 closes at 3615, and the next opener is absent: lines 3618-3709 (`// ---- 3.8 flagship machinery ----` defining naprCtyFull, prodEvtCrops, prodEvtDefault, prodEvtRowsAll, prodEvtYears, prodEvtStrip, prodEvtBars, prodEvtLines, prodEvtTable) are plain markdown. Confirmed in the rendered page: `<p>// —- 3.8 flagship machinery —- naprCtyFull = dbNapr.query(...` sits as a literal paragraph in _site/notebooks/KE-enso-explorer/notebook_v2.html. The now-orphan ``` at line 3710 then opens a plain code block whose closing fence is line 3743 — swallowing the ENTIRE GESI cell (3712-3743: dbGesi, gesiAll, gesiSeries, gesiExport) as a literal `<pre><code>\n```{ojs}\n// ---- GESI ----\ndbGesi = DuckDBClient.of({...` (confirmed in the HTML). Decoding the compiled ojs-module-contents proves all 13 definitions are absent (0 hits for `naprCtyFull =`, `prodEvtBars =`, `gesiSeries =`, `dbGesi =`, ...) while their consumers ARE compiled (viewof prodEvtPick refs prodEvtCrops/prodEvtDefault; the plotProdEvents loader+render cells ref prodEvtPick/prodEvtBars/prodEvtLines/prodEvtTable; plotFooter 3.8 refs prodEvtRowsAll; gesiRiskCard refs gesiSeries; plotFooter 1.4 refs gesiExport). At runtime these are persistent RuntimeErrors ('prodEvtBars is not defined', 'gesiSeries is not defined'): Fig 1.4 and Fig 3.8 render error callouts/never render, and per the project's boot contract (dispatches/2026-07-15: persistent OJS errors = spinner till the 60 s cap) EVERY page load sits on the loading overlay for a minute. FAOSTAT (3745+) onward re-syncs and compiles fine, which is why the rest of the page works. Note the checker false-negative: ojs_node_check.py reported 390 cells PASS because it pairs ```{ojs}...``` by regex without markdown fence state, so it paired the 3712 opener with the 3743 closer; Pandoc/Quarto (CommonMark: a closing fence cannot carry an info string) does not.
  FIX: Insert a ```{ojs} opener line between 3617 and 3618 (before `// ---- 3.8 flagship machinery ----`). That single line restores both blocks: 3618-3709 becomes a cell closed by 3710, and the GESI cell 3712-3743 parses normally again. Re-render, then grep the new HTML to confirm `flagship machinery` no longer appears in a `<p>` and that base64-decoding the ojs-module-contents scripts now contains `naprCtyFull =` and `gesiSeries =`. Separately, harden ojs_node_check.py to track CommonMark fence state (reject an info-string fence while a block is open) so this class of break can never pass the gate again.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1393 (rwChart, Fig 3.9), with the axis carrier at :1398-1399 and annotation bands via :1391 — Fig 3.9 bars are drawn half a year right of their year axis and event bands; the final year's bar overflows the frame
  PROBLEM: `Plot.rectY(data, {x: "year", interval: 1, ...})` decomposes each bar to x1=year, x2=year+1 on the continuous domain [2009.5, yEnd+0.5], so the 2016 bar is centred at 2016.5. But the year axis the reader uses is the makeYearStrip below (band scale over `years`, slot for year Y ≈ [Y-0.5, Y+0.5], centred on Y), and the named-event/driver-strength annotation rects from annotationMarks year-mode span Y±0.45 — both centred on Y. Net effect: every report-count bar sits half a year right of its tick and of the event band that is supposed to explain it (e.g. the 2016-17 drought band shades the gap left of the '2016' bar). In an events-attribution notebook this misassigns years visually. Additionally the last bar spans [yEnd, yEnd+1] while the domain ends at yEnd+0.5 and Plot does not clip rects by default, so half the final bar paints into the right margin.
  FIX: Drop the interval shorthand and give the rect explicit centred extents with the stack preserved: `Plot.rectY(data, {x1: (d) => d.year - 0.45, x2: (d) => d.year + 0.45, y: "n", fill: "cat", tip: true})` (rectY still stacks y when given x1/x2). Bars then align exactly with the strip cells and the annotation bands, and nothing exceeds the [2009.5, yEnd+0.5] domain.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:85 (.atlas-toc-toggle z-index 12) vs :39 (.controls-row.ke-sticky-controls z-index 1019) — Left-TOC toggle (Pete P4) is buried under the sticky county/season bar on narrow viewports — the collapsed TOC becomes unopenable after any scroll
  PROBLEM: The toggle is `position: fixed; top: 80px; left: 24px; z-index: 12` while the sticky controls bar is `position: sticky; top: 56px; z-index: 1019; background: #fff`. Once the user scrolls, the opaque bar occupies y=56px→~130px across the content column; wherever the column's left edge reaches x<~52px (viewports ≲1230px, i.e. phones/tablets/small laptops where Quarto's column padding is ~16-24px) it paints directly over the toggle and intercepts its clicks. Since viewports <1480px start with `atlas-toc-collapsed` (qmd:135-140), the buried button is the ONLY way to open the TOC — the cycle-3 navigation feature is effectively dead on exactly the screens that default it closed. The open panel itself (helpers/toc.ojs injects z-index 10) also slides under the bar. (The `right:auto !important; left:22px !important` override vs toc.ojs's injected `right:22px` does win correctly — that part is fine.)
  FIX: Raise the toggle and panel above the sticky bar in the page CSS: `.atlas-toc-toggle { z-index: 1021; }` and add `z-index: 1021 !important;` to the `.atlas-toc` override at qmd:83 (toc.ojs's stylesheet sets 10, so !important or a later sheet is needed). Verify at ~1100px width: scroll, click the burger, panel opens above the sticky bar.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:947 (seasonStripChart, Fig 3.2) — Fig 3.2 long-rains bars are painted one month late (Apr–Jun) vs the Mar–May season and its own driver-strength shading
  PROBLEM: `Plot.rectY(bars, {x: "t", interval: d3.utcMonth.every(3), ...})` floors each bar's timestamp to Jan/Apr/Jul/Oct boundaries. MAM bars carry t = Apr 15 (qmd:934), which floors to Apr 1 → the bar spans Apr 1–Jul 1, while the long rains are Mar–May and the strength-mode annotation rects for MAM span Mar 1–May 31 (annotationMarks, qmd:2421-2422). The bar and the shading that classifies that same season disagree by a month; OND is only coincidentally exact (Nov 15 → Oct 1–Jan 1). 
  FIX: Replace the interval with explicit per-season extents: `x1: (d) => new Date(Date.UTC(d.year, d.period === "OND" ? 9 : 2, 1)), x2: (d) => new Date(Date.UTC(d.year, d.period === "OND" ? 11 : 4, 31))` — identical to the windows annotationMarks already uses, so bars and shading coincide by construction.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2561 (plotFooter → chartDownloadButton) vs helpers/chartDownloadMenu.ojs:640 (csvColumns) — Download-button CSV ignores the curated `columns` spec (key mismatch: notebook passes `columns`, helper reads `csvColumns`)
  PROBLEM: plotFooter forwards its whole opts object to chartDownloadButton, but the notebook's per-figure column spec is under `columns` (e.g. Fig 3.6 prices, qmd:1251-1256: Market/Month/Price/Unit with formatters) while chartDownloadButton reads `o.csvColumns` (helper line 640). The split-button's CSV export therefore dumps every raw field of the data rows (market, period_date, year, month, value_kes, unit — unformatted, unlabeled) instead of the curated table, diverging from the 'Show data table' CSV which respects `columns` via plotDataTable. Two different CSVs for the same figure.
  FIX: In plotFooter (qmd:2549-2564), map the key once: pass `{...o, csvColumns: o.columns?.map((c) => c.key)}` to chartDownloadButton (or teach the helper to accept `columns` objects with key/label/fmt so both exports share the same shaping).


===============================================================================================
REVIEWER: science-integrity-adversary (v2.2 cycle-3 challenge, 4f2ddd1)
VERDICT: Build survives the lag and cache attacks (prodEvtBars 2024-harvest → OND-2023 composite z=+2.20 "Strong El Niño/+IOD" green — verified correct; zSeriesCache county-keying sound; rainfall re-standardisation an exact no-op; thresholds documented in prose). It does NOT survive the 2025 attack: the verbatim dmi_hadisst-only phaseDefs port makes every z-based surface (strength strips, composite shading, fig 3.4 OND panel, contingency intensity rows, annex combined chart) blind to OND-2025 while the coalesced discrete lanes classify it "IOD negative" and RONI classifies it "La Niña" — the page's most recent, most decision-relevant season shows three different states depending on a toggle, and the shipped caption's "HadISST extended with ERSST to reach the present" is false for the strength machinery. Two further majors: SPEI-3 is silently re-standardised (12/45 intensity-class flips in the default county, tooltip z contradicts the plotted SPEI value), and the 3.8 production strip's "short rains (year before harvest)" lane is not lag-shifted, so it visually contradicts the bar fill directly above it. Fix the coalesced-DMI member, the SPEI re-standardisation bypass, and the strip lag before ship; the rest are caption/consistency repairs.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:2304-2306 (phaseDefs members "dmi_hadisst") vs :2658/:2665 (driversCoal/iodPhaseOND, coalesced) vs :2836-2839 (strip fill/tooltip); caption data/KE-enso-explorer/nbText_v2.json line 213 — OND-2025 shows three contradictory states: z machinery is HadISST-only while discrete lanes coalesce with ERSST
  PROBLEM: phaseDefs was ported verbatim with members ["dmi_hadisst"], which ends 2025-04 (verified in driver_indices.parquet). So zSeries has NO OND-2025 entry for "IOD (DMI)" or "ENSO + IOD". Verified consequences on the shipped page: (a) in fig 2.1 the IOD lane flips from "IOD negative" (discrete mode, coalesced DMI = −0.62) to a white "no data" cell (Strength mode) for the same 2025 season; (b) with the default "ENSO + IOD" driver, "Driver strength" shading in §3 draws no rect for OND-2025 at all — the recent RONI La Niña (−0.93), Dry-tercile season reads as unshaded/neutral; (c) fig 3.4's OND beeswarm drops the 2025 dot entirely (line 1068 filters z==null) even though NDVI has 9 OND-2025 dekads; (d) the annex combined-z chart (line 3791, zscore(drivers,"dmi_hadisst")) ends the IOD line 14 months before the other two. Meanwhile the fig-2.1 caption states the DMI is "HadISST extended with ERSST to reach the present" — true only for the discrete lanes, false for every z surface. That caption is a shipped claim the plotted data does not support.
  FIX: Make zSeries and the annex combined chart consume the coalesced series: add a `dmi` member (dmi_hadisst ?? dmi_ersst, i.e. drive zByYear off driversCoal) for "IOD (DMI)" and "ENSO + IOD", coordinating the convention change with the map session (dev_rainfall_maps.qmd:417-421 is the declared source of truth); OR keep HadISST-only and (1) reword the caption to name the strength view's earlier cutoff, (2) render missing years as an explicit hatched/labelled "no index" cell instead of silence, (3) stop dropping the 2025 NDVI dot (plot it unclassified).

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2324-2327 ("seasonal SPEI-3 is already standardised", returns raw values) and :2345-2347 (zSeries re-standardises ALL drivers over 1991-2020) — SPEI-3 county driver is double-standardised: 12/45 intensity-class flips in the default county
  PROBLEM: countyZ('__spei__') returns the seasonal SPEI-03 value as z (comment: already standardised), but zSeries then re-standardises it against the 1991–2020 subset. For Marsabit OND the reference sd is 0.823, inflating every |z| by ~21%. Verified: 12 of 45 years change intensity class (e.g. 1987 and 2006 Moderate→Strong; 2025 raw SPEI −0.95 "Weak Dry" becomes z=−1.19 "Moderate Dry"). So the drought-index bars in fig 2.1 plot −0.95 while every tooltip/strip/contingency row using the "Drought index SPEI-3 (county)" driver reports "Moderate Dry (z=−1.2)" for the same season — two different numbers for one already-standardised index, on one page.
  FIX: Bypass the re-standardisation for __spei__ (return countyZ's map directly when def.members[0]==='__spei__', as the rainfall member effectively is a no-op by construction), or decide re-basing to 1991–2020 is wanted and then (a) delete the misleading "already standardised" comment and (b) disclose the re-basing wherever SPEI z is shown, so the plotted SPEI value and the class tooltips stop disagreeing.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:3641-3644 (prodEvtStrip lane "short rains (year before harvest)", season OND) + :2433-2435 (makeYearStrip: `zm.get(y)` at x=y, no offset) vs :3661/:3664 (bar fill uses `zPrevOnd.get(d.year - 1)`) — 3.8 production strip not lag-shifted: strip cell contradicts the bar directly above it
  PROBLEM: The bar for harvest-year 2024 is filled with OND-2023 composite z=+2.20 (green, "Strong El Niño/+IOD" — correct). But the strip lane labelled "short rains (year before harvest)" plots zm.get(y) at x=y with no shift, so the cell under that same 2024 bar shows OND-2024 z=−0.63 (pale purple, "Weak La Niña/−IOD") with tooltip "short rains (year before harvest) 2024". Two elements at the same x position, both claiming to be the harvest's preceding short rains, in opposite colours — on the checklist's "THE most important plot" (D8). The nbText prodCaption ("bar colour = the strength of the short rains preceding that harvest year; the strip below shows both seasons") papers over the mismatch.
  FIX: Shift the OND lane by the lag: in prodEvtStrip pass values so the cell at harvest-year x shows zm.get(x-1) (e.g. a per-lane `offset` param in makeYearStrip), keeping the MAM lane at x. Alternatively relabel the lane "short rains (OND of that calendar year)" and say in the caption that the relevant OND cell is the one to the LEFT of each bar — but the offset fix is the one that stops misreads.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1099-1104 (fig 3.4 footer: data ndviOndByPhase, filename `_ndvi_ond_by_enso_phase`) + :3132-3137 (ndviOndByPhase uses ensoPhaseOND RONI) + :1062 (panel drivers hardcoded "ENSO + IOD"/"Western-V (WNP)"); caption nbText_v2.json sections.b3.ndviPhaseCaption — Fig 3.4 caption, download and chart use three different classification systems
  PROBLEM: The rendered panels group dots by ENSO+IOD composite z-intensity bins (OND) and Western-V bins (MAM). The caption says dots are "grouped by that season's ENSO state" and describes only the OND panel (the MAM panel isn't mentioned). The download CSV (ndviOndByPhase) carries a third labelling — RONI-based ensoPhaseOND phases — and only OND years, so a user downloading "the data behind this figure" gets phases that don't match the plotted bins (e.g. 2024: RONI "La Niña" in the CSV, composite z=−0.63 "Neutral" bin on screen) and no MAM rows at all. Filename `_by_enso_phase` compounds it. This recreates the dual-classification confusion the R2 fix (one ENSO classification page-wide) was meant to end.
  FIX: Make the download rows come from the same rollup the panels plot ({season, year, ndvi, z, bin, driver}), rename the file `_ndvi_by_driver_intensity`, and rewrite ndviPhaseCaption to name the actual drivers per panel (ENSO+IOD composite for OND; Western-V for MAM) and the 0.5/1.0/1.5 z-bands.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2815-2816 (discrete ENSO lane = ensoPhaseOND, RONI ±0.5) vs :2823-2824 (Strength mode laneDriver ENSO → "ENSO (Niño 3.4)" zSeries); caption nbText_v2.json line 213 documents only RONI — Fig 2.1 ENSO lane switches index when toggling Phase→Strength: 2024 flips La Niña→Neutral
  PROBLEM: The discrete strip classifies OND-2024 "La Niña" (roni_conc = −0.94, verified in enso_outlook_base). Toggling the same lane to "Strength" recolours 2024 near-white with tooltip "Neutral (z=−0.3)" because the continuous view uses raw Niño 3.4 z (−0.33), a different index that diverges from RONI precisely in recent warm-background years. The caption documents the RONI ±0.5 rule for the phase strips but never says the strength view is Niño 3.4-based, so the same lane gives a policymaker two answers for the same season with no stated reason — the exact incoherence the "ONE ENSO classification page-wide" comment (line 2659-2660) claims was fixed.
  FIX: Either build the ENSO strength lane from a RONI z-series (roni_conc is already in outlookBase per year; standardise it 1991–2020 like zByYear) so phase and strength agree, or add to the caption/tooltip that strength uses Niño 3.4 while phase uses RONI and that they can disagree (2024 being the live example).

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:772-781 (groupOf returns null → year lands in no intensity row) and :795-797 ("All years" includes it); slider :757 advertises 2025 — 2.3 contingency silently drops unclassifiable years (incl. 2025) from intensity rows but keeps them in "All years"
  PROBLEM: With the default "ENSO + IOD" (or "IOD (DMI)") short-rains driver, OND-2025 has no z (HadISST gap), so groupOf(2025)=null and the season vanishes from every intensity row while still counting in "All years" — for Marsabit that's a Dry, RONI-La Niña season invisibly excluded. Row totals don't reconcile with the base-rate row and nothing tells the reader n years were unclassified; switching to "ENSO (Niño 3.4)" makes 2025 reappear (Weak La Niña), so the two driver options disagree about whether 2025 exists.
  FIX: Append an "Unclassified (no index)" row (or a one-line note under the panel: "k seasons lack a driver value and appear only in All years") computed from rows where groupOf(year)==null; resolves itself for the composite/IOD case if the critical coalesced-DMI fix lands.

[MINOR] data/KE-enso-explorer/events.json:39-53 (drought-2005: phase "lanina", blurb "Failed short rains under La Niña") vs notebook_v2.qmd:2661-2664 (ensoPhaseOND strict `v < -0.5`; OND-2005 roni_conc = −0.50 → "Neutral") — 2005–06 event band coloured/worded La Niña while the page's own classification calls OND-2005 Neutral
  PROBLEM: The named-event layer paints 2005–06 with the La Niña band colour and the blurb asserts "under La Niña", but the page-wide RONI rule (strict inequality at exactly −0.50) classifies OND-2005 "Neutral" — so the ENSO phase strip under fig 2.1 shows a grey Neutral cell inside a La Niña-coloured event band for the same season. A story-layer claim the page's own single classification contradicts, and it hinges on a knife-edge strict `<` against a value stored as exactly −0.50.
  FIX: Reword the blurb to the borderline truth ("Failed short rains with a borderline/weak La Niña and a cool western Indian Ocean") or use `<=` if the intent is to include the boundary — either way make the event layer and ensoPhaseOND agree for 2005.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:866-869 ("applies to every chart background in this section (Pete P19)") + :1062 (ndviSwarmPanel hardcodes driver per season); nbText b3.intro promises the controls change "what the chart backgrounds show"; checklist V2_CYCLE3_CHECKLIST.md:84 (D3: driver selector for 3.2–3.9) — eventDriver does not steer fig 3.4 despite the section-wide promise and checklist D3
  PROBLEM: The §3 intro tells readers the driver control (including the county rainfall/SPEI lens) governs the section's charts, and checklist D3 scopes the driver framework to 3.2–3.9 — but ndviSwarmPanel fixes "ENSO + IOD" (OND) and "Western-V (WNP)" (MAM) regardless of the selector. A user who switches to "Season rainfall (county)" to read greenness against the KMD-style rainfall lens sees 3.4 silently ignore them.
  FIX: Either honour eventDriver in ndviSwarmPanel (default to the current per-season choices when the selector is on the composite) or state the exception in ndviPhaseCaption/b3.intro ("fig 3.4 fixes each season's dominant driver") so the control's promise matches behaviour — the deliberate D4/D5 design suggests the caption route.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2312-2318 (zByYear averages whatever months are non-null; no month-count guard) — Partial-season means silently classified: MAM-2025 IOD z built from March+April only
  PROBLEM: dmi_hadisst ends 2025-04, so zByYear's MAM-2025 "seasonal mean" is a 2-of-3-month average (verified z=0.47, a whisker below the 0.5 Weak threshold; composite MAM-2025 = 0.24). Any surface showing MAM strength for the IOD/composite driver presents this partial value as a full season with no flag; the same failure mode will recur every year at the HadISST data edge and can flip a category.
  FIX: In zByYear require the full month set before emitting a year (e.g. `(v) => v.length === mons.length ? d3.mean(...) : null` via a count check), or mark partial years in tooltips as "(partial season)"; coordinate with dev_rainfall_maps.qmd:462 since the helper is shared convention.


===============================================================================================
REVIEWER: Final reader pass (CECM + analyst) — adversarial cycle-3 challenge on notebook_v2.2 (4f2dd
VERDICT: BLOCK — two critical defects: a missing OJS fence kills Figure 1.4 (GESI) and the flagship Figure 3.8 at runtime (verified in the rendered HTML: the code ships as literal text, not cells), and the §4 honest-box types the analogue count as "eight" when the served data computes 14. Plus 3 major reader-flow defects (broken GESI-annex promise/A5.1 gap, globally-styled Season control that is inert across §2.3–§5 with its scope stated nowhere, and a verbatim duplicated paragraph opening §5) and a tail of caption/download mismatches.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:3615-3710 (breaks Figure 1.4 at :527/:571 and Figure 3.8 at :1324/:1352/:1361) — Missing ```{ojs} fence: Fig 3.8 machinery + GESI data cell never become OJS cells — two figures dead on the shipped page
  PROBLEM: The produce-machinery cell closes at line 3615, but the next block (line 3618, '// ---- 3.8 flagship machinery ----' through prodEvtTable at 3709) has NO ```{ojs} opener — it is plain markdown. The bare ``` at line 3710 then OPENS a plain code fence that swallows the entire GESI cell (3712–3743, including its ```{ojs} line, which per CommonMark cannot close a fence because it carries an info string). Verified in _site/notebooks/KE-enso-explorer/notebook_v2.html: the flagship code is emitted as '<p>// —- 3.8 flagship machinery —- naprCtyFull = …' and the GESI cell as '<pre><code>```{ojs} // ---- GESI ---- dbGesi = …'. So naprCtyFull, prodEvtCrops, prodEvtDefault, prodEvtRowsAll, prodEvtStrip, prodEvtBars/Lines/Table, dbGesi, gesiAll, gesiSeries and gesiExport are all undefined at runtime. Figure 1.4 (gesiRiskCard references gesiSeries; its plotFooter references gesiExport), Figure 3.8 (renderToDiv → prodEvtBars; plotFooter → prodEvtRowsAll) and the §3.8 commodity multiselect (viewof prodEvtPick → prodEvtCrops/prodEvtDefault) all throw 'X is not defined' — spinner/error on the live page. Quarto render passes because OJS errors are client-side only; headless verification is untrustworthy for this stack, which is how it slipped through.
  FIX: Insert a ```{ojs} opener between line 3615's closing fence and the '// ---- 3.8 flagship machinery ----' comment (line ~3617). The existing bare ``` at 3710 then correctly closes that cell and the GESI cell at 3712–3743 parses normally. Re-render and confirm in the HTML that 'flagship machinery' no longer appears inside a <p> tag and that no <pre><code> block contains '```{ojs}'.

[CRITICAL] data/KE-enso-explorer/nbText_v2.json:502 (b4.canCannot.cannot), rendered via notebook_v2.qmd:1649 — Typed numeric claim 'eight similar past years' contradicts the live computation (14) and goes stale with every forecast change
  PROBLEM: The §4 honest box says '…eight similar past years shift odds, nothing more.' The analogue count is computed live (analogueYearsOND): with the served enso_state_probabilities (issued July 2026, forecast phase El Niño) and enso_outlook_base RONI values, the count is 14 (1982, 1986, 1987, 1991, 1994, 1997, 2002, 2004, 2006, 2009, 2014, 2015, 2018, 2023) — verified by direct DuckDB query of the shipped parquets. 'Eight' is a hand-typed number that (a) is wrong today, (b) will drift again whenever the CPC forecast phase flips (La Niña → 18, Neutral → 13). This violates the project's hard no-LLM-typed-numeric-claims rule in the story layer, on the very box whose job is honesty.
  FIX: Remove the number from the static string — e.g. '…a handful of similar past years shift odds, nothing more' — or template it like the other insight sentences (tpl with {n} filled from analogueYearsOND.length) so the count is always the computed one.

[MAJOR] notebook_v2.qmd:2005-2089 (A5) vs nbText_v2.json b1.gesiAbout (:187), annex.intro (:559), annex.a5Title (:643); fig ids A5.2/A5.3 with no A5.1 — Full GESI table promised three times but absent from A5 — and the annex figure numbering jumps A5.1
  PROBLEM: b1.gesiAbout tells the reader 'The full table, with every county's distribution, is in the technical annex'; annex.intro promises '…the complete GESI table…'; A5's own title is 'County detail: GESI, conflict, trade flows'. But the A5 section contains only the ACLED chart (fig A5.2) and the trade-flow map (fig A5.3) — no GESI content at all, and the A5.1 slot is an empty numbering gap (nbText still carries the orphaned gesiFullTitle/gesiFullCaption at :646-652). A reader sent to the annex for the full distribution table finds nothing.
  FIX: Either restore the full GESI distribution figure as A5.1 (its nbText strings still exist), or — if 1.4's 'All indicators' toggle is the intended replacement — rewrite the three promises to point at Figure 1.4, drop 'GESI' from a5Title, delete the orphaned gesiFull* strings, and renumber A5.2/A5.3 → A5.1/A5.2.

[MAJOR] notebook_v2.qmd:164-190 (sticky bar), season used only by rainActiveSeasons (:2612 → Fig 2.1), driverSeasonal/ensoPhaseByYear (:2645-2677 → A1.1) and A3 (:1952) — Sticky 'Season' control looks global but is inert across §2.3, §3, §4 and §5 — and its reduced scope is stated nowhere
  PROBLEM: The Season select sits in the always-visible sticky bar next to County and the KMD forecast note, so it reads as page-wide. In fact it drives only Figure 2.1, annex A1.1 and annex A3 (A3's caption alone says 'the season selected at the top of the page'). §2.3 has its own year-range and per-panel drivers, §3 is fixed OND+MAM, §4 is OND-only by design. A reader parked in §3/§4 who flips Season to 'Long rains' sees zero change anywhere on screen — the classic looks-global-acts-local trap, and the adjacency to the 'Season forecast: KMD' note makes it look outlook-relevant. The only sentence that ever explained scope ('the season follows the control at the top of the page') lives in the now-unrendered explore.compareIntro (nbText :517).
  FIX: Either scope-label the control in the sticky bar (e.g. small text under it: 'applies to Figs 2.1, A1, A3') or move it out of the sticky bar down to Figure 2.1's control row (its only body consumer) and keep a local copy in the annex. Also delete the dead explore.compareIntro/rainTitle/rainCaption/ndviTitle/ndviCaption strings.

[MAJOR] notebook_v2.qmd:1655 (explore.intro) + :1662 (explore.tradeIntro); nbText_v2.json :513 and :541; heading pair :2573/:2593 — §5 opens with the same paragraph twice — intro and tradeIntro are near-verbatim duplicates under near-duplicate headings
  PROBLEM: Section 5 renders explore.intro ('National, not county: Kenya is a structural maize importer (Uganda, Tanzania), and imports surge when seasons fail — as in the 2021–22 drought. Measured at national border points (FEWS NET); the flow map is in the technical annex…'), then the heading '5.1 national context — Kenya's cross-border food trade', then explore.tradeIntro — which repeats the first two sentences word-for-word. On top of that, the 5.1 title restates the H1 ('5. National Context: Cross-Border Trade'), so the reader hits 'national context / cross-border trade' four times in five lines. Reads as an editing slip in the shipped prose.
  FIX: Keep explore.intro (it carries the extra relocation notes: county comparison → §3, no price series yet) and cut tradeIntro to one non-duplicative sentence or delete it; retitle 5.1 to something non-repeating (e.g. 'imports and exports by partner') or drop the 5.1 subheading since §5 has a single figure.

[MINOR] nbText_v2.json :245 (contingencyCaption) and :427 (prodAbout); notebook_v2.qmd:831 and :1366 (download meta) — Internal dev references leak to readers: 'the rainfall-maps work' in captions, 'dev_rainfall_maps' in shipped download metadata
  PROBLEM: Fig 2.3's caption and Fig 3.8's About both justify the intensity classes as 'the same classes as the rainfall-maps work' — a reference to an internal parallel session that isn't on the page (§2 only says the map views 'are being developed separately'). Worse, the download metadata strings bake the internal filename into shipped artifacts: Fig 2.3 meta Source = '…(intensity classes as dev_rainfall_maps)' and Fig 3.8 meta = '…driver intensities per dev_rainfall_maps conventions' — these lines are written into every downloaded CSV header and PNG metadata.
  FIX: Replace with a self-contained gloss, e.g. 'weak/moderate/strong at 0.5/1.0/1.5 standard deviations from the 1991–2020 mean — the same classes used throughout this notebook', and strip 'dev_rainfall_maps' from both meta strings.

[MINOR] notebook_v2.qmd:826-833 (Fig 2.3 plotFooter, data: countyContingency) vs contingencyPanel (:765-816) and countyContingency (:3054-3080) — Fig 2.3's downloadable data table does not match the chart: different grouping, ignores the driver selectors and year range
  PROBLEM: The chart groups the county's seasons by the selected drivers' intensity classes (Strong/Moderate/Weak pos/neg + Neutral) over the user-chosen From/To years. The 'Show data table' / CSV download (countyContingency) instead serves fixed ENSO-phase (El Niño/Neutral/La Niña) and W-V-phase groupings over ALL years, ignoring contOndDriver, contMamDriver, contYearFrom and contYearTo. An analyst downloading 'the data behind Figure 2.3' gets different groups and different counts than what is on screen.
  FIX: Build the download rows from the same code path as the panels (reuse contingencyPanel's `out` array for both seasons with the live driver/year-range inputs), or at minimum label the table 'phase-based summary, all years — differs from the chart grouping'.

[MINOR] notebook_v2.qmd:648-653 (phaseChips) vs nbText_v2.json :209 (rainCaption) — Fig 2.1 phase chips colour 'warmer than normal' teal while the caption promises orange = warmer
  PROBLEM: rainCaption states 'the temperature view uses orange = warmer, blue = cooler' and rainPanel honours that (outcomeRange switches to [PALETTE.impact.bad, '#4575b4'] for TAVG). But phaseChips' colour line is a ternary with two IDENTICAL branches — `(p === "El Niño" || …) ? (dev >= 0 ? wetter : drier) : (dev >= 0 ? wetter : drier)` — so with Temperature selected, a chip like 'El Niño: +0.6 °C vs normal' renders in teal (the WETTER colour) directly beneath bars where warmer is orange. Contradictory colour semantics inside one figure.
  FIX: In phaseChips, branch on varIsTemp: use PALETTE.impact.bad for dev >= 0 and '#4575b4' for dev < 0 (matching rainPanel), and delete the dead identical-branch ternary.

[MINOR] notebook_v2.qmd:669-681 (Fig 2.1 caption selection) + rainPanel :2811-2820; nbText rainCaption :209 — With Season = 'Full year', Fig 2.1's caption promises ocean strips and event dots that the annual panel does not draw
  PROBLEM: For the 'annual' period rainPanel sets hasStrip = false (no ENSO/IOD strips) and evYears is empty (no event season starts with 'annual-'), yet the same rainCaption is shown, claiming 'The strips underneath carry the year axis and the ocean state that season — switch them between discrete phases and continuous strength. Dots mark the named events.' The stripMode control also becomes inert in this view.
  FIX: Swap in a short annual-specific caption (as already done for the monthly-climatology view via monthlyCaption), or append a conditional sentence when seasonCode === 'annual' noting strips/dots apply to the seasonal views only.

[MINOR] notebook_v2.qmd:970-976 (Fig 3.2, data: spei12Rows) and :1098-1104 (Fig 3.4, data: ndviOndByPhase) — Figs 3.2 and 3.4 download only part of what the chart shows
  PROBLEM: Fig 3.2 plots two panels — seasonal rainfall anomalies (bars, from chirpsYearlyAll) plus the SPEI-12 strip — but its download/data-table serves spei12Rows only; the rainfall-anomaly series the reader sees on top is not in the CSV. Fig 3.4 plots OND (and by default MAM) NDVI grouped by driver-INTENSITY bins, but downloads ndviOndByPhase: OND-only, grouped by simple ENSO phase — neither the MAM panel nor the intensity bins the chart displays.
  FIX: Fig 3.2: export a combined long table (year, series = 'OND anom'/'MAM anom'/'SPEI-12', value). Fig 3.4: export the `rows` actually plotted per panel (year, season, ndvi, z, bin), including MAM when shown.

[MINOR] notebook_v2.qmd:1641; heading title_contingency :2581 (no {#id}) — The MAM 'no outlook' pointer links '[Figure 2.3](#drivers)' to the top of §2, not to the figure
  PROBLEM: The §4 pointer — the one place a policymaker looking for long-rains guidance is redirected — anchors to #drivers, which is the §2 H1. The reader lands on the section intro and Figure 2.1 and must scroll past 2.1 and 2.2 to find the contingency panels the text is actually citing. title_contingency has no {#id} to target.
  FIX: Add an id to the 2.3 heading (## `{ojs} title_contingency` {#contingency}) and point the link at #contingency.

[MINOR] nbText_v2.json :209 ('±1 sd') and qmd :625 (toggle label); strip tooltips qmd :2427/:2838 ('z=…'); b3.intro :281 ('their composite') vs control option 'ENSO + IOD' (:867) — Body-layer jargon: 'sd' and 'z=' unglossed at first use; intro says 'composite' but the control says 'ENSO + IOD'
  PROBLEM: First body occurrence of 'sd' is Fig 2.1's caption/toggle '(±1 sd)' — 'standard deviations' is only spelled out later in the 2.3 caption. The driver-strength strip tooltips print raw 'z=0.8' in story sections while z-scores are explained only in annex A1. And §3's intro invites readers to pick 'their composite' as a driver, but the dropdown option is labelled 'ENSO + IOD' — a non-technical reader may not connect the two.
  FIX: Spell out 'standard deviation (sd)' once in the Fig 2.1 caption/toggle; change tooltip text to e.g. 'strength 0.8 sd' or drop the z; in b3.intro write 'their combined state ("ENSO + IOD" in the selector)'.

[MINOR] notebook_v2.qmd:2091 (## title_downloads {#data-sources} nested inside # heading_annex at :1699, between A5 and A6) — 'Data downloads' sits unnumbered inside '6. Technical Annexes' between A5 and A6, breaking the annex rhythm
  PROBLEM: The TOC reads: A1, A2, A3, A4, A5, 'Data downloads', 'A6 · Methods, data & citation'. Downloads is the only H2 in the annex without an A-number, and it is page-level material (every dataset behind the notebook) rather than annex detail — the checklist asked for downloads-before-methods, which is satisfied, but the unnumbered insertion mid-sequence reads as misfiled rather than deliberate.
  FIX: Either promote Downloads to its own top-level section between §5 and the Technical Annexes, or number it into the sequence (e.g. retitle A6 → 'A6 · Data downloads' and methods → 'A7 · Methods, data & citation').
