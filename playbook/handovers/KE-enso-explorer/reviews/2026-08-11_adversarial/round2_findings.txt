
====================================================================================================
REVIEWER: fix-verification auditor (round 2) — verified critique.txt P1–P10 + all 24 panel [CRITICAL] findings
VERDICT: Most of the priority-10 is genuinely delivered: spine/annex restructure, VoP + calendar restorations, event layer, MAM-outlook removal, >99% cap, issuance date, staleness banner, conflict-to-annex, no watchlist, no season toggle on the outlook, local driver radio. But the panel's only unanimous fix — the click-picker — is shipped broken for 3 of 47 counties (topojson name drift bricks the whole county subgraph on click), and the flagship A2 correlation choropleth paints the OPPOSITE sign of its own legend. The highest-priority integrity fix (off-diagonal guard) fires but the headline card still says LEANING WETTER unconditionally, and the analogue nearest-neighbour ranks RONI values against an ONI reference, quietly biasing the analogue set the fix was meant to de-bias.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:158-161 (also 156, 1048, 1451) — P10 #1 click-picker breaks the page for Elgeyo Marakwet, Murang'a, Tharaka-Nithi; Ilemi Triangle clickable too
  PROBLEM: Click handler does `sel.value = f.properties.admin1_name` but the topojson spells these `Elgeyo-Marakwet`, `Murang'A`, `Tharaka-Nithi` (plus a 48th feature `Ilemi Triangle`), while the select's options are the canonical 47 (`Elgeyo Marakwet`, `Murang'a`, `Tharaka Nithi`) — verified: set-difference of topo names vs county_key is exactly those 4. Per the known Inputs.select gotcha, assigning a non-option string yields county=undefined → `countyNorm(county)` throws → every county-dependent cell errors (stuck page). The `admin1_name === county` highlight comparison (picker, outlook map, corr map) also never matches for these 3 counties, so 'The selected county is outlined' is false for them.
  FIX: Resolve clicks through gaul1_code: `canonByGaul = new Map(counties.map(d=>[+d.gaul1_code,d.county]))`; on click `const name = canonByGaul.get(+f.properties.gaul1_code); if (name) {sel.value = name; dispatch}` (Ilemi gets no entry → inert, or set pointer-events:none). Highlight with `+f.properties.gaul1_code === selGaul` in all three maps.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:1440 vs 1458-1459 — A2 correlation map fill is sign-inverted relative to its legend
  PROBLEM: Fill scale is `d3.scaleDiverging([-0.8, 0, 0.8], (t) => d3.interpolateBrBG(1 - t))` — r=+0.8 → t=1 → BrBG(0) = brown — while the legend uses `interpolate: (t) => d3.interpolateBrBG(t)` (+0.8 = teal) and the inline comment says '+ = wetter with +index = teal'. Pooled OND ENSO r is +0.40 (verified on enso_outlook_base), so the delivered P10 #9 evidence map paints most of Kenya BROWN for a positive teleconnection, directly contradicting its legend and the notebook's central claim.
  FIX: Drop the `1 - t`: `d3.scaleDiverging([-0.8, 0, 0.8], d3.interpolateBrBG)` so map and legend agree and + = teal, as the comment intends.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2238-2243 — Nearest-neighbour analogue ranking compares RONI to an ONI reference; roni_pred unused
  PROBLEM: `const ref = currentState.oni ?? 0` (3-month mean of nino34_anom_noaa, +0.88) is used as the target for `Math.abs(a.roni - ref)` over roni_conc values. RONI runs systematically cooler than ONI in the recent warm decades, so the cross-index distance biases the 8-analogue set toward stronger El Niños — partially undoing the D-E nearest-neighbour fix. The parquet carries `roni_pred` (the CPC forecast RONI) exactly for this, and it is never queried; the About text claims 'ranked by similarity to the current index value' on the same index.
  FIX: Query roni_pred from enso_outlook_base and use it (or a computed current RONI) as `ref`; keep ONI only for the state chip.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1096-1106 (outlookCard) + 216-218 (glance tile) — Off-diagonal guard fires but the headline card and glance outlook tile stay unconditional
  PROBLEM: D-E (ratified): when ENSO and IOD disagree 'the card must say so, widen stated uncertainty'. `signalsMixed` is true in the current live state and the separate warnbox renders, but `outlookCard` still prints `LEANING WETTER` at 26px with no mixed-signal wording or widened uncertainty, and the B1 glance 'Short rains outlook' tile shows the same unconditional lean (only the Ocean-now tile gets the ⚠ flag). A reader who screenshots or prints the card alone gets exactly the overconfident headline the guard was meant to kill.
  FIX: Thread signalsMixed into outlookCard (e.g. prefix 'MIXED SIGNALS — on ENSO history alone, leaning wetter', add the IOD-analogue lean line inside the card) and add the ⚠ to the glance outlook tile, not just the state tile.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2697 + 856-870 (prodEventPanel) — 3.7 production panel: same-year OND phase colouring inverts the El Niño-recovery story and violates D-F
  PROBLEM: `prodYearPhase = ensoPhaseOND` colours the year-Y bar by OND-of-year-Y, but OND rains produce the NEXT year's harvest. Verified from driver_indices: OND-2023 = +1.92 (El Niño), OND-2024 = −0.37 (Neutral) — so the drought-carryover 2023 harvest is painted El Niño red and the 2024 El Niño-recovery harvest paints grey, visually contradicting the panel's own typed legend '2023–24: El Niño recovery'. It also fills OUTCOME bars with phase colours, which D-F forbids ('never as fills of outcome marks'), and the caption says the ocean state is 'underneath' when there is no strip.
  FIX: Neutral bar fill + a phase strip lane under the bars (reuse rainPanel's strip), lag-aligned: harvest year Y ← OND(Y−1) (and/or MAM(Y)); update legend/caption to state the lag.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:458 (mapsComing placeholder) / B2 — P10 #9 partial: phase-composite anomaly maps and P(Wet|phase) map not built and not covered by the map-session placeholder
  PROBLEM: D-D bullet 1 promotes 'phase-composite rainfall-anomaly maps (El Niño|Neutral|La Niña × OND; Western-V × MAM)' into story beat 2, Wave 2, 'all client-side from enso_outlook_base (served today)'. B2 ships only the per-county bars/contingency; nbText b2.mapsComing covers a 'season-by-season rainfall map explorer … through time' — the parallel session's timeline/subcounty scope, not the phase composites. The D-D 'map of P(Wet|phase) with n<8 masked' is also absent. The spatial demonstration of the teleconnection — the panel's core 'asserted, never demonstrated' critical — is still missing.
  FIX: Add the composite-map triptych (group enso_outlook_base anomaly_pct by fixed phase, mean per county per phase, n printed per panel, BrBG anchored) in B2, or get an explicit owner ruling recorded that the map session owns it and extend the placeholder text to say so.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1965 vs 2215 — Two different ENSO-phase definitions on one page misclassify years (e.g. 2019)
  PROBLEM: Story charts (2.1 strips, 2.3 contingency, 3.3, 3.7) classify phase via `ensoPhaseOND` on nino34_anom_noaa ±0.5, while the outlook/A2 use `ensoPhaseOf` on roni_conc ±0.5. Verified: OND-2019 nino34 mean = +0.507 → the strips paint 2019 'El Niño', yet the notebook's own events.json blurb for 2019 says 'without a strong El Niño' (RONI-based would be Neutral). Same year, two phases, one page — an honesty problem in a product whose whole premise is phase classification.
  FIX: Standardise all phase classification on one index (RONI, per methods text) — derive ensoPhaseOND from roni_conc (or serve a monthly RONI column) so strips, contingency and outlook agree.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1857-1872 (plotFooter) + nbText general.dataAsOf — P10 #10 partial: no per-figure 'data as of' stamps, no refresh runbook
  PROBLEM: Staleness banner + CPC issuance date are delivered, but the strategy's figure-level acceptance ('every figure has … a data-as-of stamp') is unmet: plotFooter renders no stamp and the prepared nbText key `general.dataAsOf` ('Data as of') is never referenced in the qmd (grep: 0 uses). The documented refresh runbook (CPC monthly · IPC/NAPR per release · prices monthly · NDVI dekadal) exists only as a line in the strategy, not as a playbook artifact.
  FIX: Add an optional `asOf` to plotFooter meta rendered next to the caption (derivable per dataset max date), and write the runbook into playbook/handovers/KE-enso-explorer/ (README or a RUNBOOK.md).

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd + nbText_v2.json (absent) — P10 #6 partial: tiered glossary missing; orphaned statusTitle key
  PROBLEM: grep for glossary in qmd and nbText_v2.json returns nothing — the plain-language pass shipped season labels and no-r/n/p but dropped the 'tiered glossary' the critique and Wave-1 plan both name. nbText also carries a dead `b4.statusTitle` ('the ocean right now') never referenced by the qmd.
  FIX: Add a small expander glossary (ENSO, IOD, Western-V, tercile, analogue, SPEI, IPC phase, NDVI, VoP) near the intro or B2, sourced from nbText; delete or use b4.statusTitle.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:189-231 (glanceCard) + 137-175 (picker) — P10 #1 partial: no persistent mini locator; picker not keyboard-operable
  PROBLEM: Strategy B1 specifies the at-a-glance card includes a 'locator'; the glance card has state/outlook/last-season/VoP tiles but no mini-map, and nothing persistent locates the county once the reader scrolls past 1.1. The picker SVG has role=img/aria-label but county paths have no tabindex/keydown — the critique's #1 explicitly adds 'the aria/keyboard support every persona forgot' (dropdown fallback exists and is referenced, which mitigates).
  FIX: Add a ~90px locator SVG tile to the glance card (reuse kenyaA1 + selGaul); give picker paths tabindex=0 + Enter/Space handler, or explicitly frame the dropdown as the keyboard path in b1.mapIntro.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1193-1207, 1222-1236 — P10 #2 partial: §5 compare charts lack the event annotation layer
  PROBLEM: The shared annotation helper is applied to 3.2–3.8 but critique #2 says 'applied to the production charts, all six §4 panels AND §5'; cmpRainChart and cmpNdviChart have no eventBandMarks/eventBandMarksYear, so the compare view loses exactly the named-event context the device exists to provide.
  FIX: Add `...eventBandMarksYear({y1:0, y2: ymax})` (year-x variant) to both compare charts.

[MINOR] nbText_v2.json .sections.explore.tradeIntro vs notebook_v2.qmd:1583 — Broken in-page anchor #annex-trade
  PROBLEM: explore.tradeIntro links 'the flow map is in the [technical annex](#annex-trade)' but no element carries that id — A5 is `{#annex-gesi}` (grep of rendered HTML: 0 hits for annex-trade). The link silently no-ops.
  FIX: Either point the link at #annex-gesi (retitle risk) or add an explicit anchor on the trade-map subsection.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1883-1908 + nbText b1–annex titles — Numbering typed in three places, and a visible A3→A5 gap, contradicting the 'derived, never typed' decision
  PROBLEM: Strategy §3: 'section/figure numbers derived, never typed'. v2 types them in nbText H1 titles ('1. What's At Stake…'), in qmd title vars ('1.1 …'), and per-figure (`fig: "1.1"`) — three sync points for the drift the rule was written against. The annex also jumps 'A3 · …' to 'A5 · …' with no A4 placeholder, which reads as an error to the shipped audience even though A4 (subcounty/stations) is legitimately Wave-3.
  FIX: Derive H2/figure numbers from one ordered registry cell (or at minimum strip numbers from nbText titles so they live only in the qmd), and add a one-line 'A4 · Subcounty maps & station validation — in preparation' placeholder.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2605-2619 (prodBars), 2647-2662 (prodTree) — P10 #7 partial: not-reported markers only on the line/event views; Bars/Treemap silently drop missing items
  PROBLEM: The critique asked for 'explicit not-reported markers on EVERY KNBS/AFA chart' via a shared missingMark() helper. prodLine and prodEventPanel have inline ∅ markers, but prodBars/prodTree filter `d.val > 0` for the latest year, so a selected crop with no latest-year report simply vanishes — the default Bars view can read absence as zero in a drought year, the original blank≠zero violation. There is also no shared helper; each chart re-implements the marker.
  FIX: In Bars, render a greyed '∅ not reported' row for checked items missing prodLatest; footnote the Treemap ('items not reported in YYYY are absent, not zero'); factor the marker into one missingMark() cell.


====================================================================================================
REVIEWER: data-contract-sql
VERDICT: v2's data contracts are mostly clean (int64s are cast or Number()-wrapped, county joins normalise both sides, no NAPR/AFA double-count), but the page has two render-breaking defects and one page-wide error: the redesigned headline season chart draws only 2 of ~45 bars because its band-scale domain is a d3.extent pair (verified against @observablehq/plot), and the A5 ACLED chart references `acledCty`/`dbAcled` which were never carried over from v1 — persistent OJS errors that hold the boot spinner to the 60 s cap. Clicking 4 of the 48 drawn polygons (Elgeyo-Marakwet, Murang'A, Tharaka-Nithi, Ilemi Triangle) nulls the county select and TypeErrors the entire county subgraph, and the annex correlation map paints positive r brown while its legend says teal — inverting the notebook's central El Niño→wetter evidence.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:2093 (rainPanel main), :2118 (phase strip) — Headline season chart renders only 2 of ~45 year-bars (band domain = extent pair)
  PROBLEM: `Plot.barY` forces a band x-scale, but the domain is `x: {domain: yrDomain, ...}` where `yrDomain = d3.extent(data, (d) => d.year)` — a TWO-value array [1981, 2025]. Every year not literally equal to the min or max falls outside the ordinal domain and is dropped. Verified with @observablehq/plot in node: 45 bars in, `bars rendered: 2`. The phase-strip `Plot.cell` at :2118 uses the same `x: {domain: yrDomain}` so each ENSO/IOD/W-V lane also shows 2 cells. This is the flagship B2 figure (2.1) in both anomaly and absolute views, for all variables and seasons. (Compare prodEventPanel:848 which correctly passes the full `d3.range(2019, 2025)` array.)
  FIX: Use the full year list as the band domain — `x: {domain: d3.range(yrDomain[0], yrDomain[1] + 1), ...}` — in both the main plot (:2093) and the strip (:2118); or switch to `Plot.rectY(..., {x: 'year', interval: 1})` on a continuous scale.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:1660,1670 (acledChart / its plotFooter) — acledCty and dbAcled are referenced but never defined — two permanently-errored cells, spinner to 60 s cap
  PROBLEM: A5.2 uses `Plot.barY(acledCty, {x: "year", y: "fatalities", ...})` and `data: acledCty`, but no cell in notebook_v2.qmd defines `acledCty` or `dbAcled` (grep: only the 2 consuming references; the appendix has DuckDB clients for every other parquet but none for acled_conflict.parquet). v1 (notebook.qmd:1712–1724) has the needed cell: `acledCty = dbAcled.query("SELECT CAST(SUBSTR(week, 1, 4) AS INT) AS year, CAST(SUM(fatalities) AS INT) AS fatalities ...")` — dropped in the port even though the strategy ratifies the ACLED annex chart (mapping table line 200). Per the boot contract (`_include.html`, dispatch 2026-07-15), persistent OJS errors keep the loading overlay up until the 60-second cap on EVERY page load, not just in the annex.
  FIX: Carry v1's `dbAcled = DuckDBClient.of({acled_conflict: FileAttachment("/data/KE-enso-explorer/acled_conflict.parquet")})` + `acledCty` query cell (with `${normExpr("county")} = '${countyNorm(county)}'`) into the v2 hidden appendix. Note acled_conflict has `week` VARCHAR + BIGINT fatalities, so keep the CAST(... AS INT) wrappers.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:158-161 (countyPickerMap click handler) — Clicking Elgeyo-Marakwet, Murang'A, Tharaka-Nithi or Ilemi Triangle nulls the county select and crashes every county-dependent cell
  PROBLEM: The click handler does `sel.value = f.properties.admin1_name; sel.dispatchEvent(new Event("input", ...))`. The topojson spells 4 of the 48 KEN features differently from `county_key` (verified: topo has 'Elgeyo-Marakwet', "Murang'A", 'Tharaka-Nithi', 'Ilemi Triangle'; countyNames has 'Elgeyo Marakwet', "Murang'a", 'Tharaka Nithi', and no Ilemi). Verified with @observablehq/inputs in node: assigning a non-option string sets `sel.value` to null. The dispatched input then propagates `county = null` into `countyNorm(county)` (:1925 `c.toLowerCase()`) → TypeError cascades through rainSeasonData, calCty, ndviCty, ipcCty, pricesCty, totRows, vopCty, naprCty, gesiSeries... i.e. the whole page. Ilemi Triangle is clickable because chirps_county contains it (gaul 1351), so the rainOND/rainMAM picker metrics colour it.
  FIX: Map topo names to canonical before assignment: build `const canonByNorm = new Map(counties.map((d) => [countyNorm(d.county), d.county]))` and use `const name = canonByNorm.get(countyNorm(f.properties.admin1_name)); if (name) { sel.value = name; sel.dispatchEvent(...); }` — which also skips Ilemi. Apply the same normalisation to the `admin1_name === county` highlight tests (:156-157, 1048, 1451) and to `kenyaNeighbors` (:2013-2028), which currently miss the same 3 counties.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:1440 vs :1458-1459 (corrMap) — Annex correlation map fill is inverted relative to its own legend (positive r painted brown, legend says teal)
  PROBLEM: Fill: `d3.scaleDiverging([-0.8, 0, 0.8], (t) => d3.interpolateBrBG(1 - t))` — verified in node: scale(+0.8) = rgb(84,48,5) (dark brown), scale(-0.8) = teal. Legend: `Plot.legend({color: {..., interpolate: (t) => d3.interpolateBrBG(t)}})` — shows +0.8 as teal. The code comment on :1440 says "+ = wetter with +index = teal", so the fill is the wrong one. Kenya's OND ENSO/IOD correlations are predominantly positive, so the map will paint the country brown (reads 'drier under El Niño') directly under a legend claiming the opposite — inverting the notebook's central teleconnection claim in the evidence annex (A2.1) and contradicting the ENSO-leads framing.
  FIX: Drop the `1 - t` inversion: `d3.scaleDiverging([-0.8, 0, 0.8], d3.interpolateBrBG)` so positive r renders teal, matching the legend and the physical story. Keep the legend as is.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2238-2242 (analogueYearsOND) with :1940 (currentState.oni) — Analogue nearest-neighbour matching compares a JJA raw-Niño3.4 mean against OND-concurrent RONI — roni_pred exists for this and is unused
  PROBLEM: `const ref = currentState.oni ?? 0; ... matched.sort((a, b) => Math.abs(a.roni - ref) - Math.abs(b.roni - ref))` — `ref` is the mean of the last 3 monthly `nino34_anom_noaa` values (Apr–Jun 2026, +0.88, raw anomaly), while candidates are `roni_conc` = OND-CONCURRENT RONI. Two mismatches: index family (RONI runs materially lower than raw Niño3.4 in recent warm years: 2023 OND roni_conc=1.49 vs actual ONI ~1.9) and season (El Niños grow toward OND: 1997 roni_pred 2.01→conc 2.36; 2018 pred 0.16→conc 0.77). Net effect verified against the parquet: the NN-8 set becomes {2006, 2018, 1987, 2004, 2014, 1994, 2009, 1986}, excluding every major El Niño (1997, 2015, 2023, 1982) despite a 100% CPC forecast — systematically biasing the headline outlook card/map toward weak-event outcomes. It also contradicts the notebook's own methods text (nbText_v2 outlookAbout/methods.outlook: "ranked by relative-Niño-index similarity") — the ref is not a RONI. The parquet ships `roni_pred` (predictor-season RONI) precisely for like-for-like matching, unused.
  FIX: Match like against like: rank candidate years by |roni_pred − current-season RONI| (current RONI from the CPC RONI product already cited in enso_state_probabilities.source_url, or add it to driver_indices), or at minimum by |roni_pred − currentState.oni| so both sides are predictor-season values. Update the card's "current Niño 3.4 +0.88 °C" line to name the index actually used.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1966 (iodPhaseOND) — IOD strip lane fabricates 'Neutral' for OND 2025 — a strong negative-IOD season — because it reads HadISST only
  PROBLEM: `iodPhaseOND = phaseByYearFor([10, 11, 12], "dmi_hadisst", 0.4, ...)` uses `dmi_hadisst`, which ends 2025-04 (verified). CHIRPS OND runs through 2025, and `rainSeasonData` fills missing phases with "Neutral" (`iodPhase: iodPhaseOND.get(r.year) ?? "Neutral"`, :2064). Verified: ERSST DMI OND-2025 mean = −0.62, i.e. a firmly negative IOD — yet the headline 2.1 chart's IOD lane shows 2025 as grey Neutral. That is a fabricated phase on the most recent season (blank≠zero violation applied to phases) and it also weakens the off-diagonal narrative the page is currently telling (El Niño + negative IOD). The dispatch convention is explicit: coalesce `dmi_hadisst ?? dmi_ersst`.
  FIX: Compute the OND IOD phase from the coalesced series: pre-derive `dmi = dmi_hadisst ?? dmi_ersst` per row (as recentDrivers:971 and currentState:1944 already do) and pass that to phaseByYearFor; or render years with no IOD value as a distinct 'no data' lane colour instead of Neutral grey.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2209-2213 (outlookBase), :2246-2259 (countyOutlookOND), :2810 (corrByCounty) — is_county is selected but never used — Ilemi Triangle (disputed, non-county) gets an outlook lean and a correlation on the shipped maps
  PROBLEM: enso_outlook_base contains one non-county unit (verified: `is_county=False, county='Ilemi Triangle', gaul1_code=1351`, 90 rows), and the topojson has a matching KEN feature (gaul1_code 1351). The query selects `is_county` but `countyOutlookOND`, `analogueYearsOND`, `corrByCounty` and `countyContingency` never filter on it, so outlookMapOND (:1047) and corrMap (:1447) colour the disputed territory exactly like a county — showing a tercile 'lean' and an r/p for a unit that is not one of Kenya's 47 counties, in a government-facing product. The dead `is_county` column is strong evidence the filter was intended and dropped.
  FIX: Add `WHERE is_county` to the outlookBase query (or `.filter((r) => r.is_county)` before every rollup), and let the maps' existing no-data treatments (hatch / #f0f0ec) render Ilemi. This also removes one of the four click-crash polygons in finding 3.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2474-2487 (xbtFlows/xbtImports), :1266-1276 (importsChart) — Cross-border trade sums quantities across different units and labels them with a nondeterministic unit
  PROBLEM: Verified in xbt_trade.parquet: at least 6 products mix qty_units (Kerosene: L + kg in BOTH directions; Watermelon: ea exports + kg imports; also refined oils, dried fish, cooking banana). `xbtFlows` does `SUM(qty) ..., MAX(qty_unit) AS unit GROUP BY partner, partner_iso3, direction` — adding litres to kilograms and labelling the total 'kg'. `xbtImports` sums with no unit constraint at all. `xbtUnit = xbtFlows.length ? xbtFlows[0].unit : ""` takes the first row of an unordered GROUP BY result, so the y-axis label and the ÷1e6 'kt' conversion (`xbtUnitKt = xbtUnit === "kg"`) can flip between loads for the same product. Maize (the default) is all-kg so the headline view is safe, but any user selecting Kerosene/Watermelon/oils gets a numerically meaningless chart and trade map.
  FIX: Add `AND qty_unit = 'kg'` (or filter to each product's modal unit) in xbtFlows and xbtImports — e.g. compute the dominant unit per product first and constrain both queries to it — and derive `xbtUnit` from that deterministic value, not `xbtFlows[0]`.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2806-2809 (corrByCounty driverOf) — Western-V correlation for OND uses the MAM-season index — map label claims a concurrent-season relationship
  PROBLEM: `driverOf` falls back to `wnpByYear.get(r.year)` built from `seasMean([3, 4, 5], "wnp_std_mam")` regardless of `corrSeasonCode`. With 'Short rains (OND)' + 'Western-V (WNP)' selected, the A2.1 map is labelled "correlation r (Western-V (WNP) vs OND rainfall anomaly)" but computed against the same year's MAM-mean WNP. driver_indices carries monthly WNP in all 12 months plus a dedicated `wep_std_ond` column (verified non-null for months 10-12), both unused here.
  FIX: Use season-matched W-V: `seasMean([10, 11, 12], "wep_std_ond")` (or wnp months 10-12) when corrSeasonCode === 'OND', keeping the MAM mean for MAM; or disable the W-V option for OND with an explanatory note. Same season-mismatch applies to the A1 driver chart when Western-V + OND is selected (driverCol :1971 is hard-wired to wnp_std_mam).

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2838-2842 (dotPlotRows) — Rain-vs-production dot plot picks the season window from the county's whole calendar, not the selected crop's
  PROBLEM: `const calRows = calCty.filter((r) => r.stage === "Planting")` uses ALL crops' calendar rows, so the window is 'the county plants only short rains' / 'only long rains' / else annual — independent of `dotCrop`. In any county whose calendar lists one short-rains and one long-rains crop (the common case), every crop's dot plot silently uses 'annual' rainfall percentiles, diluting the very growing-season association the figure exists to show. The code comment ("season window: crop calendar where it names this season") suggests per-crop intent.
  FIX: Filter calRows to the selected crop where the ASAP calendar has it (`calCty.filter((r) => r.stage === "Planting" && cropMatches(r.crop, dotCrop))` with a small name-normalising matcher), falling back to the county-level rule only when the crop is absent; surface the chosen window (already in `period`) in the caption, not just the download.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2026 (compareDefault) via :2013-2024 (kenyaNeighbors) — Compare-counties default silently loses neighbours for the 3 hyphen/case-mismatched counties
  PROBLEM: `kenyaNeighbors` is keyed and valued by raw topo `admin1_name`. For county = 'Elgeyo Marakwet', 'Tharaka Nithi' or "Murang'a", `kenyaNeighbors.get(county)` misses (topo spells them 'Elgeyo-Marakwet', 'Tharaka-Nithi', "Murang'A") so the Explore section defaults to the county alone; and for every other county those 3 are dropped from neighbour lists by `countyNames.includes(n)`. Ilemi Triangle can also appear as a Turkana 'neighbour' name and is correctly filtered, but the 3 real counties are lost.
  FIX: Normalise both sides once: store neighbours as canonical names via the same `canonByNorm` map recommended in finding 3 (`canonByNorm.get(countyNorm(p.admin1_name))`), and look up with the canonical county name.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:948-958 (stalenessBanner) + :1098 (outlookCard) — Page currently ships with the red stale-data banner up while the outlook card calls the same value 'current'
  PROBLEM: driver_indices ends 2026-06-01 (verified); today (2026-08-11) `ageDays ≈ 71 > 62` so the banner ("more than two months ago ... before acting on this section") renders on load, yet outlookCard says "current Niño 3.4 0.88 °C" and the glance chip presents the phase as the live state. For 1st-of-month timestamps the 62-day threshold flags data that is only one missed monthly update behind. Not a code bug, but the shipped page contradicts itself on day one.
  FIX: Either refresh driver_indices to July 2026 before shipping, or measure staleness from the END of the labelled month (`asOf + 1 month`) so a normal monthly cadence doesn't trip the banner, and soften 'current' to 'latest (Jun 2026)' in the card when the banner condition holds.


====================================================================================================
REVIEWER: OJS/Observable-Plot/reactivity reviewer (adversarial round 2)
VERDICT: The reactivity architecture is mostly sound (loader/presentation split verified correct, no cycles, all 170 nbText keys resolve, node-check passes), but three client-side-only breaks would ship: the redesigned §2.1 flagship chart gives its band scales a two-element extent domain so it renders ~2 bars and ~2 phase-strip cells; the county click-picker writes unnormalized topojson names into Inputs.select, so clicking Elgeyo-Marakwet, Murang'a, Tharaka-Nithi or the Ilemi Triangle sets county=undefined and error-cascades the whole page; and the annex correlation map's fill is colour-inverted against its own legend, reversing every correlation sign for the reader. A second tier of silent mark failures (rwChart band/rect mixing, invisible calendar season shading, NDVI area z-split, Ilemi served as a county outlook, escaped HTML on the glance tile, ONI-vs-RONI analogue distance) all survive a passing render because none execute until a real browser runs the OJS.

[CRITICAL] notebook_v2.qmd:2093 (also 2118, 2121; yrDomain at 2085) — rainPanel band scales get a 2-element extent domain — §2.1 collapses to two bars / two strip cells
  PROBLEM: `const yrDomain = d3.extent(data, (d) => d.year)` yields [1981, 2025]. The main chart uses `Plot.barY(data, {x: "year", ...})` with `x: {domain: yrDomain, ...}` and the phase strip uses `Plot.cell(laneRows, {x: "year", ...})` with `x: {domain: yrDomain, axis: null}`. barY and cell both force a BAND x scale, and a band domain is exactly the array given — two ordinal values, 1981 and 2025. Every intermediate year maps to undefined position, so the season chart renders ~2 bars and each ENSO/IOD/WNP lane renders ~2 cells; the evYears `Plot.text` dots and the `ticks: [...years%5]` mostly vanish too. v1 had no explicit domain (band inferred from all data years) — this is a v2 regression that guts the redesigned flagship §2.1 figure. The render can't catch it; it only manifests client-side.
  FIX: Use the full year list for both plots: `const yrs = d3.range(yrDomain[0], yrDomain[1] + 1)` and set `x: {domain: yrs, ...}` in BOTH the main Plot.plot and the strip Plot.plot (identical domains keep bar/cell columns aligned, and missing years show as honest gaps).

[CRITICAL] notebook_v2.qmd:158-162 (also 156-157, 1048-1049, 2019-2021) — Map click writes topojson spellings into Inputs.select — 3 counties + Ilemi Triangle break the county control
  PROBLEM: Verified: the GAUL24 topo has 48 KEN features with `admin1_name` spellings 'Elgeyo-Marakwet', "Murang'A", 'Tharaka-Nithi' and 'Ilemi Triangle'; `countyNames` (county_key) has 'Elgeyo Marakwet', "Murang'a", 'Tharaka Nithi' and no Ilemi. The click handler does `sel.value = f.properties.admin1_name; sel.dispatchEvent(new Event("input", ...))` with no normalization. Inputs.select's value setter resolves an unknown value to selectedIndex −1 → `county` becomes undefined → `countyNorm(undefined)` throws in every county-filtered query (chirps, ndvi, ipc, prices, vop, napr, gesi, …) and the whole page subgraph errors until the user re-picks from the dropdown. Clicking those 3 counties or the Ilemi sliver (cursor:pointer invites it) nukes the page. Secondary: the strict `admin1_name === county` stroke-highlight (picker map AND outlookMapOND) never outlines those 3 counties, and `kenyaNeighbors` is keyed by topo names so `compareDefault` silently loses neighbours for them.
  FIX: Resolve features to canonical names via gaul1_code: build `const gaulToCounty = new Map(counties.map(d => [+d.gaul1_code, d.county]))`; in the click handler `const name = gaulToCounty.get(+f.properties.gaul1_code); if (name == null) return;` before setting value; use the same lookup (or countyNorm comparison) for the selected-county stroke on both maps; key kenyaNeighbors by gaul1_code.

[CRITICAL] notebook_v2.qmd:1440 vs 1458-1459 — corrMap fill and its legend are colour-inverted relative to each other
  PROBLEM: Map fill: `d3.scaleDiverging([-0.8, 0, 0.8], (t) => d3.interpolateBrBG(1 - t))` paints r = +0.8 as interpolateBrBG(0) = dark BROWN (drier colour). Legend: `Plot.legend({color: {type: "diverging", domain: [-0.8, 0.8], interpolate: (t) => d3.interpolateBrBG(t)}})` shows +0.8 as TEAL. The code's own comment says the intent is '+ = wetter with +index = teal'. As shipped, a policymaker reading the annex A2 evidence map against its legend gets every correlation sign backwards — the exact figure meant to convert 'assertion to demonstration' (panel priority #9) misinforms.
  FIX: Drop the reversal in the map scale: `d3.scaleDiverging([-0.8, 0, 0.8], d3.interpolateBrBG)` so +r = teal, matching the legend and the stated intent.

[MAJOR] notebook_v2.qmd:900-903 with 2340-2346 — rwChart mixes band-scale barY with fractional-year rect bands — phantom axis bands, broken event shading
  PROBLEM: `eventBandMarksYear` emits `Plot.rect(rows, {x1: year-0.45, x2: year+0.45, ...})` (e.g. 1981.55, 1983.45) into the same plot as `Plot.barY(rwByYear, {x: "year"})`. barY forces the x scale to band; the ordinal domain is then inferred as the UNION of bar years AND the 16 fractional event-edge values, so the axis acquires phantom bands (tickFormat "d" renders 1981.55 as '1982'), bar spacing is distorted, and the event 'bands' collapse to band-width slivers at wrong positions. The shared-annotation helper was ratified (strategy: bands 'applied identically to … all §4 impact panels') and here it corrupts the chart it decorates.
  FIX: In rwChart give the x scale an explicit full-year band domain (d3.range over the data extent) and rewrite eventBandMarksYear to snap x1/x2 to integer years within that domain — or convert rwChart to a utc-x time axis (like the other §3 panels) and reuse eventBandMarks instead.

[MAJOR] notebook_v2.qmd:2209-2213 with 2246-2259, 1114-1115 — is_county fetched but never used — Ilemi Triangle served as a county outlook
  PROBLEM: Verified in the parquet: enso_outlook_base contains 90 rows with is_county = false, county = 'Ilemi Triangle', gaul1_code = 1351 — and that gaul code EXISTS in the topojson. The outlookBase query selects `is_county` but no cell filters on it, so `countyOutlookOND` builds a full analogue outlook for the disputed territory: outlookMapOND paints it with a real tercile fill (not hatch), its tooltip reads like any county, and the fig 4.2 download CSV (`[...countyOutlookOND.values()]`) includes an 'Ilemi Triangle' row presented as county-level guidance. corrByCounty likewise colours its polygon. The round-1 panel explicitly flagged Ilemi leakage as the future-roll-up hazard.
  FIX: Filter at the source: add `WHERE is_county` to the outlookBase query (and keep the topo feature hatched/grey by construction since gaul 1351 then has no entry); alternatively guard `if (!r.is_county) continue` in countyOutlookOND and corrByCounty.

[MAJOR] notebook_v2.qmd:218 — glanceCard outlook tile prints literal '<a href="#outlook">' text — htl escapes HTML inside string interpolation
  PROBLEM: The tile sub-line interpolates a plain JS template string containing markup: `` `${o.counts[o.modal]} of ${o.n} similar past years — <a href="#outlook">see the outlook</a>` ``. htl's html`` escapes interpolated strings as text nodes, so the shipped first-screen glance card displays the raw tag characters instead of a link — on the highest-visibility component of the page (D-decision: 60-second glance panel first screen).
  FIX: Wrap the branch in html`` like the neighbouring outlookCard code does: `o == null ? "no baseline for this county" : html`${o.counts[o.modal]} of ${o.n} similar past years — <a href="#outlook">see the outlook</a>``.

[MAJOR] notebook_v2.qmd:600-604 — ndviChart areaY coloured by sign splits into two z-series that bridge across opposite-sign runs
  PROBLEM: `Plot.areaY(ndviCty, {x, y: "ndvi_pct_normal", y1: 100, fill: (d) => d.ndvi_pct_normal < 100 ? "Below normal" : "Above normal"})` — a varying fill becomes the z channel, so Plot draws ONE 'Below normal' area connecting only the below-100 dekads and one 'Above' area connecting only the above-100 dekads. Each polygon spans straight across the months where the series was on the other side, painting deficit/surplus area where none existed (e.g. a brown band drawn across the 2019 IOD-wet OND if flanked by dry dekads). The 3.2 vegetation figure visually misstates when the county was in deficit — exactly what event-anchored §3 exists to show.
  FIX: Use two clamped areas with no z-split: `Plot.areaY(ndviCty, {x, y1: 100, y2: d => Math.min(100, d.ndvi_pct_normal), fill: PALETTE.impact.bad, fillOpacity: .65})` and the mirror with Math.max + impact.good; keep the line and 100-rule as is (legend via explicit color domain unchanged).

[MAJOR] notebook_v2.qmd:2238-2242 — Analogue nearest-neighbour compares raw Niño 3.4 (ONI) against RONI — mismatched index scales bias the analogue set
  PROBLEM: `const ref = currentState.oni ?? 0;` (3-month mean of nino34_anom_noaa, +0.88 now) is used as the distance target against `roni_conc` values (`Math.abs(a.roni - ref)`). RONI subtracts tropical-mean warming and runs materially below raw ONI in recent decades, so ranking by |RONI − ONI| systematically selects analogue years whose RELATIVE events were stronger than the current one — a quiet violation of the ratified 'nearest-neighbour on index similarity' decision (strategy line 112) and a numeric-selection bias feeding the page's headline outlook counts (map, card, glance tile).
  FIX: Compute the current-state reference on the same index as the baseline: either serve a current roni value (extend driver_indices or state_probs with the CPC RONI) and use that as ref, or match on the concurrent nino34-based index consistently on both sides. If no current RONI is servable, fall back to phase-matched ALL years (drop the distance sort) rather than a cross-index distance.

[MAJOR] notebook_v2.qmd:488-489 — calendarStrip season-shading rects use numeric y1/y2 on a band y scale — shading never renders
  PROBLEM: `Plot.rect([{x1: 2.55, x2: 5.45}], {..., y1: () => -0.5, y2: () => labs.length - 0.5, fill: "#1b9e77", fillOpacity: 0.06})` (and the OND twin) sit in a plot whose y scale is BAND (`Plot.barX(..., y: "lab")` with explicit `y: {domain: labs}`). Band lookup of −0.5 / labs.length−0.5 returns undefined, so both MAM/OND highlight rectangles are silently invisible — the visual link between the crop calendar and the two rain seasons (a ratified §2.2 element) is missing from the shipped page.
  FIX: Drop the y1/y2 channels entirely — `Plot.rect([{x1: 2.55, x2: 5.45}], {x1: "x1", x2: "x2", fill: ..., fillOpacity: 0.06})` spans the full frame height on its own; keep insets if a small margin is wanted.

[MAJOR] notebook_v2.qmd:137-175 — Click-picker has no keyboard/AT path — the ratified aria/keyboard requirement from round 1 was dropped
  PROBLEM: The round-1 priority-10 list item #1 ratified restoring the click-picker 'and add the aria/keyboard support every persona forgot'. v2's countyPickerMap has `role="img"` + aria-label on the svg and `.on("click")` on paths only — no tabindex, no keydown handling, no focusable county elements; role=img actively tells AT the map is a static image while cursor:pointer promises interactivity. Keyboard and screen-reader users cannot use the primary county selector by map (the dropdown fallback exists, but the ratified decision explicitly required the map path).
  FIX: Give each county path `tabindex="0"`, `role="button"`, `aria-label` = county + value, and a keydown handler (Enter/Space → same handler as click); change the svg role to `group`. Reuse the gaulToCounty lookup from the name-mismatch fix so the label/name is canonical.

[MINOR] notebook_v2.qmd:277-281, 300-303, 1174-1181 (deps 2505, 2025-2028) — County change silently resets prodCommodity, prodPick and compareCounties selections
  PROBLEM: `viewof prodCommodity` takes `value: prodDefaultCommodity` (← vopSummary ← county), `viewof prodPick` takes `value: prodDefault` (← prodItems ← county), and `viewof compareCounties` takes `value: compareDefault` (← county). Every county change (including map clicks) re-creates all three inputs and discards the user's choices; the radio also flickers while the vop query resolves. No circularity or reset-storm (the graph is a DAG and settles in one pass), but a user comparing a fixed county set or a pinned commodity loses state on each county switch.
  FIX: If intentional, document it; otherwise decouple: initialize the inputs once (static default) and apply the county-derived default only when the current selection is empty/invalid, e.g. wrap in a cell that preserves `viewof prodCommodity.value` across rebuilds when still valid.

[MINOR] notebook_v2.qmd:950-953 — Staleness banner threshold (62 days) fires under normal monthly index latency — it is showing right now
  PROBLEM: driver_indices ends 2026-06-01 (verified); today 2026-08-11 → ageDays ≈ 71 > 62, so the ⚠ 'more than two months ago' banner is displayed on the shipped page even though a June-final value in mid-August is ordinary NOAA publication latency. A permanently-on warning trains readers to ignore it — undermining the freshness machinery it implements (critique #10).
  FIX: Measure staleness against expected cadence: warn when `ageDays > 92` (two missed monthly updates), or compare against the month AFTER next expected release; keep a quiet 'data through Jun 2026' stamp for the 62–92-day window.

[MINOR] notebook_v2.qmd:434, 527, 1141 — Story layer displays 'n=' values — tension with the ratified 'no r/n/p in the story layer' rule
  PROBLEM: phaseChips renders '(n=${sub.length})' in §2.1, and contingencyChart/mamHistoryChart print 'n=${d.total}' labels in §2.3/§4.3 body figures. The ratified plain-language rule reserves r/n/p for the annex ('house rule of no r/n/p in the story layer'). Counts-as-denominators ('12 of 44 seasons') are the sanctioned phrasing; a bare 'n=' token is exactly the notation the rule bans.
  FIX: Rephrase the labels: chips → '(12 years)' or 'across 12 El Niño years'; bar labels → 'of ${d.total} seasons' appended to the tooltip only, or 'from ${total} seasons' as the axis-side annotation.

[MINOR] notebook_v2.qmd:2806-2809 — corrMap offers Western-V × OND: correlates the MAM-season WNP mean against OND rainfall
  PROBLEM: `driverOf` for the Western-V pick always uses `wnpByYear` built from months [3,4,5], regardless of `corrSeasonPick`. Choosing 'Western-V (WNP)' with 'Short rains (OND)' therefore maps MAM WNP vs OND anomalies — a season-mismatched pairing that contradicts the ratified framing (Western-V is the MAM control) and invites readers to find spurious OND Western-V 'signal'.
  FIX: Disable the WNP option when OND is selected (or vice-versa): e.g. gate the radio options on corrSeasonPick, or short-circuit corrByCounty to an empty map with an explanatory note when the combination is WNP+OND.

[MINOR] notebook_v2.qmd:867-870 — prodEventPanel legend hardcodes English editorial claims ('2020–23: triple-dip La Niña drought') in the qmd
  PROBLEM: The legend line embeds typed event claims and year ranges directly in code — `<b>2020–23:</b> triple-dip La Niña drought · <b>2023–24:</b> El Niño recovery` — bypassing both the nbText i18n layer (everything else routes through _lang) and the curated events.json that exists precisely to own event wording. The dates are consistent with events.json today, but a future events.json edit will silently desynchronize this hardcoded text.
  FIX: Derive the annotation from namedEvents (label.en/blurb via _lang-style {en,fr} objects) or move the sentence into nbText.sections.b3 and interpolate it.


====================================================================================================
REVIEWER: Story/UX adversary (round 2 — policymaker + story-editor lenses)
VERDICT: v2 is a real structural improvement but it ships with one outright constraint violation (raw "n=" statistics printed on three story-layer figures), several ratified Wave-1/2 deliverables silently missing (phase-composite maps, per-beat insight cards, tiered glossary), and the highest-priority integrity fix only half-delivered — the off-diagonal warning never reaches the LEANING-WETTER card or the glance tile that people will actually screenshot. The worst single defect for a lay reader is §2.1's temperature view, which paints hotter-than-normal years teal and labels them "wetter / better than normal" under a caption that still says teal = wetter rainfall.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:434, 527, 1141 — Raw n= statistics printed in the story layer (constraint: no r/n/p outside annex)
  PROBLEM: §2.1 phase chips render `(n=${sub.length})` (line 434), story Fig 2.3 draws `text: (d) => \`n=${d.total}\`` on the chart (line 527), and §4 Fig 4.3 repeats the same `n=${d.total}` mark (line 1141). The constraint says r/n/p live in the annex only; a Marsabit CECM reads 'n=44' as unglossed jargon on three story figures.
  FIX: Replace with words the captions already use: '(12 seasons)' on the chips and 'of 44 seasons' / '44 seasons' as the end-of-bar label; keep bare n= in annex figures only.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2080-2100 + nbText_v2.json:64 (b2.rainCaption) — Temperature view colours hotter years teal and labels them 'wetter / better than normal'
  PROBLEM: rainPanel computes `wet: varIsSpei ? r.value >= 0 : r.value >= clim` and fills `d.wet ? "wetter / better than normal"` — so with Show=Temperature (°C), every hotter-than-normal year is teal/'better'. The fixed caption still reads 'teal = wetter than the 1991–2020 normal, brown = drier' for all three variables. This is exactly the D-F colour-language violation the palette refactor was meant to kill.
  FIX: For TAVG invert or neutralise the semantics (hotter = orangeRed 'warmer than normal', cooler = blue) with a variable-aware legend, and make rainCaption (or a caption variant) switch with climateVar.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:381-548 (section #drivers) vs STRATEGY_v2_redesign.md:98-99, 196 — Phase-composite rainfall-anomaly maps (D-D, promoted into beat 2, Wave 2, zero new data) are missing entirely
  PROBLEM: Strategy: 'Phase-composite rainfall-anomaly maps (El Niño | Neutral | La Niña × OND; Western-V high/low × MAM) — promoted into story beat 2'. grep 'composite' finds nothing in notebook_v2.qmd. §2 therefore never SHOWS the ocean steering spatially — the lay reader gets bars, a calendar and a contingency chart but no map of what El Niño does to Kenya. The §2 placeholder (nbText b2.mapsComing) covers only the parallel-session subcounty CHIRPS explorer, not this ready-now triptych built from enso_outlook_base.parquet already in every browser.
  FIX: Add the composite anomaly-map triptych (per-phase mean anomaly_pct by county, n-years printed under each panel, BrBG, ensembling-per-event-first) into §2 between Fig 2.1 and 2.3.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1010-1108 + 216-219 — Off-diagonal guard never reaches the verdict card or the glance outlook tile (partial D-E — the strategy's 'highest-priority integrity fix')
  PROBLEM: The live state fires the guard (ONI +0.88, DMI −0.44 → signalsMixed true), but the warnbox renders before the map (line 1010), while the big 'LEANING WETTER' card (lines 1096-1106) carries no mention of the disagreement, unwidened wording, and no IOD reading — and cards are what get screenshotted to WhatsApp. Likewise glance tile 2 shows 'leaning wetter' with the ⚠ mixed flag on a *different* tile (line 213). D-E: 'the card must say so, widen stated uncertainty, and show the IOD-conditioned analogue set beside the ENSO one.'
  FIX: When signalsMixed, inject a caution line + the IOD-analogue counts INSIDE outlookCard (and soften 'LEANING' to 'MIXED SIGNALS — ENSO leans wetter / IOD leans …'), and put the ⚠ on the glance outlook tile itself.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:949-958 vs 189-231 — Staleness banner fires only in §4 while §1's glance card asserts the same stale ocean state unflagged
  PROBLEM: driver_indices ends 2026-06; today 2026-08-11 → ageDays ≈ 71 > 62, so the red 'more than two months ago' banner shows in §4. But the first-screen glance card still states 'El Niño conditions are present … (Jun 2026)' and 'leaning wetter' with no staleness cue — the reader who never scrolls to §4 (the whole premise of the 60-second design) acts on a state the page itself elsewhere calls too old to act on.
  FIX: Render the staleness warning (or a compact 'data ends Jun 2026 — check NOAA/KMD' badge) on the glance 'Ocean now' tile whenever the §4 condition is true; one shared `isStale` cell.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:101-107, 1913-1921, 1973-1977, 2301-2322, 2744-2754 — Season sticky control scope is illegible: one story figure obeys it, two annex statistics silently recompute, 'Full year' produces mislabelled physics
  PROBLEM: Trace: `season` → seasonMonths/seasonPeriods drives Fig 2.1 (yes), Fig 5.1 (labelled), annex A1.1 driverSeasonal, and — silently — A3's headline stat (cropJoin/cropStat use driverSeasonal, so the printed 'maize vs ENSO — r=…, n=…' changes with the top-of-page Season; with 'Full year' it correlates maize against an annual-mean Niño 3.4, and nothing in A3 says which window is in force). Meanwhile §2.3 always shows BOTH seasons regardless of the control, and §3/§4 ignore it entirely. Worse, Season='Full year' renders annual totals with OND-derived ENSO/IOD phase lanes underneath (rainPanel else-branch, lines 2107-2113) while the caption claims the strips show 'the ocean state that season'.
  FIX: State the season window in A3's stat line and A1.1's caption (or decouple them from the sticky control with a local season pick); either drop 'Full year' from the sticky options or suppress/label the phase lanes for annual; add one line to §2.3/§3 intros saying they always show both seasons / fixed OND.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:442-455, 2133-2160 — §2.1 Monthly-climatology view ignores the Show/As controls and keeps the yearly caption, data table and filename
  PROBLEM: rainCharts switches to rainMonthlyChart, which queries only ptot — so Show='Drought index (SPEI-3)' or 'Temperature' + View='Monthly climatology' silently plots rainfall mm. The plotFooter still shows rainCaption ('Each bar is one year's season… strips underneath show the ocean state'), still exports rainDownloadRows (yearly anomalies) and still names the file `…_by_year` — three pieces of furniture describing a chart that is not on screen.
  FIX: Disable/hide climateVar+rainView when Monthly is selected (or honour them), and switch caption, data payload and filename with rainTimeRes.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:187-231 (glance) and 1072-1119 (Fig 4.2) — 60-second first screen not delivered: glance card is buried under Figure 1.1's furniture; Fig 4.2's caption is separated from its map by the outlook card
  PROBLEM: Page order is hero → TOC → intro → sticky bar → §1 intro → 1.1 intro → metric control → 640×560 map + legend → caption + download row + data-table/About expanders → THEN the unlabeled glance card. The card the strategy wanted on the first screen sits ~2 screens down and visually reads as part of Figure 1.1. In §4.2 the order map → note → verdict card → 'Figure 4.2 —' caption detaches the caption from the map it describes.
  FIX: Move the glance card above Fig 1.1 (directly under the sticky bar) with its own small heading; move the outlook card below the Fig 4.2 plotFooter or give the card its own labelled block.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd (no insight cells) vs notebook.qmd:304,463,1005,1075; nbText_v2.json:58,216 — Regression: v1's four dynamic insight cells dropped; D-A's 'one county-aware insight card per beat' never built
  PROBLEM: v1 rendered four insight blocks (keyfacts.insight, b2.insight1/2, b3.insight); v2 renders zero — grep 'insight' finds nothing in the v2 body. nbText_v2 ships dead keys ('b1.insight': 'Quick insight', 'general.quickInsight'). The ratified design (D-A: 'one dynamic county-aware insight card per beat'; Wave 2: 'dynamic county insight cards') is a no-show, and with it the county-specific takeaway sentences a CECM would quote.
  FIX: Add one computed, county-aware insight card per beat (e.g. B2: 'In Marsabit, 8 of 11 El Niño short rains were wetter than normal' from countyContingency — all deterministic), or delete the dead keys and log the deferral.

[MAJOR] nbText_v2.json:146 (explore.tradeIntro) — Broken in-page link: #annex-trade target does not exist
  PROBLEM: tradeIntro links 'the flow map is in the [technical annex](#annex-trade)', but the qmd defines no {#annex-trade} anchor (only #annex, #annex-drivers, #annex-evidence, #annex-national, #annex-gesi, #methods). Clicking it does nothing.
  FIX: Point it at #annex-gesi's section (A5 contains the trade map) or add {#annex-trade} — better, give the A5 trade-map subsection its own anchor.

[MAJOR] nbText_v2.json:173 + notebooks/KE-enso-explorer/notebook_v2.qmd:1583 — Annex numbering visibly skips A4 (A1, A2, A3, A5, A6) and 'A5'/'A1' etc. are typed in nbText, violating the derived-numbering rule
  PROBLEM: a5Title is hard-typed '"A5 · County detail: GESI, conflict, trade flows"' and the page renders A3 → A5 with no A4 and no placeholder (A4 was the Wave-3 subcounty/station annex). Readers see a hole; and the strategy's cross-cutting rule says 'section/figure numbers derived, never typed' — annex letters are typed inside nbText prose strings, exactly what the rule forbids.
  FIX: Either renumber A5→A4/A6→A5 for v2, or add a one-line 'A4 · Subcounty maps & station validation — in preparation' placeholder; move annex letters out of nbText into the title_ vars like the H2 numbers.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:550-940 (§3) vs STRATEGY_v2_redesign.md:197 — §3 event stack missing its promised rainfall + SPEI-12 drought strip, and two figures (3.3, 3.4) have no story text at all
  PROBLEM: The ratified B3 row lists '(rainfall, SPEI-12 strip, NDVI, IPC…, price anomaly, production event panel)'. v2's §3 opens on NDVI — the rains themselves never appear in the event story, and SPEI-12 is absent from the whole notebook even though the schema ground truth shows SPEI-12 is already served in chirps_county (this is not the Wave-3 slim re-export). Additionally `## title_ndviPhase` (line 620) and `## title_ipc` (line 652) drop straight into charts with no intro sentence — chart-soup residue in the beat that was supposed to read as a story.
  FIX: Add a compact OND/MAM anomaly-bar or SPEI-12 strip as Fig 3.2 using existing dbChirps data, and give 3.3/3.4 one-line intros (nbText keys).

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:579-584 (fig 3.1) and About coverage across all plotFooters — Figure furniture inconsistent: About on only 8 of 24 figures; Fig 3.1 has no source meta at all; 'data as of' key dead
  PROBLEM: about: appears on figs 1.1–1.4, 2.1, 3.5, 4.1, 4.2 only; 2.2, 2.3, all of §3 except 3.5, 4.3, §5 and every annex figure lack it. Fig 3.1 (event timeline) has neither about nor meta — the downloaded CSV of curated editorial events carries no source/provenance line. nbText ships an unused 'general.dataAsOf' key while no figure has a data-as-of stamp. The strategy's own acceptance line is 'every figure has visible caption…, About, …, data-as-of stamp'.
  FIX: Add meta {Source: 'Curated event list; windows from NOAA driver-index thresholds'} to Fig 3.1 minimally; either add About to the remaining figures or state the deliberate tiering; wire dataAsOf or remove the key.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:760, 968 + nbText_v2.json (no glossary) — Plain-language pass incomplete: '(σ)' axis label in a story figure, 'WNP (standardised)' unexpanded, ENSO never defined, tiered glossary (Wave 1) missing
  PROBLEM: Prices seasonal-anomaly axis reads 'departure from the market's usual level for that month (σ)' (line 760) — sigma in the story layer; §4.1 panel label 'Western-V — WNP (standardised)' never expands WNP; 'ENSO' is used from §2 onward but never glossed as 'the El Niño/La Niña cycle'; and the Wave-1 ratified 'tiered glossary' does not exist anywhere (grep: no hits).
  FIX: '(in standard deviations — σ)' → 'compared with its usual level (standard deviations)'; drop 'WNP' or expand once; add one-line ENSO gloss in b2.intro; ship the small glossary expander promised in Wave 1.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:570, 242, 204, 867-870 — Assorted story-surface nits: no-op label truncation on the timeline, silent top-14 VoP cut, hardcoded English editorial strings
  PROBLEM: (a) eventTimeline's `label.en.replace(/ (El Niño|…).*$/i, (m) => m)` replaces the match with itself — a no-op, so full-length labels ('2020–23 triple-dip La Niña drought') will collide with neighbours (2019 vs 2020 events overlap at ~2 yrs apart). (b) vopChart slices to 14 commodities (line 242) but the caption claims 'by commodity' with no truncation note (export has all rows). (c) Substantial story copy is hardcoded in code cells outside nbText/_lang — 'leaning wetter/drier' (204), the whole outlook card, the prodEventPanel legend including the typed editorial '2020–23: triple-dip La Niña drought · 2023–24: El Niño recovery' (867-870) — breaking the AAA text-in-nbText convention the fr-fallback plumbing depends on.
  FIX: Fix or delete the replace; add 'top 14 shown; full list in the data table' to the VoP caption; migrate the hardcoded strings to nbText keys (label/blurb text can come from events.json).

[MINOR] nbText_v2.json:73 + notebooks/KE-enso-explorer/notebook_v2.qmd:2856-2867 — Small caption/label dishonesties: Fig 2.3 caption says 'Counts' but the chart plots shares; 'Both seasons' compare chart sums OND+MAM without saying so
  PROBLEM: contingencyCaption opens 'Counts of the county's past seasons…' while the x-axis is 'share of past seasons' in % (counts only in tooltip). cmpRain does SUM(value_mean) over OND+MAM when 'Both seasons' is selected, yet Fig 5.1's caption still says 'Seasonal rainfall … for each selected county' — the y value is a two-season total, unlabelled.
  FIX: 'Shares (hover for counts)…' in the 2.3 caption; append 'Both seasons = OND + MAM combined total' to the 5.1 caption or y-label when seasonCode === "OND+MAM".


====================================================================================================
REVIEWER: science-integrity adversary (teleconnection scientist, hostile pass)
VERDICT: v2's outlook machinery is structurally better than v1 but ships three integrity failures: the annex correlation map's fill is color-inverted relative to its own legend (positive r paints brown while the legend says teal), the currently-firing off-diagonal guard asserts a historical reliability claim ("in past seasons like this the ENSO-only reading was least reliable") that the notebook's own record cannot support — zero El Niño + negative-IOD OND seasons exist in 1981–2025 — and the analogue nearest-neighbour metric compares a relative index (roni_conc) against plain ONI (+0.88), a systematic ~0.2–0.6 °C unit clash the prose then misdescribes. Around these, the "1/3 expected by chance" reference line is the wrong null for terciles defined on 1991–2020 (MAM marginal Wet runs 40–44%), the "chance would give ~3 of 8" sentence understates the modal-count null (E[max]=4.07, P(≥5)=26%), and the IOD phase strip shows the genuinely negative-IOD OND-2025 (ERSST DMI −0.62) as Neutral because it reads only dmi_hadisst, which ends 2025-04.

[CRITICAL] notebooks/KE-enso-explorer/notebook_v2.qmd:1440 (corrMap) vs :1458-1459 (legend) — Correlation map fill is color-inverted relative to its legend
  PROBLEM: Map fill uses `d3.scaleDiverging([-0.8, 0, 0.8], (t) => d3.interpolateBrBG(1 - t))` so r=+0.8 maps to BrBG(0)=brown and r=-0.8 to teal — contradicting the code's own comment `// + = wetter with +index = teal` — while the legend uses `interpolate: (t) => d3.interpolateBrBG(t)` (+0.8=teal). Kenya's OND ENSO/IOD correlations are predominantly positive, so nearly the whole map renders in the polarity opposite to the legend: readers will conclude El Niño dries Kenya's short rains.
  FIX: Make map and legend share one mapping: change the map scale to `d3.scaleDiverging([-0.8, 0, 0.8], d3.interpolateBrBG)` (positive = teal) and keep the legend as is; verify one known county (e.g. a coastal county, r>0 for ENSO-OND) renders teal.

[CRITICAL] data/KE-enso-explorer/nbText_v2.json:127 (offDiagonalWarn) + notebook_v2.qmd:1010-1022 — Guard's 'ENSO-only reading was least reliable' is an unsupported LLM-typed claim
  PROBLEM: The guard text asserts "in past seasons like this the ENSO-only reading was least reliable", but nothing computes that, and it CANNOT be computed from the served record: zero OND seasons in 1981–2025 have roni_conc>0.5 with dmi_conc<−0.4 (verified against enso_outlook_base). "Past seasons like this" do not exist in the data. This is exactly the class of typed empirical claim the project bans, and it fires NOW (ONI +0.88, DMI −0.44).
  FIX: Rewrite to what is defensible without data support: e.g. "ENSO and the IOD usually move together in Oct–Dec; the record contains no season with this combination, so the historical analogues carry extra uncertainty" — or compute and show the claim (it can't be, so say the record is silent).

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2238-2242 + :1098 + nbText_v2.json:126,190 — Analogue similarity metric mixes RONI with standard ONI (unit clash)
  PROBLEM: `const ref = currentState.oni ?? 0` is a 3-month mean of `nino34_anom_noaa` (standard Niño 3.4), then `Math.abs(a.roni - ref)` ranks RONI values against it. RONI runs systematically below ONI in the modern warm base state (verified: ONI−RONI mean +0.23 for 2010–2025, +0.57 in 2024), so ranking to ONI +0.88 targets years whose RONI≈0.88 ≙ ONI≈1.0–1.1 — biased toward stronger events, the exact bias D-E's nearest-neighbour switch was meant to remove. The prose contradicts the code both ways: outlookAbout says ranked by "relative Niño index" similarity while the card prints "current Niño 3.4 +0.88 °C" as the reference.
  FIX: Use one index on both sides: either add the current RONI value to the served state (enso_state_probabilities or driver_indices already carries roni_pred in outlook_base) and rank |roni_conc − RONI_now|, or rank on ONI vs an ONI column. Then make card + about text name that index.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:526,1140 + nbText_v2.json:73,130 — Dashed 1/3 'expected by chance' line is the wrong null for these terciles
  PROBLEM: Terciles in enso_outlook_base are terciles of 1991–2020 applied to the full 1981–2025 record (verified: 100% match for Baringo OND vs 91% for full-record terciles). Marginal shares therefore deviate from 1/3 — MAM Wet share averages 0.40 across counties (Baringo 20/45 = 0.44) — yet both captions say "the dashed line marks the one-third share expected by chance" and the charts draw `Plot.ruleX([1 / 3])`. Conditional-on-phase bars absorb the MAM wetting trend, so 'Western-V high → 50% wet' vs the 1/3 line reads as driver signal when nearly half is baseline drift.
  FIX: Draw each county's own marginal share per outcome as the reference (computable from the same rows), or state the reference honestly ('share vs the county's 1981–2025 base rate'), and add the strategy-required stationarity caveat (§5.8) to the contingency/outlook method text.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1101 + nbText_v2.json:126 — 'Chance would give about n/3' understates the null for a modal-category count
  PROBLEM: The card shows the MODAL tercile count (`o.counts[o.modal]`) but benchmarks it against `Math.round(o.n / 3)`: "chance alone would give about 3 of 8". For the max of a multinomial(8, 1/3) the null expectation is 4.07 and P(max≥5)=0.264, P(max≥6)=0.059 (computed exactly). A '5 of 8 were drier' verdict therefore looks like strong evidence against the stated ~3 baseline while being unremarkable (26% by pure chance). The D-E strategy typed the same error; the build inherits it.
  FIX: Benchmark against the modal-count null: "by chance alone the most common outcome would average about 4 of 8; 6 or more of 8 is unusual (≈6%)" — both numbers derivable in-cell from n, no typing needed.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1966 + :2064 — IOD lane shows OND-2025 (a real negative IOD) as Neutral — data gap rendered as a phase
  PROBLEM: `iodPhaseOND = phaseByYearFor([10, 11, 12], "dmi_hadisst", 0.4, ...)` reads only dmi_hadisst, which ends 2025-04 (verified). OND-2025 has no entry, and line 2064's `iodPhase: iodPhaseOND.get(r.year) ?? "Neutral"` paints the missing year as Neutral. By the notebook's own fallback source (used by currentState and recentDrivers: `dmi_hadisst ?? dmi_ersst`), OND-2025 DMI = −0.62 — a genuine negative IOD on the most recent, most policy-salient season. Blank≠zero violated in the phase strip.
  FIX: Coalesce in the phase map (compute a `dmi = dmi_hadisst ?? dmi_ersst` column before phaseByYearFor, as recentDrivers does at line 971), and render truly-missing years as a distinct 'no data' hatch, not Neutral grey.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1965 (ensoPhaseOND) vs :2215,2240 (RONI phase matching) — Two contradictory ENSO classifications of the same seasons on one page
  PROBLEM: Phase strips, contingency chart and NDVI beeswarm classify years by OND-mean nino34_anom_noaa (±0.5), while the outlook analogues classify by roni_conc (±0.5). These disagree on real years: OND-2024 is Neutral by ONI (−0.37) but clear La Niña by RONI (−0.94, matching the official call); OND-2019 is 'El Niño' by ONI (0.51 — a hair over the strict > 0.5) even though the 2019 event blurb's whole point is "the IOD, not ENSO alone" and RONI says 0.23 (Neutral). The same label 'El Niño' means different year-sets in Fig 2.3 vs Fig 4.2.
  FIX: Standardise on one ENSO year-classification (RONI, since the forecast side is RONI-based) for strips, contingency and analogues; if ONI is kept for history, disclose the two definitions and reconcile 2019/2024 explicitly.

[MAJOR] data/KE-enso-explorer/events.json:29 (drought-2005), :47 (drought-2016) — Event blurbs claim 'negative-IOD conditions' that the notebook's own threshold refutes
  PROBLEM: OND-2005 DMI = −0.34 and OND-2016 DMI = −0.35 (dmi_hadisst OND means, verified) — both Neutral under the notebook's ±0.4 IOD threshold, so the visible IOD lane paints these years grey while the blurbs say "La Niña / negative-IOD conditions" and "weak La Niña / negative-IOD conditions". This directly contradicts events.json's own header claim that "event windows align with driver-index thresholds computed in the notebook". (The 1982 'unusually wet short rains' claim checks out: 45/47 counties Wet.)
  FIX: Edit the two blurbs to what the thresholds support (e.g. "La Niña with a cool western Indian Ocean" or drop the IOD clause), or lower nothing — do not tune the threshold to rescue prose.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2806-2809 + :1425 + nbText_v2.json:164 — Western-V selectable for OND in the correlation map — cross-season pairing sold as 'that season's driver'
  PROBLEM: `driverOf` returns `wnpByYear.get(r.year)` built from `seasMean([3, 4, 5], "wnp_std_mam")` regardless of corrSeasonPick, so choosing 'Western-V (WNP)' with 'Short rains (OND)' correlates OND rainfall against the SAME YEAR'S MAM Western-V — while the caption claims "the driver index for that season" and the page's framing says Western-V is a MAM control. driver_indices even carries an unused `wep_std_ond` column for the OND west-Pacific state.
  FIX: Gate the Western-V option to MAM (disable/hide it when OND is selected), or use wep_std_ond for OND and label it as a different index; correct the caption either way.

[MAJOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2270-2290 (selectedOutlookIod) + :1018-1021 — IOD-conditioned 'reading' rests on 4 analogue years with no minimum-n, drawn from opposite-ENSO states
  PROBLEM: Only 4 OND seasons in the record have dmi_conc < −0.4 (1984, 1992, 1996, 1998 — verified), all non-El-Niño years, and `selectedOutlookIod` has no n floor (only `if (!rows.length) return null`) while countyContingency enforces `total < 5 → hide`. The guard then displays 'IOD-state analogues lean drier (3/4)' with equal typographic weight to the 8-year ENSO lean, without disclosing that every IOD analogue comes from a different ENSO state than today's.
  FIX: Apply the same min-n=5 rule (which here means: show 'too few negative-IOD seasons (4) for a reading' instead of a lean), and/or annotate that the negative-IOD years were all non-El-Niño seasons.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2082,2096-2099 (rainPanel) — Temperature bars label hotter-than-normal as 'wetter / better than normal'
  PROBLEM: `wet: r.value >= clim` with color domain ["wetter / better than normal", "drier / worse than normal"] applies to TAVG too: hotter years render in the 'wetter/better' green. For SPEI the wet='better' framing also collides with the page's own flood narrative (1997, 2019, 2023 are flood disasters), though the outlook card's wetAfterDrought text partly covers that.
  FIX: Per-variable legend: for TAVG use 'warmer / cooler than normal' with the warm side in the orange-red (bad) ramp per the repo colour convention; for SPEI drop 'better' ('wetter / drier than normal').

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:966-968 (recentDrivers) — WNP panel plots a MAM-standardised index monthly year-round with a ±0.5 threshold band
  PROBLEM: `wnp_std_mam` has values for all 12 months (verified) but is by name standardised for MAM; the panel gives it the identical visual contract as ENSO/DMI (threshold band, dashed rules) in the coming-season section, where the current Jun-2026 value of 2.1 invites a long-rains inference — yet the page (correctly, per constraint) offers MAM no outlook, and off-season values of a MAM-standardised index may not be comparable to the ±0.5 band.
  FIX: Either restrict the WNP lane to Mar–May points, or annotate the panel 'meaningful in Mar–May only — no long-rains outlook is offered', after verifying with the producer whether the column is per-month or MAM-window standardised.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:1942-1949 (currentState / signalsMixed) — Guard trigger uses a single-month ERSST DMI against a 3-month ONI mean
  PROBLEM: `currentState.oni` is a 3-month mean but `dmi` is the last single monthly value (−0.44, only 0.04 beyond the ±0.4 threshold, and from ERSST while historical phases use HadISST). One noisy monthly update flips the entire off-diagonal guard and the header's mixed-signals flag on and off.
  FIX: Use a 3-month mean of the coalesced DMI (`dmi_hadisst ?? dmi_ersst`) for currentState.dmi, mirroring the ONI smoothing.

[MINOR] notebooks/KE-enso-explorer/notebook_v2.qmd:2246-2259 + :2810 — Ilemi Triangle (is_county=false) treated as a county in outlook and correlation outputs
  PROBLEM: enso_outlook_base contains 90 rows for 'Ilemi Triangle' flagged `is_county=false` (gaul 1351), but countyOutlookOND and corrByCounty group all rows without filtering is_county, so the disputed territory is painted on both maps and exported in the outlook CSV (`data: [...countyOutlookOND.values()]`, line 1114) as if it were one of the 47 counties.
  FIX: Filter `r.is_county` in both aggregations (or at the outlookBase query), and decide explicitly whether the polygon should be hatched or shown — don't let it ride in silently.

[MINOR] data/KE-enso-explorer/nbText_v2.json:126 (outlookAbout) + notebook_v2.qmd:1461 — Hard-coded counts in prose that the code computes live elsewhere
  PROBLEM: outlookAbout types "With eight analogue seasons..." while the map legend correctly computes `based on ${analogueYearsOND.length} analogue years`; if the CPC phase flips (e.g. to Neutral) or the pool shrinks, the typed 'eight' silently goes stale. Similarly corrMap's footnote types "n ≈ 45 seasons per county" though per-county n is computed and shown in tooltips.
  FIX: Move both numbers into the OJS layer (interpolate analogueYearsOND.length; derive the n range from corrByCounty) so no count is typed.
