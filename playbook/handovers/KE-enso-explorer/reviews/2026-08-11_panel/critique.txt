MISSED BY ALL PERSONAS:
- OUTLOOK SELF-CONTRADICTION IN THE LIVE STATE: the header shows El Nino (+0.88) with a NEGATIVE IOD (-0.44), yet the 6.2 'LIKELY WETTER' card conditions on ENSO analogues alone while the notebook's own 3 insight says IOD carries MORE OND signal (r~0.67 vs ~0.45). The current off-diagonal driver state (El Nino + negative IOD) is exactly where ENSO-only analogues are least reliable — the shipping headline may be wrong THIS season. Not one persona, including the climate scientist, flagged it. Needs an off-diagonal guard: when ENSO and IOD disagree in sign, the card must say so, widen the stated uncertainty, and show the IOD-conditioned analogue set beside the ENSO one.
- UPDATE CADENCE / FRESHNESS MACHINERY: nobody asked how this page ages. No per-figure 'data as of' stamps, no staleness banner when the CPC state or driver indices exceed their monthly cadence, no documented refresh runbook (CPC monthly, IPC per release, FEWS prices monthly, NDVI dekadal, CAP snapshot per build, NAPR per edition via the extract-knbs-napr rebase workflow, CHIRPS v3 updates). A decision-maker reading a 9-month-old 'El Nino 100%' card is the single most dangerous failure mode of a forecast-adjacent product; only one persona asked for an issuance date, on one card.
- ACCESSIBILITY BEYOND COLOURBLINDNESS: no persona covered alt text / aria-labels / role=img+title for ~19 SVG figures, keyboard operability of Observable Inputs and the unanimously-demanded click-map picker, screen-reader fallbacks (the existing Show-data-table is the natural a11y fallback but was never framed as one), focus order across the sticky bar, or WCAG contrast of the very encodings personas proposed (pale/low-opacity 'leaning' fills, 0.8px NDVI strokes, grey hatching).
- PRINT/OFFLINE AS A FIRST-CLASS USE-CASE: two spot @media-print fixes were proposed, but the actual product gap wasn't — a printable one-page per-county brief for county cabinet meetings and offline ASAL field use. In any static export, toggle states freeze at defaults, hover-only information (analogue share, n, tooltips) vanishes, and expander content disappears; the very PDF the panel reviewed shows the outlook map split across a page break and duplicated. A print stylesheet audit plus a 'Print county brief' view is missing entirely.
- DEEP-LINKABLE STATE: no URL parameters (?county=&season=&lang=). County users cannot share or bookmark 'their' view — which undermines the whole personalization premise and any dissemination plan (WhatsApp/email links are how county officials actually share). UX discussed anchor slugs but never query-state; this also blocks citing a specific view and testing per-county renders in CI.
- LANGUAGE FIT: sci-comm audited FR completeness but nobody asked whether French serves Kenyan county decision-makers at all. For a Kenya-specific product the material second language is Kiswahili; EN+SW (or a SW story-layer summary) versus the Atlas-standard EN+FR is an owner decision that should precede paying for the professional FR pass.
- FAILURE MODES AND FIRST-LOAD REALITY: no persona designed error/timeout/empty states for parquet fetch failure or DuckDB-WASM init failure — a known stuck-spinner stack (loader/render race, headless hang lessons). The payload critique counted parquets only: the DuckDB-WASM runtime bundle itself dominates first load on county-office bandwidth, and no one proposed lazy-loading below-the-fold sections or a skeleton/progress treatment for the 19-figure single page.
- CITATION OF THE NOTEBOOK ITSELF + LICENCE PROPAGATION: the engineer covered ACLED attribution, but there is no recommended-citation block, version, or DOI for the notebook; chartDownloadMenu CSV/PNG exports carry no source/licence metadata into the downloaded file; and IPC, FEWS NET FDW/XBT, and WFP VAM reuse terms were never verified the way ACLED's were. The 8 'please cite the original providers' sentence has no machine-usable counterpart.
- CONFLICT-DATA SENSITIVITY: publishing county-level conflict fatalities (4.2) juxtaposed with climate drivers in a government-facing, county-branded product received no do-no-harm or political-sensitivity review from any persona — only a statistical-honesty label. A county executive seeing their county ranked by fatalities next to an ENSO narrative is a reputational and framing risk that needs an explicit editorial decision.
- STATIONARITY OF THE CLIMATE BASELINE: analogue pools (1982-2023) and 1991-2020 normals span a warming record with a documented MAM long-rains decline; composites and analogues silently assume stationarity. Only PRODUCTION detrending was addressed (7.3); trend-nonstationarity of the rainfall composites/analogue method went unflagged by everyone including the climate scientist.
- COUNTY-JOIN CROSSWALK AS A CI GATE: the engineer's gaul1_code note covered CHIRPS only. No persona demanded one canonical 47-county crosswalk (GAUL24 code <-> KNBS name <-> FEWS admin <-> WFP p-code <-> IPC area) with per-dataset join-completeness assertions — the only systematic defence against Murang'a/Elgeyo-Marakwet/Tharaka-Nithi/Nairobi-City spelling drift across the 10+ sources this notebook joins.

CONFLICTS:
- Tabs vs single scroll: the owner, CECM and sci-comm all endorse a tabbed dual-audience layout; UX explicitly warns that charts inside hidden containers break Plot sizing and PNG export (and print/deep-links). Resolve in UX's favour: a visible story spine + visible technical annex + Explore section, navigated via TOC/short labels — adopt tabs only if every panel re-renders on activation, which no one has budgeted.
- Fate of the 2.2 maize chart: CECM says fold into 1.1's trend view; sci-comm says delete; ag-econ says supersede with the phase-annotated production timeline; the engineer says just retitle and keep. Resolve: supersede — the phase-annotated timeline (multiple personas' #1 ask) makes the standalone chart redundant; the engineer's retitle is the interim fix only.
- Statistics in the story layer: the climate scientist demands live-computed r/n substituted into the 3 insight text; CECM and sci-comm ban r/n/p from the story entirely. Resolve: both — qualitative wording ('strong/moderate link') in the spine, live OJS-interpolated r/n/p in the 7 annex, which also satisfies the scientist's no-static-numbers requirement.
- GESI treatment: dataviz invests in redesigning the full 47-dot strip in place (beeswarm, 320px, wrapped labels); CECM/UX/ag-econ demote GESI to a curated 3-6 indicator 'who a bad season puts at risk' card with the table in the annex. Resolve: curated card in the spine; dataviz's redesign applies to the annex version. Label truncation is fixed at the parquet/extractor source under the extraction gate either way — all four agree on that.
- Trade 4.6: dataviz invests (cropped EAC projection, kt arrow legend, phase-faceted composite maps); CECM and UX demote trade to one sentence + one slim chart with detail in the annex. Resolve: demote first; apply only the cheap fixes (projection crop, width legend) to the single annex map; decline the phase-faceted map series as effort on a demoted figure.
- chirps_county payload: the engineer's slim re-export (PTOT+SPEI-03+TAVG, 646 KB) conflicts with the scientist's asks for SPEI-12 (the 4 drought strip) and TMAX anomaly. Resolve: freeze the variable union from ACCEPTED findings before re-exporting (PTOT, SPEI-03, SPEI-12, TMAX/TAVG) — still roughly a 60% payload cut; do not ship 9 variables to use 1, and do not slim past what the accepted charts need.
- Ocean-driver control: UX relocates the radio to local controls above 3.2/7.3; CECM removes it from the simple view entirely with per-season auto-select (IOD/ENSO for OND, Western-V for MAM). Resolve: auto-bind driver to season in the story layer; expose the radio only in annex/Explore — the two are compatible once the spine/annex split exists.
- 6.2 season scope: UX wants one season concept driven by the global control; the scientist restricts the analogue outlook to OND and replaces MAM with a historical Western-V composite explicitly labelled 'not an outlook'. Resolve: the outlook section derives from the global season control (UX) but renders a different, honestly-labelled product per season (scientist) — 'no skilful MAM outlook exists here until KMD's seasonal outlook is machine-readable'.
- Subcounty interim path: the scientist offers WFP VAM admin2 dekadal rainfall as an interim (clearly labelled non-CHIRPS); the engineer shows the D409 admin2 CHIRPS v3 rerun is trivial (~40k rows, ~297 KB, exact gaul2_code join). Resolve: skip the VAM interim entirely — the rerun is cheaper than managing the mislabelling risk the scientist himself warns about, and it satisfies the owner's literal CHIRPS v3 subcounty ask.
- Crop-calendar placement: CECM wants it in the glance panel; UX and the engineer under 3.1's climatology; ag-econ under 3 plus reused in the 6.2 card. Resolve: build it once as a component — canonical strip under 3.1, a one-line alignment rendering reused in the glance card and the outlook card. Same resolution pattern for the county mini-map (picker at top, locator reused in cards).

PRIORITY VIEW:
If only 10 changes ship: (1) Restore the choropleth county click-picker + persistent mini locator (the panel's only unanimous finding, 5/7 personas; dropdown kept as fallback — and add the aria/keyboard support every persona forgot). (2) Build ONE shared annotation helper — driver-phase season ribbons + a curated events.json (~7 named events, EN/FR) — and apply it to the production charts, all six §4 panels and §5: this single helper IS the owner's remit ('impacts ASSOCIATED with ENSO/IOD/Western-V'), independently demanded by four personas, zero new data. (3) Restore the two port regressions: the VoP exposure chart as the county-stakes headline (routing ASAL counties to the livestock narrative) and the ASAP crop-calendar strip under §3.1 with the honest 38/47 gap. (4) §6.2 outlook-integrity bundle: OND-only analogues ranked nearest-neighbour, MAM replaced by a historical Western-V composite labelled 'not an outlook', confidence (share, n) encoded on a BrBG map, '>99%' capping, CPC issuance date on the card, controls fixed above the map with @media print — PLUS the off-diagonal guard nobody asked for (current El Niño with negative IOD contradicts the ENSO-only analogue conditioning and must be surfaced on the card). (5) Story-spine restructure: 60-second glance panel first screen, methods prose deduplicated into existing expanders, trade and Compare demoted to annex/Explore, empty §9 removed — implemented as a visible spine + visible annex, NOT display:none tabs (UX's hidden-container warning overrides the owner's tab suggestion and CECM/sci-comm's tab framing). (6) Plain-language pass: status banner, 'short rains (Oct–Dec)' relabelling everywhere, tiered glossary, house rule of no r/n/p in the story layer with live values OJS-interpolated in §7. (7) A shared missingMark() helper putting explicit 'not reported — not zero' markers on every KNBS/AFA chart, plus the §2.2 retitle to 'KNBS NAPR + AFA, 2019–24' (four personas flagged the blank≠zero violation). (8) Data-layer bundle: slim chirps_county to the accepted-variable union (PTOT, SPEI-03, SPEI-12, TMAX — reconciling engineer vs scientist), rerun D409 zonal at admin2 for true CHIRPS v3 subcounty (~297 KB) with a Kenya-only a2 topojson and iso3/gaul-sorted row-groups, and fix the v2/v3 caption drift in all five captions. (9) The teleconnection evidence set: phase-composite maps promoted into the story, correlation choropleth with FDR masking and per-county tercile counts in the annex — converting the notebook's central claim from assertion to demonstration, all client-side from served parquets. (10) Freshness machinery no persona requested but the outlook cannot responsibly ship without: per-figure 'data as of' stamps, a staleness banner when the CPC/driver state exceeds monthly cadence, and a documented refresh runbook (CPC monthly, IPC/NAPR per release via the existing extraction skills), with the KMD CAP snapshot's 'as of {fetched_at}' as the pattern. EXPLICITLY DECLINE: the agricultural economist's national watchlist table (analogue direction × NDVI deficit × IPC phase × livestock VoP ranking) — it compounds an n=8 modal tercile into a ranked county risk product, precisely the false-precision object the teleconnection scientist's uncertainty findings prohibit, and it drifts toward an operational early-warning forecast that the Kenya-Met-only constraint reserves for KMD/NDMA; revisit only if KMD's own seasonal outlook becomes a machine-readable input. Also deferred (not declined on principle, just sequencing): dataviz's phase-faceted trade-map investment, wasted effort once trade is demoted to the annex, and the goat:maize terms-of-trade panel, which is sound but is an addition — it should follow, not displace, the ten fixes above.