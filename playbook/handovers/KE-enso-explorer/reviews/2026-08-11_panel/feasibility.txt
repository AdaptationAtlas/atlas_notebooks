PAYLOAD BUDGET: Current FileAttachment'd payload ≈ 6.73 MB (18 parquets = 6.29 MB + a0/a1 topojsons = 0.44 MB); chirps_county.parquet alone is 4.52 MB (67% of parquet bytes), with only PTOT x OND/MAM/annual queried. Projected after all non-rejected items: additions ≈ +1.0–1.2 MB (admin2 CHIRPS ~0.3 + Kenya a2 topojson ~0.25 + station parquets 0.3–0.6 + CAP/calendar/vop/drivers/events/catalog ~0.1), offset by the CHIRPS slim re-export at −2.9 MB (3-var, all 13 periods, measured 1.64 MB) to −4.1 MB (3-var, OND/MAM/annual only, measured 0.38 MB). Net projected ≈ 3.7–5.0 MB — BELOW today's payload even with every build landed, provided the slim re-export ships with the SPEI/temp toggle.

[READY-NOW] County click-picker choropleth + county-at-a-glance header (merges: 'County-at-a-glance hero panel' [CECM], 'County choropleth click-picker' [UX], 'County-at-a-glance header card + restored click-picker locator' [viz critic], 'Choropleth county picker' [engineer], 'Restored Key-Facts choropleth county picker' [sci-comm])
  Pure port-regression restore + composition. a1 topojson already attached (qmd:1429); all five metric parquets attached except exposure_vop (+13 KB attach); nbText keys verified present (keyfacts.mapHeading/mapCaption/metrics.{vop,ndvi,ipc,rainOND}). Caveat: the CECM variant's subcounty zoom-inset needs the Kenya a2 topojson cut — defer that sub-feature until the admin2 item lands. ~+0.02 MB.
  source: data/shared/atlas_gaul24_a1_africa_simple-vlowres.topojson (attached), exposure_vop/ndvi_county/ipc_county/chirps_county parquets (local)

[READY-NOW] VoP exposure chart restore + livestock-share routing + GESI divergence card (merges: 'County economy at a glance' [CECM], 'VoP exposure bars + GESI risk card' [UX], 'Restore Atlas VoP exposure chart' [viz critic], 'Restored VoP stakes chart' [agro-economist], 'Exposure value-of-production chart' [engineer])
  exposure_vop.parquet verified: 12.7 KB, 1,156 rows, 47 counties x 34 commodities, kind=Crop|Livestock, vop_intld15, gaul1_code join, meta present — staged but NOT attached in qmd (confirmed port regression). gesi_v2 already attached. b1.vop* EN/FR strings verified in nbText.json. Notebook-only work: one FileAttachment + charts. +0.013 MB.
  source: data/KE-enso-explorer/exposure_vop.parquet (local, unattached), gesi_v2.parquet (attached)

[READY-NOW] JRC ASAP crop-calendar strip + outlook alignment line (merges: 'Season calendar strip' [CECM], 'ASAP crop-calendar strip' [UX], 'Crop-calendar strip + outlook alignment' [agro-economist], 'Crop-calendar strip restore' [engineer], 'Restored JRC ASAP crop-calendar strip' [sci-comm])
  seasonal_calendar.parquet verified: 5 KB, 571 rows, 38/47 counties (9 ASAL/urban honestly absent), county x crop x season x stage with m_start/m_end, meta present — staged but NOT attached (port regression). b2.calendar* incl. calendarNoData verified in nbText. §6.2 alignment sentence is code-templated from enso_state_probabilities (attached). +0.005 MB.
  source: data/KE-enso-explorer/seasonal_calendar.parquet (local, unattached)

[READY-NOW] Shared phase/event annotation layer + events.json + event timeline spine (merges: 'Event shading bands across §4' [CECM], 'Driver-phase ribbon annotation helper' [agro-economist], 'Named-event annotation layer' [sci-comm], 'Event timeline spine' [sci-comm])
  Phase-by-season already derivable from attached outlook_base.roni_conc + driver_indices. Correction to the agro-economist claim: enso_drivers_seasonal.parquet is local (18 KB) but NOT currently attached — trivial attach if wanted. events.json is ~3 KB curated names/dates/prose (no numeric claims — event windows derived from driver-index thresholds, not typed). Keep click-to-highlight interactions OUT of loader dep arrays (loader/render race rule). +0.02 MB.
  source: enso_outlook_base.parquet + driver_indices.parquet (attached); enso_drivers_seasonal.parquet (local, unattached); new tiny events.json

[READY-NOW] Event-anchored county impact stack / analogue event strips (merges: 'Past ENSO seasons in my county' [CECM], 'Event-anchored county impact stack' [UX])
  All inputs attached today: chirps_county, ndvi_county, ipc_county, market_prices + phase table. Pure OJS composition (shared-x stacked panels + per-analogue-year rows), no new data, no statistics claimed. +0 MB.
  source: chirps_county, ndvi_county, ipc_county, market_prices parquets (all attached)

[READY-NOW] County production x driver-phase event panel (merges: 'Phase-shaded county production timeline' [agro-economist], 'County production x driver-phase event panel' [engineer], 'County production x phase event strip 2019–2024' [viz critic], 'County produce x events mini-panel' [sci-comm])
  knbs_napr_county_production verified: 3,442 rows, 31 crops, 2019–2024 (meta refreshed 2026-08-07 — the '12 crops stale meta' note in DATA.md is itself outdated). afa_rice + driver phases attached. Season windows from seasonal_calendar (+5 KB, shared with the calendar item). Hatched not-reported cells; event alignment only, no n=6 statistics. +0 MB beyond calendar attach.
  source: knbs_napr_county_production.parquet + afa_rice.parquet (attached), seasonal_calendar.parquet (local)

[READY-NOW] Phase-composite / phase-grouped seasonal-anomaly choropleth small multiples + 2020–24 event map sequence (merges: 'Phase-composite rainfall-anomaly maps' [scientist], 'Phase-grouped CHIRPS seasonal-anomaly map small multiples' [viz critic], 'Event-anchored anomaly map sequence 2020–2024' [scientist]; also covers the admin1 'simple view' of the CECM subcounty-map ask)
  enso_outlook_base (attached, 58 KB) verified to carry anomaly_pct + tercile per county x season x year 1981–2025 with roni_conc/dmi_conc — everything the composites need client-side. MAM panels composite on wnp_std_mam from attached driver_indices (Western-V, not ENSO — correct per notebook framing). a1 topo attached. +0 MB.
  source: enso_outlook_base.parquet + driver_indices.parquet + a1 topojson (all attached)

[READY-NOW] Outlook honesty upgrades: certainty-encoded map v2 + tercile-frequency-by-phase contingency (merges: 'Outlook map v2 with certainty encoding' [viz critic], 'Tercile-frequency-by-phase contingency' [scientist])
  outlook_base tercile column served and already queried; analogue counts/share per county already computed for the hover. Client-side aggregation only; mask n<6–8 counties as specced. +0 MB.
  source: enso_outlook_base.parquet (attached)

[READY-NOW] Plain-language outlook card + driver-state chip strip (merges: 'Plain-language outlook card upgrade' [CECM], 'Plain-language driver-state chip strip' [UX])
  All values computed from attached driver_indices + enso_state_probabilities (currentState/stateProbs cells exist). Consequence/confidence lines are curated human-written qualitative prose in nbText (EN/FR) — compliant with no-LLM-typed-numbers since no numerals are typed. Include the 'history-based guidance, see KMD' footer. +0 MB (nbText growth only).
  source: driver_indices.parquet + enso_state_probabilities.parquet (attached), nbText.json

[READY-NOW] County correlation choropleth, driver x season (technical annex) [scientist]
  roni_conc + dmi_conc verified in served outlook_base (n≈45 yrs); Western-V per-year from attached driver_indices (wnp_std_mam/wep_std_ond). Pearson r computed deterministically in OJS — replaces, not substantiates-by-typing, the r≈0.67/0.45 prose. Keep annex-only with the FDR/multiple-testing footnote. +0 MB.
  source: enso_outlook_base.parquet + driver_indices.parquet (attached)

[READY-NOW] MAM historical Western-V composite replacing MAM analogue outlook [scientist]
  Data identical to the phase-composite item (outlook_base + wnp_std_mam from driver_indices, both attached). This is a methods/design swap in §6.2, not a data build; scientifically sound (MAM has no skilful ENSO conditioning). +0 MB.
  source: enso_outlook_base.parquet + driver_indices.parquet (attached)

[READY-NOW] Temperature / SPEI variable toggle + SPEI drought strips (merges: 'SPEI drought-severity integration' [scientist], 'Temperature/SPEI toggle' [UX], 'Temperature/SPEI toggle on rainfall panels' [engineer])
  Verified in chirps_county.parquet: variables PTOT, TAVG/TMAX/TMIN, SPEI-01/03/06/12/24 across 13 periods (12 rolling seasons + annual) — 100% already downloaded, 0% queried. Zero new bytes; the 12 rolling seasons give SPEI-12 a quasi-monthly strip for §4. Variable toggle is a DATA input → loader dep array (race rule). +0 MB.
  source: chirps_county.parquet (attached, variables verified)

[SMALL-BUILD] CHIRPS slim re-export (payload cut, pairs with the SPEI/temp toggle) [engineer sub-recommendation]
  Measured on the real file: full = 4.52 MB; PTOT+SPEI-03+TAVG all 13 periods = 1.64 MB (−2.9 MB, keeps the SPEI-12 quasi-monthly strip); PTOT+SPEI-03+TAVG x OND/MAM/annual = 0.38 MB (−4.1 MB, loses rolling-season SPEI). Engineer's '646 KB' sits between my two variants — re-measure at final variable set. Notebook currently queries only PTOT x OND/MAM/annual. Re-export script + meta refresh + verify in a REAL browser before promoting (DuckDB-WASM smoke-test rule).
  source: chirps_county.parquet re-export via _sources script

[READY-NOW] NDVI-by-phase county composite [agro-economist]
  ndvi_county attached: dekadal, 47 counties incl. all ASAL, 2002–2026, ndvi_pct_normal precomputed. Phase joins from attached drivers. Per-event-first-then-aggregate is required (ensembling-is-last rule) and is cheap client-side (~40k rows). +0 MB.
  source: ndvi_county.parquet + driver_indices/outlook_base (attached)

[READY-NOW] Goat:maize terms-of-trade panel [agro-economist]
  Verified in attached market_prices: 'Goats (Local Quality)' 4,864 rows / 20 counties / 2000–2026; 'Maize Grain (White)' in 42 counties. Deterministic DuckDB ratio; honest empty state for the ~27 counties without a goat series. One check in the cell: confirm unit column (goat per head vs maize per kg) before dividing. +0 MB.
  source: market_prices.parquet (attached, series verified)

[READY-NOW] Price anomaly toggle for §4.3 [agro-economist]
  Raw KES / 12-month change / month-of-year z-score all computable in DuckDB-WASM from attached market_prices — deterministic, no new data. +0 MB.
  source: market_prices.parquet (attached)

[READY-NOW] County watchlist table (outlook x conditions x stakes) [agro-economist]
  All columns from attached parquets + exposure_vop (attach shared with the VoP item). Forecast column = existing analogue tercile + NOAA CPC probs — constraint-compliant. Design caution: drop or clearly label the 'rank sum' mini-choropleth — a composite rank is an invented index; transparent sortable columns are the defensible product. +0 MB.
  source: enso_outlook_base, ndvi_county, ipc_county, market_prices (attached) + exposure_vop (local)

[READY-NOW] Season-window rainfall percentile vs production anomaly dot plot (annex) [agro-economist]
  chirps_county + napr attached; season windows from seasonal_calendar (attach shared with calendar item; CHIRPS-climatology fallback windows for the 9 ASAL counties). Explicit 'six seasons — pattern check, not a statistic' caption as specced. +0 MB incremental.
  source: chirps_county + knbs_napr_county_production (attached), seasonal_calendar (local)

[READY-NOW] Unified palette token module + §3.1 season-outcome redesign (merges the two viz-critic code items)
  Code-only refactor: PALETTE cell + recolour §3.1/§6.1/§6.2/§7.2; anomaly bars + phase ribbon lanes use already-served chirps + phase columns. Run node --check on edited OJS cells per memory rule. +0 MB.
  source: none (code only)

[READY-NOW] Dynamic county insight cards [sci-comm]
  All values interpolated in OJS from attached parquets (satisfies no-LLM-typed-numbers); sentence templates into nbText EN/FR. Beat-2 phase-mean sentence is a composite mean over n≈8–13 phase years — same legitimacy basis as the phase-composite maps. +0 MB.
  source: chirps_county, ndvi_county, ipc_county, napr, exposure_vop parquets

[SMALL-BUILD] Served-data catalog table (technical annex) [engineer]
  All 26 .meta.json sidecars verified present (refreshed 2026-08-07). NAPR meta already says 31 crops — the engineer's 'fix stale meta first' step is already done (DATA.md line 37 is the stale text). Needs a tiny build-time aggregator (meta files aren't attachable as a directory) emitting one catalog JSON. ~+0.03 MB.
  source: data/KE-enso-explorer/*.meta.json (26 files, verified)

[SMALL-BUILD] KMD CAP warnings panel (merges: 'KMD watch box' [CECM], 'KMD live warnings (CAP feed) panel' [UX], 'KMD CAP warnings strip' [engineer])
  Constraint-compliant (KMD is the approved forecast source). Live client fetch is CORS-blocked, so: build-time snapshot script in _sources (git-full, same pattern as enso_drivers_build.py) → cap_warnings.parquet ~5–10 KB + county geomatch. Mandatory honest 'as of {fetched_at}' stamp — a stale 'live warnings' panel is worse than none; request ClimWeb CORS allowlisting in parallel and upgrade to live fetch if granted. +0.01 MB.
  source: meteo.go.ke/api/cap/rss.xml → new _sources snapshot script

[NEW-PIPELINE] Subcounty CHIRPS v3 seasonal rainfall maps, admin2 (THE OWNER'S ASK; merges: 'CHIRPS v3 seasonal rainfall map timeline with subcounty overlay' [CECM], 'Subcounty CHIRPS v3 seasonal rainfall maps' [scientist], 'CHIRPS v3 seasonal timeseries maps with subcounty overlay' [UX], 'County-zoom seasonal timeseries maps' [viz critic], 'Subcounty seasonal rainfall timeseries map' [engineer])
  Verified: chirps_county.parquet has ZERO non-null gaul2_code/admin2_name rows; the Atlas hub product is adm1-only. Build = D409 zonal rerun at GAUL24 admin2 (Kenya, PTOT, OND/MAM/annual, 1981–2026, ~40k rows ≈ 0.3 MB) + Kenya-only a2 topojson cut (291 KEN features with gaul2_code verified in the shared file; ~0.2–0.3 MB — never attach the 3.34 MB Africa file). Same anchored BrBG scale as the national grid. Total ≈ +0.5–0.6 MB. The admin1 'simple view' interim is already covered by the phase-composite maps item.
  source: D409 zonal rerun on CHIRPS v3 @ GAUL24 a2 + Kenya cut of data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson

[REJECT] Interim WFP VAM admin2 dekadal-rainfall stopgap (sub-option proposed inside four subcounty-map recommendations)
  Two disqualifiers: (1) WFP VAM publishes on the OCHA LEGACY sub-county grid — its admin2 units do not match the GAUL24 a2 polygons in the shared topojson, so the choropleth join would be an approximate crosswalk with silent geometry mismatches; (2) it is a different rainfall product than CHIRPS v3 — shipping it under a 'CHIRPS v3' heading mislabels the data. The honest D409 rerun costs ~0.3 MB and reuses existing zonal code at a finer level; do that instead.
  source: HDX WFP VAM dekadal rainfall (not pursued)

[NEW-PIPELINE] Weather-station layer + CHIRPS validation annex (merges: 'Station-vs-CHIRPS validation annex' [scientist], 'Weather-station layer + CHIRPS validation strip' [UX], station overlay in county-zoom maps [viz critic], 'Weather-station overlay + CHIRPS validation scatter' [engineer])
  New acquisition but small, keyless and git-full-able in _sources (no D409 raster work): NOAA GHCN-Daily KE* + ISD/GSOD → QC-flag drop → ≥85% season-completeness gate → stations_key.parquet (~40 rows, county via point-in-polygon, never name-match) + station_seasonal.parquet ≈ 0.3–0.6 MB. Must be framed as point validation, not coverage (~10 counties have a long-record open station; TAHMO/KMD/ENACTS gated — per memory). Historical only → no forecast-policy conflict. +0.3–0.6 MB.
  source: NOAA GHCN-Daily + ISD/GSOD (open, keyless) → new _sources build
