
====================================================================================================
PERSONA: Non-technical county decision-maker — CECM for Agriculture in an ASAL county (Marsabit), no statistics training, 15 minu
====================================================================================================
VERDICT: For this reader the notebook currently fails its remit: the single answer I came for — "wetter or drier next season, how confident, what do I do" — exists (§6.2's analogue outlook card is genuinely the best thing in the notebook) but sits on page 8 of 12, behind a produce control panel, a GESI table, driver-index charts and r-values, with no map showing where my county even is. The raw material for my second question — "what did past El Niño / La Niña years do HERE" — is fully present across §3.1, §4.1–4.5 but is never composed into an event story; I am left to cross-read five separate charts by eye. My third question — "what should I watch and when" — gets two external links and no calendar, no KMD, no alerts. The single most important change: invert the structure so a plain-language, map-anchored "My county: now and next season" panel is the FIRST screen (outlook card + county map + season calendar + KMD alerts + one produce headline), and push driver mechanics, methodology and national context behind tabs/annexes. Everything needed for that panel already exists locally or is a small build.

STRUCTURE PROPOSAL: Adopt the owner's tabbed instinct, with the story inverted to lead with the decision: Tab 1 "My County" = glance hero (map picker + status banner + outlook card + season calendar + KMD alerts + economy headline) followed by the analogue event strips and a slimmed impacts chain (IPC, prices, NDVI with shared event bands) — everything a 15-minute reader needs, zero r-values, every caveat inside 'About this plot' expanders. Tab 2 "Explore & Compare" = the full NAPR produce multi-view, county comparison (§5), CHIRPS map explorer, trade detail. Tab 3 "Technical annex" = current §3.2, §6.1, §7 (driver mechanics, correlations, regressions — unchanged, they are honest and good for that audience). Tab 4 "Methods & Data" = current §8 restructured per-dataset + acknowledgements; kill the visible empty §9. Section order inside Tab 1: now → next season → what similar years did → how it reaches people; the driver-mechanics explainer (§3 intro + §3.1, simplified) moves to a short "Why?" section at the end of Tab 1 with a link into the annex.

[CRITICAL] §6.2 / global — The outlook — the reader's #1 question — is buried on page 8
  PROBLEM: A 15-minute decision-maker must scroll past produce controls (§1.1), a 15-row GESI table (§2.1), driver mechanics (§3), five impact charts (§4), and county comparison (§5) before reaching 'Given the Forecast, What Are the Coming Rains Likely to Do?'. The best artifact for this persona — the county card 'LIKELY WETTER than normal / 6 of 8 analogue years were wetter' with clickable analogue-year chips — is the second-to-last content figure in the notebook. Most county officials will never reach it.
  EVIDENCE: PDF p.8–9: §6.2 heading, choropleth, and card 'SHORT RAINS (OND) · MARSABIT … LIKELY WETTER than normal, 6 of 8 analogue years were wetter'; pages 1–7 contain no forward-looking statement for the county.
  REC (M): Promote the §6.2 outlook (map + county card) into a first-screen 'My county at a glance' hero panel, directly under the title. §1's current-state line and §6.1's driver chart become supporting detail behind it. The glance panel should read top-to-bottom in under 60 seconds: county mini-map → 'El Niño conditions now (June 2026)' → 'Short rains (Oct–Dec): LIKELY WETTER — more rain than usual in 6 of the 8 most similar past years' → what-to-watch line with next-update timing.

[CRITICAL] global — No map of my county anywhere; county selection is a naked dropdown
  PROBLEM: In 12 pages the selected county never appears on a map except as a thin outline inside the national §6.2 choropleth. Page 1 says 'Pick a county to begin' but the county picker itself only appears in the §6.2 control strip on page 8. A county official's first instinct — 'show me my county' — is unanswerable, and a Kenyan user cannot orient neighbours, subcounties, or ASAL context. The Key-Facts choropleth click-to-select picker that previously existed is a confirmed port regression.
  EVIDENCE: PDF p.1: intro text 'Pick a county to begin' with no visible picker; first county control appears p.8 §6.2 ('COUNTY / Marsabit' dropdown). Known-facts brief confirms the map picker regression and that the admin2 topojson exists locally.
  REC (M): Restore the click-to-select county map (GAUL24 topojson is already served) at the very top, with subcounty (admin2) boundaries overlain from data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson. Clicking a county sets the whole notebook. Keep the dropdown as a secondary control for accessibility.

[CRITICAL] §3–§4 — No 'what did the last El Niño / La Niña do to MY county' story — the data is all there but never composed
  PROBLEM: My second core question is event-shaped: 'in 2020–23 the rains failed — show me.' The notebook has OND rainfall by phase (§3.1), NDVI deficits (§4.5, prose even names '2011, 2017 and 2021–22'), price spikes (§4.3), IPC (§4.1) and conflict (§4.2) — but as five disconnected charts with independent x-axes. Nobody says 'In the 2020–23 La Niña, Marsabit lost five straight rainy seasons, pasture fell to ~70% of normal, maize prices doubled, and IPC hit Emergency.' I must derive that myself by eyeballing across three pages, which this persona will not do.
  EVIDENCE: PDF p.5 §4.5 prose: 'Watch the deep, sustained deficits — 2011, 2017 and 2021–22 are Kenya's major droughts' — the reader is told to do the synthesis manually; no chart or text ties phase years to county outcomes.
  REC (L): Add an event-anchored 'Past ENSO seasons in my county' block right after the glance panel: for each analogue year already computed in §6.2 (1997, 2015, 2023, 2020–22…), a compact per-event row: season rainfall vs normal, NDVI minimum, price change, peak IPC phase — all from parquets already loaded. Additionally add shared vertical event bands (La Niña 2020–23, El Niño 2023) across the §4 timeseries so the same droughts are visually linked chart-to-chart.

[CRITICAL] §6.2 'What to watch' — 'What should I watch and when' is unanswered: no season calendar, no KMD, no alerts
  PROBLEM: The only forward guidance is one sentence ('OND short-rains outlook is skilfully forecastable (IOD); MAM long rains are not') plus links to NOAA CPC and FEWS NET. There is no calendar telling me when MY county's seasons start (the JRC ASAP seasonal_calendar.parquet sits unused — a confirmed port regression), no Kenya Met Department presence at all despite KMD being the mandated national source and running a live CAP warnings feed, and no 'next update' date. For a county official, watching NOAA instead of KMD is institutionally backwards.
  EVIDENCE: PDF p.9: 'What to watch. OND short-rains outlook is skilfully forecastable (IOD); MAM long rains are not. For the live forecast see the NOAA CPC ENSO advisory and FEWS NET East Africa seasonal monitor' — no KMD, no dates, no calendar. Known-facts brief: ASAP parquet unused; KMD CAP feed live now.
  REC (M): (1) Restore the ASAP crop-calendar strip in the glance panel (38/47 counties; for ASAL gaps show an honest 'no cropping calendar — rangeland county' fallback with OND/MAM rain-season timing from CHIRPS climatology). (2) Add a KMD block: link to meteo.go.ke seasonal outlook and surface the live CAP alerts feed (meteo.go.ke/api/cap/rss.xml) as 'Current KMD warnings for your county' — this is constraint-compliant (KMD is the approved forecast source). Keep NOAA CPC as the ENSO-state source it already is.

[MAJOR] §1 header — The current-state line is a wall of codes, not a status
  PROBLEM: The very first data the reader meets is 'Current state (as of 2026-06) — Niño 3.4 (ONI, 3-mo mean) 0.88 °C (El Niño), IOD (DMI) -0.44. Selected county: Marsabit. Season lens: OND.' ONI, 3-mo mean, DMI, OND and '0.88 °C' are all unexplained; a non-technical reader gets nothing except the words 'El Niño'. 'Season lens' is app jargon.
  EVIDENCE: PDF p.1, first line under §1 heading, quoted verbatim above.
  REC (S): Rewrite as a plain-language status banner: 'El Niño conditions are present (June 2026). For Marsabit this has usually meant wetter Oct–Dec short rains.' Keep the index values in a hover/expander for technical users. Spell out OND/MAM as 'short rains (Oct–Dec)' / 'long rains (Mar–May)' at every first use per section.

[MAJOR] §1.1 — Extraction-methodology caveats dominate page 1 before any insight
  PROBLEM: Between the §1.1 heading and the first (tiny) chart sit two dense paragraphs of QA methodology: 'one deterministic engine (additivity to the printed national Total + completeness + dual-engine where readable; no number read by the model)', edition/rebase rules, AFA rice provenance. This is exactly the 'confusing details' the owner wants hidden from this audience — and an 'About this plot' expander already exists as the right home. The blank≠zero warning is important but is delivered as prose here while the chart that most needs it (§2.2) carries nothing.
  EVIDENCE: PDF p.1: two italic paragraphs incl. 'additivity to the printed national Total + completeness + dual-engine where readable; no number read by the model' rendered in the main flow, above the first bar chart; collapsed 'KNBS NAPR — methodology & per-table sources (53 tables · blank ≠ zero)' expander sits directly below, duplicating the topic.
  REC (S): Move all extraction/edition/provenance prose into the existing 'About this plot' / 'KNBS NAPR — methodology' expanders (they are already on the page — deduplicate into them). Keep exactly one plain sentence in the open: 'Blank means not reported, not zero.' Net visible text under §1.1 should be ~3 lines.

[MAJOR] §1.1 / §2.2 — ASAL county defaults to crops; livestock economy invisible; stray maize chart misplaced
  PROBLEM: The notebook's own text says 'In the arid north and east, livestock dwarfs crops — which is why an ENSO drought that fails pasture hits those counties hardest', yet with Marsabit selected the default §1.1 view is a five-crop bar chart topping out near 1k tonnes, and §2.2 gives Marsabit a full-page 'dry maize production (AFA)' chart inside the gender section. A pastoralist-county official sees their economy misrepresented on first contact. The Atlas value-of-production chart (exposure_vop.parquet, MapSPAM+GLW4) that would show livestock dominance in one glance is a confirmed port regression sitting unused.
  EVIDENCE: PDF p.1: §1.1 default 'Crops' radio checked, maize ~1k tonnes; PDF p.2: '2.2 Marsabit — dry maize production (AFA, 2020–24)' placed under '§2 What Does My County Look Like?' whose intro says the produce lives 'up top'. Italic caveat about livestock dwarfing crops is on p.1.
  REC (M): (1) Restore the VoP exposure chart as the county-economy headline (one bar/donut: crops vs livestock value share, then top items). (2) Default §1.1 SHOW to Livestock when the selected county's livestock VoP share exceeds crops. (3) Delete §2.2 as a standalone; fold the AFA maize trend into §1.1's 'trend' view where it logically lives.

[MAJOR] §2.2 — Missing county-year renders as zero — blank ≠ zero violated visually
  PROBLEM: The §2.2 Marsabit maize chart is titled '2020–24' but plots '19–'24, and 2022 has no bar at all. A naive reader — especially one who remembers 2022 as a drought year — will read 'zero harvest in 2022'. The blank≠zero rule is explained only in §1.1 prose two sections earlier; the chart itself carries no gap marker. This is a data-honesty constraint failure at the visual layer.
  EVIDENCE: PDF p.2, §2.2: heading says '(AFA, 2020–24)', x-axis runs '19–'24, bars present for '19,'20,'21,'23,'24, nothing at '22, no annotation.
  REC (S): Wherever a KNBS/AFA county-year is not reported, render an explicit gap marker (grey hatched band or '×  not reported' tick label) and add one caption line: '2022: not reported by AFA — not zero.' Fix the title to match the plotted year range.

[MAJOR] §2.1 — GESI table is unreadable and unconnected to the season story
  PROBLEM: Fifteen rows with truncated labels ('Proportion of households by primary reliance on' — on what?; 'Proportion of population that used improved' — improved what?), rank denominators that change row to row (17/43, 42/46, 2/47, 19/40) with the explanation only in 6pt footnote text, and quartile dot-strips no lay reader can parse. Worse, nothing links any indicator to ENSO risk, so the section intro's promise — 'who a bad season puts at risk' — is never cashed. I would scroll straight past.
  EVIDENCE: PDF p.2, §2.1: truncated row labels quoted above; mixed denominators '17/43…2/47…19/40'; footnote 'Rank by raw value (1 = highest); "better" direction varies by indicator'.
  REC (M): Replace the table in the main flow with 3–5 headline tiles chosen for season-risk relevance (e.g. households reporting lacking food 58.1% — 2nd highest in Kenya; teenage pregnancy; women's time burden), each with a one-line 'why this matters in a drought' sentence. Full 15-indicator table moves to the annex/technical tab. Fix truncated labels regardless.

[MAJOR] §3 / §7 leakage — Statistical jargon leaks into the non-technical main story
  PROBLEM: The §3 'Quick insight' — a block explicitly aimed at the story reader — contains 'r≈0.67 vs ENSO ≈0.45' and '(Niño 3.4 ↔ Western-V r=0.08)'. §3's intro deploys 'partly-independent Western-V Pacific pattern that ENSO explains little of', 'anomaly view', and ▲/▼ IOD markers explained only in prose. §6.1's y-axis reads 'index / z'; §3.2's reads '↑ v'. 'Tercile', 'analogue', 'detrended', 'z-score' all appear before or without definition-in-place. Each term is a stop sign for this persona.
  EVIDENCE: PDF p.3 Quick insight: 'the IOD carries more signal (r≈0.67 vs ENSO ≈0.45)'; PDF p.8 §6.1 axis 'index / z'; PDF p.3 §3.2 axis '↑ v'; PDF p.8 'most common tercile'.
  REC (S): House rule for the story layer: no r/n/p, no 'z', no Greek. §3 Quick insight keeps only its last sentence ('El Niño / positive-IOD years tend wetter, La Niña / negative-IOD drier — strongest in the arid north and east') as the headline, with the correlations moved to §7 where they already have a home. Rename axes ('index / z' → 'ocean index'; fix '↑ v'). Keep 'analogue' but always with its existing plain gloss ('past seasons most like the one forecast'). 'Western-V' gets one fixed plain description ('a Pacific ocean pattern that steers the long rains') reused everywhere.

[MAJOR] §6.2 — Outlook map layout is broken and the card frames 'wetter' as unambiguously good
  PROBLEM: Two issues in the notebook's most important figure. (1) In the render, the COUNTY/CLIMATE SEASON/OCEAN DRIVER control strip splits the Kenya choropleth in half — the southern third of the country floats below the controls, which reads as a bug and damages trust at the decisive moment. (2) The card shouts 'LIKELY WETTER' in green with no consequence line; for an ASAL county, a wet OND after drought means pasture recovery AND flash-flood / livestock-disease risk (1997 and 2023 El Niño floods hit Marsabit). Green + 'wetter' reads as 'relax'. Also, the reader is made to choose an 'OCEAN DRIVER' radio — a technical decision the notebook itself says has a right answer per season.
  EVIDENCE: PDF p.8: choropleth interrupted mid-figure by the control panel, with the southern chunk of Kenya rendered below it; p.9 card 'LIKELY WETTER than normal' with no caution or action text; p.8 control strip 'OCEAN DRIVER ◉ ENSO (Niño 3.4) ○ IOD (DMI) ○ Western-V (WNP)'.
  REC (M): (1) Fix the layout so map and controls never interleave. (2) Add a one-line 'what this can mean' to the card, phase-aware: wetter → 'good for pasture and planting, but watch flash floods and livestock disease after drought'; drier → action-oriented equivalent. (3) Remove the OCEAN DRIVER control from the simple view — auto-select IOD/ENSO for OND and Western-V for MAM (the notebook's own framing) and expose the radio only in the technical tab.

[MAJOR] §4.6 — National trade section spends 1.5 pages of a county story on non-county data
  PROBLEM: For a reader with 15 minutes, the cross-border trade block (centroid arrow map + imports-by-partner bars) costs about 12% of the notebook and answers no county question — the section itself opens with a warning that it is 'National, not your county'. It is honest, but it is placed inside '§4 How Does A Bad Season Reach People?', the persona's second-most-important section, diluting it. A Marsabit official trading through Moyale learns nothing actionable from country-centroid arrows.
  EVIDENCE: PDF p.6: '⚠ National, not your county — cross-border trade is measured at national border points…'; map + per-partner bar chart span pp.6–7.
  REC (S): Collapse §4.6 to one sentence + one small chart in the main flow ('Kenya imports maize when seasons fail — imports tripled in the 2021–22 drought') and move the arrow map and partner detail to the annex/technical tab.

[MAJOR] §7 / §8 / §9 — 'For analysts' content is labelled but not actually separated — and the notebook ends on an empty Appendix
  PROBLEM: §7 (3 pages of scatterplots, z-score charts, regression caveats) and §8 (a single ~350-word methods wall including 'ERSSTv5', 'de-rotate them by word position', 'dual-engine + additivity') sit inline in the same scroll. Labelling §7 'for analysts' is the right instinct but the non-technical reader still pays the scroll cost, and the document's final page is a bare '9. Appendix' heading over an empty page — a limp ending that reads as unfinished.
  EVIDENCE: PDF pp.9–11: §7 scatter 'r = 0.59, n = 75 (they co-vary)', §7.3 'r = 0.20, n = 64, not significant (p>0.05)'; p.11 §8 single dense paragraph; p.12: '9. Appendix' heading over an otherwise blank page.
  REC (L): Adopt the owner's tabbed proposal: 'My County' (story) / 'Explore & Compare' / 'Technical annex' (current §7) / 'Methods & Data' (current §8, restructured into per-dataset subsections rather than one paragraph). Remove the visible empty §9 heading entirely (keep hidden cells unlisted). Keep the acknowledgements visible in Methods.

[MINOR] §3.1 — ENSO colour semantics fight the rainfall story
  PROBLEM: Red = El Niño bars are the tall WET years in OND; blue = La Niña bars are the dry ones. Red/blue is correct ENSO convention, but a lay reader decodes red as danger/dry and blue as wet — the exact inverse of what the chart shows. The tiny ▲/▼ IOD triangles are additionally unexplained on the figure itself.
  EVIDENCE: PDF p.3 §3.1: legend 'El Niño (red) / La Niña (blue) / Neutral (grey)'; tallest bars (≈400–560mm) are red; no event annotations; triangles present but unlabelled on-figure.
  REC (S): Keep the phase colours (convention matters) but add an in-figure annotation on two or three landmark years ('1997 El Niño — floods', '2021 La Niña — rains failed') so the reader learns the mapping from the events, not the legend; add a one-line legend note 'El Niño years (red) are usually the WET short-rains years here'. Explain ▲/▼ in the legend, not only in intro prose.

[MINOR] §4.1 — IPC phases shown as bare numbers
  PROBLEM: The IPC chart's y-axis runs 0–5 with no phase names. IPC 3 vs 4 is the difference between Crisis and Emergency — the labels are the meaning, and ward-level staff this official shares the page with may not know the scale.
  EVIDENCE: PDF p.4 §4.1: axis '↑ IPC phase 5…0', no phase names anywhere on the figure or caption.
  REC (S): Label y ticks with phase names ('3 Crisis', '4 Emergency') and tint the ≥3 region; one caption line: 'Phase 3+ means crisis-level food insecurity requiring response.'

[MINOR] §4.4 — ReliefWeb report counts are a weak severity proxy and the table is oversized
  PROBLEM: 'Reports per year' partly measures humanitarian attention and archiving practice, not impact (2017's ~120 reports vs 2022's ~80 does not mean 2017 was worse). The 10-row recent-reports table then consumes most of a page in the main flow.
  EVIDENCE: PDF pp.4–5 §4.4: stacked bar chart 2010–2026 plus a 10-row table of titles/sources/hazards spanning most of p.5.
  REC (S): Keep the recent-reports list (genuinely useful for situational awareness) but collapse to the 5 most recent inside an expander; caption the counts chart 'report volume reflects attention as well as severity'. Consider filtering to drought/flood tags by default.

[MINOR] §5 — County comparison is nice-to-have, not decision-relevant, and its control state is invisible
  PROBLEM: Two spaghetti-line charts (rainfall, NDVI) comparing Marsabit/Nakuru/Turkana answer an analyst's curiosity, not a CECM's decision. The 'COUNTIES TO COMPARE' control renders as an empty underline in the PDF so the reader can't tell what's selected or how to change it.
  EVIDENCE: PDF p.7: 'COUNTIES TO COMPARE' label with no visible selected chips; three-county line charts §5.1–5.2.
  REC (M): Move §5 to the 'Explore & Compare' tab. Default the comparison set to the selected county + its neighbours (derivable from the topojson) so it opens with a locally meaningful contrast.

--- VIZ/DATA RECOMMENDATIONS ---
* County-at-a-glance hero panel with click-to-select map [new §1 (replaces current header block)]
  purpose: Answers 'show me my county' and 'wetter or drier, when, how confident' on the first screen for the non-technical official; becomes the notebook's single entry point.
  spec: Left: Kenya choropleth (GAUL24 admin1) with click-to-select, selected county filled and zoom-inset showing subcounty (admin2) boundaries; right: stacked cards — (a) plain-language ENSO status banner, (b) the existing §6.2 analogue outlook card verbatim but with a phase-aware 'what this can mean' line, (c) season-calendar strip, (d) one county-economy headline number. Height ≤800px total; dropdown retained as fallback control.
  data: All local: data/shared GAUL24 admin1+admin2 topojsons, existing analogue-outlook computation, NOAA CPC ENSO-state (already loaded). Restores the ported-out map-picker feature.
* Past ENSO seasons in my county (analogue event strips) [new §2, directly after the glance panel]
  purpose: Answers 'what did the last El Niño / La Niña do here' without asking the reader to cross-read five charts; event-anchored, so no fake county-level statistics needed (honours the short 2019–24 county series constraint).
  spec: One compact row per analogue year (reuse §6.2's analogue set + 2020–23 triple-dip as a named event): season rainfall vs 1991–2020 normal (bullet bar), NDVI minimum (% of normal), maize retail price change over the season (%), peak IPC phase (labelled chip). Rows sorted recent-first; the forecast phase's analogues highlighted. Click a row to highlight that year in the §3/§4 charts.
  data: All local: chirps_county.parquet, WFP VAM NDVI, FEWS FDW prices, IPC parquet, driver-phase table. Composition work only, no new pipeline.
* Season calendar strip (JRC ASAP) + KMD watch box [glance panel + §6]
  purpose: Answers 'what should I watch and when'; puts Kenya Met — the mandated national forecast authority — visibly in the loop (constraint-compliant: KMD + NOAA CPC only).
  spec: Horizontal 12-month strip per county: planting/growing/harvest bands from ASAP (38/47 counties; ASAL fallback = OND/MAM rain-season bands from CHIRPS climatology labelled 'rangeland seasons'), with 'you are here' cursor and the forecast season shaded. Beside it a KMD box: link to meteo.go.ke seasonal outlook + live CAP warnings for the selected county (geolocated alerts from meteo.go.ke/api/cap/rss.xml), with issued-date shown.
  data: seasonal_calendar.parquet EXISTS locally unused (port regression); CAP feed is live but needs a small fetch/refresh pipeline step and county geomatching.
* Event shading bands across all §4 impact timeseries [§4.1–4.5]
  purpose: Makes the drought/El-Niño years visually co-registered across IPC, prices, NDVI and conflict so the causal chain the section title promises ('How does a bad season reach people?') is visible to a lay reader.
  spec: Shared vertical bands on every §4 time axis: La Niña seasons tinted light orange, El Niño light blue, with named labels for major events ('2020–23 drought', '2023 El Niño'). Same bands in §3.1. One shared helper so the bands are pixel-identical across charts.
  data: Local: driver-phase-by-season table already used in §3.1. No new data.
* County economy at a glance (value of production) [§1.1 (headline above the existing NAPR multi-view)]
  purpose: Shows an ASAL official their livestock-dominated economy honestly in one glance, replacing the crops-default misread; also reinstates a ported-out Atlas feature.
  spec: Single horizontal stacked bar: crop vs livestock share of county VoP (constant I$), then top-5 items as small bars; caption gives the one number ('Livestock is ~X% of Marsabit's agricultural value'). The existing SHOW/VIEW/UNITS/FILTER panel moves below it and defaults to Livestock where livestock share > 50%.
  data: exposure_vop.parquet (MapSPAM + GLW4) EXISTS locally unused — port regression restore only.
* CHIRPS v3 seasonal rainfall map timeline with subcounty overlay [new subsection in §3 (simple view: latest 6 seasons; full explorer in technical tab)]
  purpose: The owner's explicit ask; for this persona it answers 'which part of my county got rain last season' — the first sub-county-resolution view in the notebook.
  spec: Small-multiple county maps (one per recent season, OND/MAM), fill = seasonal rainfall % of 1991–2020 normal (diverging brown–blue), admin2 boundaries overlain from the existing topojson, season/year scrubber in the technical tab; canvas raster with integer pixel boundaries if gridded, else admin2 choropleth. Optional GHCN/ISD station dots (~28 stations) as point context with observed totals — labelled 'station check', not coverage.
  data: Admin2 zonal CHIRPS needs a pipeline run: either D409 zonal rerun at admin2 (CHIRPS v3 at data.chc.ucsb.edu, CC-BY) or the WFP VAM HDX dekadal admin2 CSVs (p-code prefix = county) rolled up to seasons — VAM path is machine-readable today. Station layer: NOAA GHCN-Daily/ISD, open and keyless, sparse.
* Plain-language outlook card upgrade (consequence + confidence in words) [§6.2 card (also reused in glance panel)]
  purpose: Converts '6 of 8 analogue years were wetter' into something a non-statistician can act on without changing the honest analogue framing.
  spec: Keep the analogue count but add: a words-scale confidence line mapped from the analogue share ('most similar years were wetter'), a phase-aware consequence line ('after drought, heavy short rains can bring flash floods and livestock disease — as in 1997 and 2023'), and a 'this is history-based guidance, not a rainfall forecast — see KMD for the official outlook' footer. No new statistics; text templating only.
  data: Local: existing analogue computation + a small static lookup of consequence text per phase/tercile (curated prose in nbText.json, EN/FR).


====================================================================================================
PERSONA: Teleconnection climate scientist — East African rainfall variability expert (GHACOF / ICPAC / UCSB CHC reviewer)
====================================================================================================
VERDICT: The notebook's scientific hygiene is genuinely good — detrended production, live r/n/p on the national regression, honest non-significance labels, correct ENSO-leads/IOD-coupled/Western-V-independent framing in most prose — but its central scientific promise is asserted rather than demonstrated: there is no county-resolution evidence figure anywhere (no correlation map, no El Niño/La Niña composite maps, no tercile-frequency-by-phase), only single-county bars coloured by phase, while the quantitative claims that should anchor §3 (r≈0.67/0.45, 'strongest in the arid north and east') float free of any figure. The bitter irony is that enso_outlook_base.parquet — already served to every reader's browser — contains exactly the county × year × season anomaly/tercile/driver table needed to build all three canonical figures client-side with zero new pipeline. The single most important change is therefore to build that county teleconnection evidence block (composite maps into the story, correlation choropleth + tercile contingencies into the analyst annex). Second priority: stop conditioning the MAM outlook on ENSO across the spring barrier — the notebook's own framing says that driver is nearly irrelevant to MAM — and replace it with a historical Western-V composite. The CHIRPS v2/v3 caption contradiction is small effort but high credibility stakes and should be fixed immediately.

STRUCTURE PROPOSAL: Support the owner's tabbed instinct, implemented as a two-track split rather than many tabs: a default Story track (current §1→§6 linear county narrative, keeping only the phase-coloured bars and ONE promoted phase-composite anomaly map as the §3 evidence), and a Technical annex track holding the current §7 plus the new evidence block — correlation choropleths per season/driver with significance masking, per-county tercile contingencies, analogue-method robustness notes, station-vs-CHIRPS validation, and the methods/provenance detail that currently interrupts §1.1. Order within the story should stay season-causal (current state → county profile → drivers → impacts → outlook), but §2.2's stray maize chart should merge into §2's profile with phase annotation, and §6.2 should split OND (analogue outlook) from MAM (historical Western-V composite only). Every story-track insight that cites a number must link to the annex figure that computes it.

[CRITICAL] §3 (and global) — County-level teleconnection asserted, never demonstrated
  PROBLEM: The notebook's core remit — associate county rainfall with ENSO/IOD/Western-V — is supported only by §3.1 phase-coloured bar charts for one county at a time (evidence by eyeball). There is no county correlation map, no phase-composite anomaly map (El Niño vs La Niña years), and no tercile-frequency-by-phase figure anywhere. Yet the §3 Quick insight makes quantitative AND spatial claims ('r≈0.67 vs ENSO ≈0.45', 'strongest in the arid north and east') that no figure substantiates. A GHACOF/ICPAC reviewer's first three asks are all absent.
  EVIDENCE: grep of notebook.qmd: only two live correlations exist (ensoIodR line 1312, cropStat line 1340) — neither is county-resolved or mapped. Meanwhile enso_outlook_base.parquet (verified: 47 counties × 1981–2025 × OND/MAM, columns anomaly_pct, tercile, roni_conc, dmi_conc) already encodes everything needed and is ALREADY served to the browser — §6.2 uses only its modal tercile.
  REC (M): Add the three canonical figures, all computable client-side from parquets already served (no new pipeline): (a) correlation choropleth — per-county Pearson r of seasonal anomaly_pct vs roni_conc (ENSO, OND), dmi_conc (IOD, OND) and Western-V for MAM (join per-year WNP seasonal mean from the drivers parquet, which is national so a year-join suffices); mask or stipple counties with p>0.05 and add a field-significance/FDR footnote (Wilks 2016). (b) Phase-composite maps — mean anomaly_pct across El Niño / Neutral / La Niña years per season, side by side. (c) Per-county tercile counts by phase (e.g. 'in 12 El Niño ONDs: 8 Wet / 3 Near / 1 Dry'), as the selected county's contingency strip plus a map of P(Wet|El Niño). These three figures ARE the demonstration the notebook currently only claims; (b) is simple enough to promote into the non-technical story, (a) and (c) belong in the technical annex.

[CRITICAL] §6.2 — MAM analogue outlook conditions on ENSO, which the notebook itself says doesn't drive MAM
  PROBLEM: Analogue years are selected by ENSO phase for BOTH seasons (analogueYears, qmd lines 1455–1470), using FMA as a proxy for MAM — across the spring predictability barrier — while §3 states 'ENSO alone explains little of MAM (Niño 3.4 ↔ Western-V r≈0.08)'. The MAM map is thus a composite conditioned on a near-irrelevant driver, rendered with the same visual authority (full-country red/green choropleth) as the defensible OND map. The outlookMamCaveat text exists but a caveat under an authoritative-looking map does not neutralise it — this is internally inconsistent science.
  EVIDENCE: qmd line 1441: `stateProbs.find((d) => d.season === (outlookTarget === "OND" ? "OND" : "FMA"))`; nbText b2.insight1 'ENSO alone explains little of MAM'; b5.outlookMamCaveat 'MAM is outside the current CPC forecast window (nearest season used as a proxy)'.
  REC (M): Keep the ENSO-analogue outlook map for OND only. For MAM, replace the forecast-conditioned map with a purely HISTORICAL Western-V composite: 'in past Western-V high/low MAM seasons your county was X' — historical third-party data, so it violates no forecast constraint, and it matches the notebook's own framing. State plainly that no skilful MAM outlook exists in this notebook until KMD's seasonal outlook becomes machine-readable (ClimWeb icechunk/Zarr is coming; the CAP feed exists now). Name the spring predictability barrier explicitly in the FMA-proxy caveat.

[MAJOR] §6.2 — Analogue method: strongest-first ranking, n=8 modal colouring, no climatological reference
  PROBLEM: Three robustness gaps: (1) analogues are ranked by |RONI| 'strongest first' (qmd line 1467–1468), so a weak forecast El Niño is composited from the eight STRONGEST El Niños — biasing the map toward extreme outcomes; (2) the map colours each county by modal tercile with no visual encoding of share or n — a 3/8 plurality paints as confidently as 8/8 (share is hover-only); (3) '6 of 8 analogue years were wetter' is never compared to the 33% climatological expectation, so users can't judge whether 6/8 is remarkable.
  EVIDENCE: qmd lines 1467–1468 `matched.sort((a,b) => … Math.abs(b.roni) - Math.abs(a.roni)); return matched.slice(0, 8)`; PDF p8–9: near-uniformly green Kenya map + card 'LIKELY WETTER … 6 of 8 analogue years were wetter'.
  REC (M): (1) Rank analogues by similarity of concurrent RONI to the current/forecast index magnitude (nearest-neighbour), not by strength; (2) encode share on the map — opacity ramp or hatch counties with modal share <50%, annotate n on the legend; (3) add one sentence to the card: 'climatology alone would give ~3 of 8' and a note that 8 analogue seasons cannot distinguish shares below ~60% from chance.

[MAJOR] §3 Quick insight (nbText b2.insight1) — Static typed correlations (r≈0.67, 0.45, 0.08) with no figure, no derivation, no provenance
  PROBLEM: The headline scientific claim of §3 — IOD carries more OND signal than ENSO — cites r≈0.67 vs ≈0.45 as typed prose. Nothing in the notebook computes or displays these numbers; the reader cannot tell what rainfall series (national mean? which counties?), what window, or whether detrended. They will silently go stale as CHIRPS updates. Contrast §7.1, which correctly computes r = 0.59 live (ensoIodR) — and the near-collision of 'IOD-rainfall r≈0.67' with 'ENSO-IOD r≈0.6' (insight2) invites readers to conflate two different quantities.
  EVIDENCE: nbText.json b2.insight1: 'the IOD carries more signal (r≈0.67 vs ENSO ≈0.45)'; b2.insight2 'r ≈ 0.6'; PDF p10 §7.1 title 'r = 0.59, n = 75' (live). grep confirms 0.67/0.45 appear exactly once each, only in prose.
  REC (S): Compute both correlations live from the served parquets (national or county-median OND PTOT anomaly vs seasonal DMI and Niño 3.4) and substitute into the insight text like §7.1 does, with n; or, minimally, footnote the derivation and its source (the D409 RESEARCH doc). The correlation choropleth (finding 1) then substantiates the 'strongest in the arid north and east' spatial claim, which currently has zero graphical support.

[MAJOR] §3.1, §5.1, §6.2, §7.1, §7.2 captions — Captions credit CHIRPS v2; Methods and DATA.md say CHIRPS v3
  PROBLEM: Five figure captions cite '**Data:** CHIRPS v2 rainfall (UCSB Climate Hazards Center)' while §8 Methods states 'Rainfall: CHIRPS v3 (Atlas hub, county)' and DATA.md confirms 'CHIRPS v3 precip'. A version mis-citation is the first thing a CHC reviewer checks — v2 and v3 are materially different products (station density, algorithm) — and it undermines trust in every other caption.
  EVIDENCE: nbText.json lines 185, 301, 309, 313, 487 all read 'CHIRPS v2'; line 501 (methods) reads 'CHIRPS v3'; DATA.md §6 'CHIRPS v3 precip + CHIRTS-ERA5 temp + SPEI'.
  REC (S): Confirm from the D409 pipeline which version produced chirps_county.parquet, then correct all captions to match (expected: v3.0, cite data.chc.ucsb.edu/products/CHIRPS/v3.0/). Add the version to the §8 acknowledgement of UCSB CHC as well.

[MAJOR] §3.1 / §4 — SPEI and temperature are served to the browser and never used
  PROBLEM: chirps_county.parquet carries SPEI-01/03/06/12/24, TAVG, TMAX, TMIN for all counties, all 3-month periods, 1980–2026 — but every query filters `variable = 'PTOT'`. For the impact chain (§4) SPEI-12/24 is the canonical multi-season drought-severity metric and is THE variable for the 2020–23 triple-dip story (raw seasonal PTOT cannot show cumulative failure); TMAX anomalies are the heat-stress complement to El Niño OND. The notebook pays the byte cost of these variables without earning their scientific value.
  EVIDENCE: Parquet inspection: variables ['PTOT','SPEI-01','SPEI-03','SPEI-06','SPEI-12','SPEI-24','TAVG','TMAX','TMIN']; qmd line 1546 `AND variable = 'PTOT'`; grep for SPEI/TMAX/TAVG in qmd returns zero usage.
  REC (M): (a) §3.1: add a rainfall-view option 'Drought severity (SPEI-3)' alongside Absolute/Anomaly — same bars, season-matched SPEI-03, phase colouring unchanged; (b) §4: insert an SPEI-12 timeline strip directly above the NDVI chart — the two corroborate and jointly nail the 2020–23 drought; (c) optionally a TMAX-anomaly line in the county profile for heat context. Implementation caution per repo memory: the new variable toggle changes DATA, so it belongs in the loader dep array (it is not a presentation toggle).

[MAJOR] §2.2 — Production charts carry no driver context — the notebook's stated remit
  PROBLEM: The owner is right that the notebook 'fails to show historical production vs ENSO signals': §2.2 (AFA maize 2019–24) and the §1.1 produce views are bare bars with no indication of which years were La Niña drought (2020–23 triple-dip) or El Niño recovery (2023–24). With only 6 county-years, correlation is statistically illegitimate — but event-anchored annotation is not, and the series happens to span Kenya's best natural experiment in decades.
  EVIDENCE: PDF p2 §2.2: plain blue bars 2019–24, no phase shading, no annotation; §1.1 produce views likewise. Known-facts brief confirms 2019–2024 spans triple-dip La Niña and 2023 El Niño.
  REC (S): Shade or annotate the production-year axis by the season-relevant driver phase (same ensoPhaseByYear machinery already in §3.1), and add one honest sentence: 'six years is too short for correlation — but 2021–22 (La Niña, failed rains) vs 2023–24 (El Niño, recovery) is visible directly'. Optionally a county map of % change in maize production 2022→2024 as the 'recovery map'. No new data; no fake statistics; this is the event-anchored storytelling the short series legitimately supports.

[MAJOR] hero / intro — Hero sentence frames Western-V as a conduit of the ENSO signal
  PROBLEM: The opening sentence — 'how that signal reaches each Kenyan county — through the Indian Ocean Dipole (short rains) and a partly-independent Western-V Pacific pattern (long rains)' — grammatically routes the ENSO signal THROUGH the Western-V. This contradicts the notebook's own hard framing rule (Western-V is a partly-independent MAM control, NOT an ENSO conduit, r≈0.08) which the rest of the prose maintains carefully. It is the most-read sentence in the notebook.
  EVIDENCE: PDF p1 intro paragraph; CLAUDE.md framing rule 'Western-V is a partly-independent control on MAM … NOT an ENSO conduit'.
  REC (S): Rephrase: 'This notebook follows how that signal reaches each Kenyan county through the Indian Ocean Dipole (short rains) — and contrasts it with the MAM long rains, which answer mostly to a partly-independent Western-V Pacific pattern that ENSO explains little of.'

[MAJOR] global — No uncertainty treatment on any county-facing figure
  PROBLEM: §7.1 (CI band) and §7.3 (live r/n/p) handle uncertainty well, but everything a policymaker actually sees does not: §3.1 bars have no n or reliability cue, the §6.2 map hides share/n in hover, insights state relationships without sample sizes. For a notebook whose differentiator is statistical honesty, uncertainty must be visible where the non-technical audience looks, not only in the analyst annex.
  EVIDENCE: PDF pp3, 8–9: §3.1 and §6.2 carry no visible n/CI/share; §7.1 shows a shaded band; §7.3 shows 'r = 0.20, n = 64, not significant (p>0.05)'.
  REC (M): Minimum bar: every correlation shown displays n and p; every composite/analogue map displays the number of years composited in the legend; §6.2 encodes modal share visually (finding 3). For the new correlation choropleth, stipple/mask non-significant counties and add a one-line multiple-testing note. Also add a forking-paths footnote to §7.3's crop selector (cycling crops until one 'looks significant' is invited by the UI).

[MAJOR] owner remit (§3/§5) — Subcounty CHIRPS v3 maps are impossible with currently served data
  PROBLEM: The owner asks for county timeseries maps with subcounties overlain for CHIRPS v3 seasonal rainfall. chirps_county.parquet contains zero admin2 rows (verified: admin2_name all null, 255,024 rows), so no client-side work can deliver subcounty rainfall today. The admin2 GAUL24 topojson boundary file already exists in data/shared, so boundaries are not the gap — zonal statistics are.
  EVIDENCE: Parquet inspection: 'admin2 non-null rows: 0 of 255024'; known-facts: D409 pipeline did admin1, admin2 would be a rerun; data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson exists.
  REC (L): Two honest paths: (a) rerun the existing D409 zonal pipeline at admin2 on CHIRPS v3 (preferred — true CHIRPS v3, matches captions); or (b) interim, use WFP VAM dekadal admin2 rainfall from HDX (machine-readable, p-code prefix = county) clearly labelled as a different product, NOT as CHIRPS. Do not label VAM-derived maps 'CHIRPS v3'. Meanwhile ship the admin1 year-by-year anomaly small-multiples immediately (data served today).

[MINOR] §1 — 'ONI' label is loose for a trailing 3-month Niño 3.4 mean
  PROBLEM: The header calls the current state 'Niño 3.4 (ONI, 3-mo mean) 0.88 °C (El Niño)'. True ONI is ERSSTv5 Niño 3.4 anomalies on centred 30-year base periods updated every 5 years, and NOAA's event classification requires five consecutive overlapping seasons ≥ ±0.5 °C — a single 3-month window labelled 'El Niño' conflates nowcast phase with event status.
  EVIDENCE: qmd lines 1269–1277: `d3.mean(n34.slice(-3), …)`, `phase = oni > 0.5 ? "El Niño" …`; PDF p1 'Niño 3.4 (ONI, 3-mo mean) 0.88 °C (El Niño)'.
  REC (S): Label it 'ONI-style 3-month mean' (already done in code comments — surface it) and add a hover/footnote: 'NOAA declares an El Niño event only after five consecutive overlapping seasons ≥ +0.5 °C; this is the instantaneous state.'

[MINOR] §6.2 card — 'El Niño 100%' displayed as certainty
  PROBLEM: The card shows 'NOAA CPC forecast: El Niño 100%'. Verified faithful to the parsed CPC RONI probability table (OND = 100/0/0, issued July 2026), so not a parsing bug — but printing '100%' implies a certainty no seasonal forecaster intends (CPC rounds to integers), and the issuance date is not shown on the card, so staleness is invisible.
  EVIDENCE: enso_state_probabilities.parquet inspected: OND row la_nina=0, neutral=0, el_nino=100, issued 'July 2026'; PDF p9 card 'NOAA CPC forecast: El Niño 100%'.
  REC (S): Display probabilities ≥99.5 as '>99%', and print the issuance ('CPC, issued July 2026') on the card itself, not only in methods.

[MINOR] §8 vs §7.3 — Typed statistics drift from live-computed ones
  PROBLEM: Methods text types 'maize-IOD raw r=0.34 vs detrended r=0.21' while the live §7.3 currently computes r = 0.20, n = 64. Harmless today, but every typed statistic diverges from its live counterpart as data refreshes, and readers who cross-check will find mismatches.
  EVIDENCE: nbText.json methods line 501 'raw r=0.34 vs detrended r=0.21'; PDF p10 §7.3 header 'r = 0.20, n = 64'.
  REC (S): Where a live value exists, substitute it into the prose (OJS interpolation, as §7.1/§7.3 titles already do); where it doesn't, use '≈' and date-stamp the number.

[MINOR] §3.2 — Western-V index (wnp_std_mam) selectable outside its MAM standardization
  PROBLEM: The driver selector maps 'Western-V (WNP)' to column wnp_std_mam — a MAM-standardized index whose physical interpretation (Funk's post-1997 MAM control) does not carry to OND or annual windows, yet the season lens lets users average it over any months.
  EVIDENCE: qmd line 1251 driverCol mapping; line 1284 averages driverCol over seasonMonths for whatever season is selected.
  REC (S): Either restrict the Western-V option to the MAM lens, or caption the §3.2 figure that WNP is defined/standardized for MAM and other windows are shown for context only.

--- VIZ/DATA RECOMMENDATIONS ---
* Phase-composite rainfall-anomaly maps [§3 (story) + annex]
  purpose: The single figure that DEMONSTRATES the teleconnection for non-technical readers: what El Niño vs La Niña historically did to each county's season.
  spec: Small-multiple choropleths (El Niño | Neutral | La Niña) × season (OND, MAM), county fill = mean anomaly_pct across phase years, diverging brown–green scale centred at 0, n-years printed under each panel, selected county outlined; GAUL24 a1 topojson already loaded. Promote the OND triptych into §3 story; MAM panel composited on Western-V phase, not ENSO.
  data: enso_outlook_base.parquet (served today) + phase from roni_conc / drivers parquet — fully client-side, no pipeline.
* County correlation choropleth (driver × season) [annex (new)]
  purpose: The reviewer-grade evidence for analysts: where and how strongly seasonal rainfall tracks each driver, substantiating the typed r≈0.67/0.45 claim and the 'arid north and east' gradient.
  spec: Choropleth of per-county Pearson r (seasonal anomaly_pct vs seasonal driver index, n≈45 yrs), driver toggle ENSO/IOD/Western-V, season toggle OND/MAM; stipple or grey-mask p>0.05; footnote on multiple testing (FDR, Wilks 2016); tooltip shows r, n, p. Technical annex only.
  data: enso_outlook_base.parquet (roni_conc, dmi_conc served) + Western-V per-year join from enso_drivers parquet — client-side; no pipeline.
* Tercile-frequency-by-phase contingency [annex + §6.2]
  purpose: Answers the practitioner question 'given an El Niño, how often was my county wet?' — the probabilistic form GHACOF audiences expect.
  spec: For the selected county: 3×3 stacked-bar strip (phase → Dry/Near/Wet counts) per season, with the 33% climatology reference line; companion map of P(Wet | forecast phase) with counties n<8 masked. Feeds and honestifies the §6.2 card.
  data: enso_outlook_base.parquet tercile column — served today, client-side.
* Event-anchored anomaly map sequence 2020–2024 [§2/§4]
  purpose: Non-technical story of the triple-dip La Niña drought → 2023 El Niño recovery, county by county — legitimate storytelling where the short production series forbids statistics.
  spec: Year-by-year small-multiple county anomaly maps (anomaly_pct, OND) for 2020–2024 with phase labels; pairs with phase-annotated §2.2 production bars and a 2022→2024 production-change map.
  data: enso_outlook_base.parquet + knbs_napr_county_production.parquet — served today.
* SPEI drought-severity integration [§3.1, §4]
  purpose: Cumulative drought severity (the mechanism connecting failed seasons to IPC/NDVI/conflict in §4) — raw seasonal PTOT cannot show multi-season accumulation.
  spec: (a) §3.1 rainfall-view option 'Drought severity (SPEI-3)' with same phase colouring; (b) §4 SPEI-12 timeline strip above the NDVI chart, shaded below −1/−1.5/−2 severity thresholds. Variable toggle is a DATA input → belongs in the loader dep array (loader/render race rule).
  data: chirps_county.parquet SPEI-01…24 — served today, currently unqueried.
* Station-vs-CHIRPS validation annex [annex (new)]
  purpose: Answers 'can I trust CHIRPS in my county?' honestly for technical users, and fulfils the owner's station-data ask without pretending sparse stations give county coverage.
  spec: Technical annex: (a) map dots for GHCN-Daily/ISD stations over the county layer; (b) per-station scatter of seasonal station total vs co-located CHIRPS county value with r and bias; (c) one-line verdict per station. Explicitly framed as validation/point context, never a rainfall layer.
  data: NOAA GHCN-Daily (~10 counties) + ISD (~28 stations) — open, keyless; needs a small self-fetching build script (git-full achievable). CHIRPS side served.
* MAM historical Western-V composite (replaces MAM analogue outlook) [§6.2]
  purpose: Gives MAM an honest county map consistent with the notebook's own framing, removing the ENSO-conditioned MAM outlook.
  spec: Choropleth of mean MAM anomaly_pct across Western-V high vs low years (two panels), clearly labelled historical relationship — no forecast conditioning; §6.2 MAM tab swaps to this with a note that no skilful MAM outlook is offered until KMD structured outlooks arrive.
  data: enso_outlook_base.parquet + WNP year-classification from drivers parquet — client-side.
* Subcounty CHIRPS v3 seasonal rainfall maps [§3/§5 (new)]
  purpose: The owner's explicit ask: county timeseries maps with subcounty overlay.
  spec: Per-county zoom map, admin2 polygons filled by seasonal rainfall/anomaly, year scrubber or small multiples (≤800px); admin1 version shippable immediately, admin2 after data lands. Boundaries from data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson (already in repo).
  data: Does NOT exist locally (chirps_county.parquet has zero admin2 rows — verified). Needs D409 zonal pipeline rerun at admin2 on CHIRPS v3 (preferred), or interim WFP VAM admin2 dekadal rainfall from HDX clearly labelled as a non-CHIRPS product.


====================================================================================================
PERSONA: Senior UX / information architect — data-heavy public dashboards for mixed technical audiences (OCHA HDX / OWID-style IA
====================================================================================================
VERDICT: The notebook is honest, data-rich and well-engineered at the figure level, but its information architecture serves neither audience today: the non-technical reader hits a jargon status line, three unexplained global controls and a 120-word extraction-methodology paragraph before the first chart, with no map to anchor "my county"; the technical reader finds methods scattered across per-figure foldouts, a single-paragraph §8 wall, and three previously-built features (county map picker, VoP exposure chart, ASAP crop calendar) silently regressed out while their strings still sit in nbText.json. The deepest IA failure is that the story never connects its own halves — production (§1) and drivers (§3) are never shown together for a county, which is the owner's stated remit. The single highest-value change is restoring the county choropleth as the entry interaction and re-sequencing into a story spine (pick county → what it grows → what drives its rains → what happened in 2020–23/2023 → what's coming) with technical annexes at the end — not tabsets, which break OJS/PNG-export/deep-linking for marginal gain. Second priority: fix the control-scope lie — the sticky "Ocean driver" radio looks global but drives only 2 of ~16 figures.

STRUCTURE PROPOSAL: Recommend option (b): one page, story spine + per-section dig-deeper + visible technical annexes — not tabsets, not a page split. Tabsets (a) are the wrong tool here: OJS cells in hidden panes still execute (all the DuckDB cost, none of the benefit), Plot/PNG export and htl rendering misbehave in hidden containers, Quarto's TOC/scrollspy and deep links don't reach tab panes, and tabs would sever the causal narrative (drivers→rains→impacts) that is this notebook's whole value. A two-page split (c) is cleanest in theory but doubles nbText/control/parquet maintenance for a single-maintainer notebook and loses shared county state across pages (only recoverable via query params); revisit it only if the annex outgrows ~40% of the page. Proposed outline — Story spine: §1 Pick your county (chip strip → choropleth picker → county profile: produce multi-view + VoP + compact GESI risk card); §2 What drives its rains? (rainfall-by-phase, monthly climatology + ASAP calendar; driver radio local here); §3 What did the last ENSO cycle do here? (event-anchored stack: rainfall/NDVI/IPC/prices/produce with 2020–23 and 2023–24 bands); §4 What's coming? (recent drivers, analogue map + card, KMD warnings, What-to-watch); §5 Compare counties. Technical annexes (visible sections, question-free declarative headings): A1 Driver mechanics (ENSO×IOD scatter, combined z, driver index chart, temp/SPEI); A2 Production vs drivers — national, honest (current §7.3–7.4 with its non-sig framing); A3 National supply context (trade map + imports); A4 Subcounty rainfall maps + station validation; A5 Methods & per-figure source tables (break §8's wall into per-dataset subsections mirroring plotFooter meta). Sticky bar carries County + Season only; every non-county-scoped figure keeps the ⚠️ scope banner; section numbers and Figure N.M derived, not hard-coded.

[CRITICAL] global / §1 — No county map anywhere; entry is a bare dropdown
  PROBLEM: The notebook's organizing unit is the county, yet county selection is a naked Inputs.select in the sticky bar and no map of Kenya appears until §6.2 (page 8 of 12). A non-technical user picking 'Marsabit' gets no spatial anchor, no sense of what kind of county it is, and no invitation to explore. This is a confirmed port regression: the click-to-select Key-Facts choropleth was built (handover README: 'County map + multi-county compare also DONE 2026-07-16') and its strings still live in nbText.json (keyfacts.mapHeading 'Pick a county — Kenya at a glance', mapIntro, mapCaption, metrics.vop/ndvi/ipc/rainOND/rainMAM) but the qmd has zero references to them.
  EVIDENCE: grep of notebook.qmd returns no hits for mapHeading/mapIntro/exposure_vop; nbText.json contains sections.keyfacts.mapHeading = 'Pick a county — Kenya at a glance' + 5 metric labels; PDF p.1 shows only the dropdown-less 'Current state … Selected county: Marsabit' line; first map of Kenya appears PDF p.8.
  REC (M): Restore the choropleth picker as THE entry interaction at the top of §1: gaul24 a1 topojson (already loaded for §6.2), recolourable by the five metrics whose strings already exist, click sets `county`, selected county outlined. Keep the dropdown beside it as the accessible/searchable alternative. All data is local (exposure_vop, ndvi_county, ipc_county, chirps_county).

[CRITICAL] global (sticky bar) / §3.2 / §7.3 — Global 'Ocean driver' radio drives only 2 of ~16 figures
  PROBLEM: The sticky bar presents County / Climate season / Ocean driver as three global lenses. Tracing the qmd: driverKind → driverCol/driverName → used ONLY by §3.2 driverChart (driverSeasonal/driverMonthly, lines 427–446) and §7.3–7.4 (cropJoin line 1338, title_scatter). §3.1's rainfall colouring is hard-coded (OND=ENSO+IOD glyph, MAM=Western-V, rainPanel line 1566), §6's outlook is ENSO-only, §4/§5 ignore it entirely. Worse, §3's insight text says 'Switch the driver above to see it' — the user switches, and the rainfall chart they're looking at does not change. This teaches users that controls are unreliable, poisoning trust in the ones that do work.
  EVIDENCE: qmd line 208 (viewof driverKind in .ke-sticky-controls); line 1251 driverCol map; only consumers: driverSeasonal/driverMonthly (§3.2), cropJoin/cropStat (§7.3), title strings. nbText b2.insight1: 'Switch the driver above to see it.' §3.1 rainPanel hard-codes phaseKey = isMAM ? 'wnp' : 'enso'.
  REC (S): Remove the driver radio from the sticky bar. Place it as a local control directly above §3.2 (where a per-figure radio pattern already exists: rainTimeRes, driverTimeRes) and reuse it above §7.3. Sticky bar keeps County + Season only. Rewrite b2.insight1's 'Switch the driver above' to point at the now-adjacent control.

[CRITICAL] global — No progressive-disclosure architecture: one linear scroll serves neither audience
  PROBLEM: Plain-language story sentences, 120-word extraction-methodology paragraphs (§1.1 produceIntro + produceNote render as two italic walls BEFORE the first chart, PDF p.1), per-figure provenance foldouts, and analyst-grade caveats all interleave in one 12-page scroll. The non-technical reader must wade through KNBS edition/rebase detail to reach a bar chart; the technical reader must hunt methods across 16 foldouts plus a single-paragraph §8. There is no simple path and no complete technical path.
  EVIDENCE: PDF p.1: two dense italic paragraphs (produceIntro ~120 words incl. 'additivity to the printed national Total + completeness + dual-engine') precede Figure 1.1; §8 methods.text is one unbroken ~250-word paragraph (PDF p.11).
  REC (L): Adopt the story/explore two-track split (option b, detailed in structure_proposal): a story spine where each section leads with one plain-language sentence + one headline figure, per-section 'Dig deeper' foldouts for methodology text (charts stay OUT of closed <details> — hidden-container rendering breaks Plot sizing and PNG export), and visible technical annex sections at the end (extend the existing §7 pattern). Move produceNote/produceMethodology into the existing KNBS <details> block; the story text keeps one sentence: 'Blanks are counties KNBS did not report — not zero.'

[CRITICAL] §1 vs §3 (remit) — Production and drivers never meet at county level — the owner's core remit
  PROBLEM: The story shows what a county produces (§1, KNBS 2019–24) and what drives its rains (§3, CHIRPS+drivers 1981–) but never puts them in one view. The only production-vs-driver figure is §7.3 — national, detrended, r=0.20 n=64 non-significant, correctly labelled but buried for analysts. A county user cannot see 'the 2020–23 La Niña drought is these three failed OND bars, this NDVI collapse, this IPC spike, this price spike, and this dip in my county's beans.' The county series (2019–24) is too short for statistics but spans the triple-dip La Niña and 2023 El Niño — event-anchored storytelling is possible without fake correlation.
  EVIDENCE: §1.1 produce views stop at county bars/lines with no driver context; §3.1 rainfall has driver phases but no production; §7.3 caption: 'r = 0.20, n = 64, not significant (p>0.05)' and b3.intro: '⚠️ National, not county'. Known-facts brief confirms 2019–24 spans both events.
  REC (L): Add an 'What did the last ENSO cycle do here?' section: a shared-x, event-shaded stack (La Niña 2020–23 band, El Niño 2023–24 band) over county rainfall bars, NDVI, IPC and the KNBS produce lineplot, with plain-language annotations per event. No r-values at county level — narrative + shading only, keeping §7's honest national statistics as the technical counterpart.

[MAJOR] §6.2 vs sticky bar — Two competing 'season' controls with inconsistent labels
  PROBLEM: The sticky bar owns `season` ('OND', 'MAM', 'OND+MAM', 'annual' — raw acronyms) which drives §3.1/§3.2/§5.1/§7.3; §6.2 introduces a second, independent `outlookSeason` radio with different labels ('OND (short rains)', 'MAM (long rains)'). A user who set MAM at the top gets an OND outlook by default and no signal that the top control is being ignored. §5's intro even says 'Rainfall uses the season selected at the top' — the cross-reference exists precisely because scope is illegible.
  EVIDENCE: qmd line 198 viewof season vs line 851 viewof outlookSeason; nbText compare.intro: 'Rainfall uses the season selected at the top'; outlookSeason labels differ from season labels for the same two seasons.
  REC (S): One season concept: sticky-bar options relabelled 'Short rains (Oct–Dec)' / 'Long rains (Mar–May)' / 'Both' / 'Full year'; §6.2 derives its target from the global season (mapping Both/Full-year → OND with a note), or if outlook must stay independent, visually mark §6.2 as locally-scoped ('This section has its own season switch') using a distinct local-control style.

[MAJOR] §2 — GESI interrupts the climate story and its table ships truncated labels
  PROBLEM: Between 'what does my county produce' (§1) and 'what drives my county's rains' (§3) sits a 15-row gender-and-equity table — valuable context, wrong position: it breaks the produce→climate causal thread the intro promises. Additionally the rendered table shows truncated indicator labels that read as bugs: 'Proportion of population who spend less than 30', 'Proportion of households by primary reliance on', 'Proportion of population that used improved' (PDF p.2) — the units/objects of the sentences are missing.
  EVIDENCE: PDF p.2 §2.1 rows: 'Proportion of population who spend less than 30' (missing '…minutes to fetch drinking water'-type ending); section order §1 produce → §2 GESI → §3 drivers; b1.intro itself apologises for the split: '(What the county grows is up top…)'.
  REC (M): Fold GESI into the county-profile moment of §1 as a compact 'who a bad season puts at risk' card (top 3–5 indicators where the county diverges most from Kenya), with the full 35-series table in a technical annex. Fix the label truncation at source (gesi_v2.parquet label column or the extractor) — verify against the sheets; do not hand-type completions (extraction-gate constraint).

[MAJOR] §4.6 — National trade flows sit inside the county impacts section
  PROBLEM: §4 is 'How Does A Bad Season Reach People?' scoped to the selected county (IPC, ACLED, prices, ReliefWeb, NDVI all county-filtered) — then §4.6 pivots to national cross-border trade with a ⚠️ 'National, not your county' banner and two large figures (map 760×560 + imports bars) spanning ~1.5 PDF pages. The banner is honest but the placement forces every county reader through a national detour, and the same-for-every-county content re-renders per county visit.
  EVIDENCE: nbText b4.tradeIntro opens '⚠️ National, not your county'; PDF pp.6–7 show the trade map + imports chart consuming most of two pages inside §4.
  REC (S): Move trade to a technical annex ('National supply context') or to a short national interlude AFTER the county sections, keeping one slim figure (imports bars) with a link. The warning-banner pattern (⚠️ scope flags) is good — standardise it as the marker for any non-county-scoped content that remains in the spine.

[MAJOR] global (sticky bar) — Sticky-controls DOM hoist is fragile and collides with content in print/PDF
  PROBLEM: The controls bar is authored inside section 1 and moved into <main> at runtime by stickyControlsHoist (qmd 1388–1396) because CSS sticky is clipped by its parent section. In the PDF export the bar prints detached from the top and overlaps the §6.2 outlook map (COUNTY/CLIMATE SEASON/OCEAN DRIVER floating over the choropleth, PDF p.8), and page 1 shows a large empty band where the bar was hoisted from. Sticky top:56px also means in-page anchor jumps (e.g. the '§7 Supplemental analysis' link) land headings under the bar.
  EVIDENCE: qmd 1383–1396 (comment: 'CSS sticky is clipped by its parent <section id=keyfacts>… moving the node into <main>'); PDF p.8 control bar rendered over the outlook map; PDF p.1 whitespace gap between intro and §1.
  REC (S): Author the bar as the first child of the content column (before the first # heading) so no runtime hoist is needed; add `@media print { .ke-sticky-controls { position: static } }` and a scroll-margin-top on section headings equal to navbar+bar height so deep links land clear.

[MAJOR] §1 (entry) — Jargon wall at the entry point
  PROBLEM: The first data the reader meets is: 'Current state (as of 2026-06) — Niño 3.4 (ONI, 3-mo mean) 0.88 °C (El Niño), IOD (DMI) -0.44. Selected county: Marsabit. Season lens: OND.' Five acronyms, zero explanation, no visual. Control labels compound it: 'Climate season: OND / MAM / OND+MAM / annual', 'Ocean driver: ENSO (Niño 3.4)'. The hero promises a story for decision-makers; the first screen speaks analyst.
  EVIDENCE: PDF p.1 current-state line verbatim; qmd line 198 season options ['OND','MAM','OND+MAM','annual']; nbText controls.season = 'Climate season' with no option-level translation of acronyms.
  REC (S): Replace the status line with a plain-language chip strip: phase chip ('El Niño — moderate'), one-line meaning ('short rains usually wetter in most counties'), outlook chip from stateProbs, as-of date; put the numeric indices in a hover/foldout. Relabel season options ('Short rains (Oct–Dec)' etc.). Add a 3-term inline glossary on first use (ENSO, IOD, Western-V) — nbText already has good plain definitions scattered in b2.intro to reuse.

[MAJOR] global (numbering/anchors) — Hard-coded section & figure numbers are already stale and colliding
  PROBLEM: Section numbers live inside nbText title strings and figure numbers inside caption strings and title vars ('2.1 ${county} —…', '7.3 ${cropSel}…', 'Figure 4.6a'). This has already broken: b3.title = '4. How Does Climate Move Kenya's Harvests?' collides with b4.title = '4. How Does A Bad Season Reach People?' (b3's H1 was demoted into §7 but its numbered string survives), and anchor ids (#b1,#b2,#b4,#b5) are legacy block names two off from displayed numbers (§2=#b1, §3=#b2, §4=#b4, §6=#b5) — confusing for anyone sharing deep links. Any re-ordering (which this review recommends) means hand-renumbering ~30 strings in two languages.
  EVIDENCE: nbText: sections.b3.title and sections.b4.title both begin '4.'; qmd 1215–1229 title vars embed literal '2.1…7.4'; body anchors {#b1}{#b2}{#b4}{#b5} vs displayed §2/§3/§4/§6.
  REC (M): Strip literal numbers from nbText titles/captions; derive section numbers from document order (small OJS helper that numbers H1/H2 at runtime, or accept Quarto's number-sections) and generate 'Figure N.M' prefixes in plotFooter from a counter. Rename anchors to stable semantic slugs (#drivers, #impacts, #outlook) before the notebook gets external links.

[MAJOR] §2.2 (PDF) — Missing bars in the AFA/NAPR maize chart read as zero production
  PROBLEM: PDF §2.2 'Marsabit — dry maize production (AFA, 2020–24)' shows bars for '19, '20, '21, '23, '24 with 2022 absent and no annotation. During the worst drought year, an absent bar will be read as zero or as a chart bug — violating the notebook's own blank≠zero doctrine at exactly the moment it matters. (Note source/PDF drift: the current qmd body has no cell producing this figure and no reference to b1.maizeTitle, yet the string and afa_production.parquet exist — the chart shipped in the rendered PDF must be reconciled with the source.) The title also says 2020–24 while the chart plots 2019.
  EVIDENCE: PDF p.2 §2.2: no 2022 bar, no annotation; title '(AFA, 2020–24)' vs x-axis starting '19; grep shows no maizeTitle/afaMaize reference in current notebook.qmd while nbText b1.maizeTitle/maizeCaption exist.
  REC (S): Wherever county production is plotted over years (this chart and §1.1 Lineplot), render explicit missing-year markers: an × or hollow tick at y=0 with tooltip 'not reported by KNBS — not zero', driven by the known year domain minus reported years. Fix the title/domain mismatch. Reconcile qmd vs rendered PDF for §2.2.

[MAJOR] §1.1 / §3 (regressions) — VoP exposure chart and ASAP crop-calendar strip regressed out with strings orphaned
  PROBLEM: Two more built features are missing from the qmd while their nbText strings and parquets remain: (a) the Atlas value-of-production chart (b1.vopTitle/vopIntro/vopCaption; exposure_vop.parquet unused) — the only figure that says what a bad season puts at economic risk, and the one that explains why ASAL counties are livestock stories; (b) the JRC ASAP crop calendar (b2.calendarHeading/Intro/Title/NoData; seasonal_calendar.parquet unused) — the only bridge from rain timing to harvest timing, whose honest 38/47 no-data state was already written. Orphaned strings also mean the FR translation payload is silently drifting from the page.
  EVIDENCE: grep: zero qmd hits for vopTitle/calendarHeading/exposure_vop/seasonal_calendar; nbText contains full EN/FR strings incl. calendarNoData ('Crop-calendar coverage: 38 of 47 counties'); both parquets present in data/KE-enso-explorer/.
  REC (M): Restore both: VoP bars into the §1 county profile (crops green / livestock brown per vopCaption); calendar strip under §3.1's monthly climatology view where planting/harvest timing directly annotates the rains. Then add a CI-able check: every nbText.sections.* key referenced nowhere in the qmd fails the build (or is listed in a deliberate 'parked' allowlist).

[MINOR] global (TOC) — TOC labels wrap to 4–5 lines and the empty '9. Appendix' heading renders
  PROBLEM: Question-form H1s are great in the body but brutal in an ~80px sidebar: '1. Where Do Things Stand Right Now?' wraps to five lines (PDF every page), making the TOC a wall of fragments; '7. Supplemental analysis (for analysts)' wraps to five. Meanwhile '9. Appendix' prints as a visible heading with no content beneath it (PDF p.12) — atlasTOC skips it but the heading itself still renders.
  EVIDENCE: PDF sidebar on every page shows 4–5-line wrapped entries; PDF p.12 '9. Appendix' followed by blank page; qmd 1086–1088 heading is outside the ::: {.hidden} block that starts after it.
  REC (S): Give atlasTOC short labels ('1 · Now', '2 · My county', '3 · Rain drivers', '4 · Impacts', '5 · Compare', '6 · Outlook', 'A · Technical annex', 'Methods') via a per-section short-title map while keeping long question headings in the body; wrap the appendix H1 in the .hidden div (or drop the heading, keeping only the anchor).

[MINOR] §1 (insight) — 'Quick insight' at the end of §1 is actually orientation text
  PROBLEM: keyfacts.insight ('Kenya's rains are steered by ocean patterns — the IOD for the Oct–Dec short rains… Pick a county and follow how the climate signal reaches its harvests…') is instructional framing, not an insight derived from §1's produce chart. It renders after the KNBS methodology foldout and straddles the p.1/p.2 break, so it reads as a stray paragraph.
  EVIDENCE: nbText sections.keyfacts.insight text is second-person instruction ('Pick a county and follow…'); PDF p.1 bottom 'Quick insight' heading with body on p.2; Quick insights render as plain h3+paragraph, undifferentiated from intro italics.
  REC (S): Move this text into the notebook intro (it duplicates and improves sections.intro) and either drop §1's insight slot or replace it with a genuine dynamic insight from the produce data (e.g. '${county}'s top commodity is X; livestock is Y% of value' once VoP is restored). Style all Quick insight blocks as visually distinct callout boxes so they scan as payoffs.

[MINOR] global (figure furniture) — The §4.3 caption/About/Show-data-table exemplar covers 1 of ~16 figures
  PROBLEM: plotFooter supports caption + About + labelled data table + metadata CSV, but only §4.3 (prices) passes about/columns/meta (qmd comment: '§4.3 is the exemplar'). Every other figure gets an auto-derived table with raw column names (value_mean, ndvi_pct_normal, roni_conc) and no About body — so the technical audience's 'find the underlying data and methods' path is inconsistent figure to figure. Caption formatting also varies ('**Figure 4.3**' bold vs plain 'Figure 4.1').
  EVIDENCE: qmd 1104–1108 comment ('§4.3 is the exemplar'); body plotFooter calls for ipc/acled/rw/ndvi/compare/outlook pass only {filename, data}; nbText pricesCaption uniquely bolds '**Figure 4.3**'.
  REC (M): Finish the rollout: per-figure about text (much already exists as section intro italics that could move down), columns[] with friendly labels/units, meta{Source,Licence,County} for every figure — this is content work in nbText + call sites, no new machinery. Normalise the Figure-prefix styling in plotFooter itself.

[MINOR] global (responsive) — Fixed 900px plot widths with no overflow wrapper will break on mobile
  PROBLEM: Nearly every Plot.plot hard-codes width: 900 (or 700/760) and charts are not wrapped in an overflow-x container (only .plot-data-table-wrap has one). County policymakers will open shared links on phones; a 900px SVG in a ~375px viewport either overflows the body horizontally or gets clipped. The sticky bar's three stacked controls will also consume a third of a phone screen permanently.
  EVIDENCE: qmd: width: 900 at lines 429/437/475/495/560/601/644 etc.; no overflow-x rule for .cell-output-display; .ke-sticky-controls has no media query.
  REC (M): Wrap chart outputs in a shared `<div style="overflow-x:auto">` inside plotFooter's caller or give SVGs max-width:100%/height:auto CSS; for the sticky bar, collapse to a single compact county chip + expander below a width breakpoint. Test §6.2's 720×620 map specifically.

[MINOR] §6.2 — Analogue outlook map is near-uniform and hides its confidence
  PROBLEM: The current render is one green field (every county 'Wetter', PDF p.8) — a low-information map where the legend suggests variation that isn't on screen, while the genuinely decision-relevant numbers (6 of 8 analogue years; share %) hide in hover tooltips and the card below. Non-technical users read a solid-green Kenya as a strong forecast; it's an analogue composite with varying support.
  EVIDENCE: PDF p.8: all counties green except one grey; share/count only in Plot title tooltip (qmd 880–881) and in the card ('6 of 8 analogue years were wetter', PDF p.9).
  REC (S): Encode confidence: opacity or a hatch by `share` (e.g. <60% analogue agreement rendered pale), and surface 'X of N analogue years' as a small map subtitle. Keep the excellent county card as the headline and consider shrinking the map to a supporting role beside it.

--- VIZ/DATA RECOMMENDATIONS ---
* County choropleth click-picker (restore) [§1 (top, replacing bare dropdown as primary selector)]
  purpose: Give both audiences a spatial entry point — 'find and pick my county' — and let non-technical users browse Kenya by what counties grow, how green they are, food insecurity or seasonal rain before committing to one.
  spec: Kenya a1 choropleth (mercator, ~640px), metric select from the five existing nbText metric strings (VoP I$, latest NDVI %, latest IPC phase, mean OND mm, mean MAM mm); sequential palette per metric, click sets `county` (writeback to the viewof), selected county outlined 2px, hover tooltip name+value; dropdown kept alongside for search/accessibility.
  data: All local: data/shared/atlas_gaul24_a1_africa_simple-vlowres.topojson (already loaded), exposure_vop.parquet (unused), ndvi_county.parquet, ipc_county.parquet, chirps_county.parquet. Strings already in nbText (keyfacts.mapHeading/metrics/*). No pipeline work.
* Event-anchored county impact stack ('the last ENSO cycle here') [new story section between current §3 and §4 (or replacing §4's opening)]
  purpose: Close the remit gap — associate county production/exposure with ENSO/IOD events honestly, without county-level correlation claims, for the non-technical spine.
  spec: Vertically stacked shared-x (2015–present) panels: seasonal rainfall anomaly bars, NDVI % of normal, IPC phase, staple retail price, KNBS produce lineplot (top items) — with two labelled Plot.rect shaded bands (La Niña 2020–23, El Niño 2023–24) spanning all panels and 2–3 plain-language annotations per band; county-reactive; no r/p statistics displayed.
  data: All local: chirps_county, ndvi_county, ipc_county, market_prices, knbs_napr_county_production. Event-window dates hard-coded from driver_indices (deterministic threshold, not typed numbers).
* CHIRPS v3 seasonal timeseries maps with subcounty overlay (owner's explicit ask) [§3 (dig-deeper panel) or technical annex]
  purpose: Show where within the county the rains failed, season by season — the owner's requested 'timeseries maps of county with sub counties overlain'; serves technical users and county officers who think in subcounties.
  spec: Small-multiples grid (last 12 seasons of the selected season, 4×3, each ~180px) of subcounty (admin2) seasonal rainfall anomaly vs 1991–2020, diverging brown–blue palette, admin2 boundaries stroked, county outline heavy; hover = subcounty + mm + anomaly; season follows global control; canvas/fillRect with integer pixel boundaries if raster tiles are used instead of polygons.
  data: Boundaries exist (data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson). Values need a pipeline: either D409 admin2 zonal rerun on CHIRPS v3 (admin1 already done; same code, finer level) or WFP VAM HDX dekadal rainfall on the OCHA admin2 grid rolled up via p-code prefix. Not in the local data dir yet.
* Weather-station layer + CHIRPS validation strip [technical annex (with a station-dot overlay option on the county picker map)]
  purpose: The owner's 'I would like to see weather station data' — gives technical users ground-truth context for the gridded CHIRPS series while being honest that open station coverage is sparse.
  spec: (a) Station dots (GHCN-Daily ~10 counties + ISD ~28 stations) on the Kenya map, sized by record length, tooltip station/years; (b) per-station seasonal-total scatter vs co-located CHIRPS county value with 1:1 line and r shown live; framed explicitly as 'point validation, not county coverage'. No forecast content (historical only, so Kenya-Met-only rule untouched).
  data: Needs a small keyless pipeline: NOAA GHCN-Daily + ISD fetch → station_seasonal.parquet (station, lat/lon, county, season, year, mm). Not local yet; TAHMO/KMD/ENACTS are gated — do not promise dense coverage.
* VoP exposure bars (restore) + GESI risk card [§1 county profile]
  purpose: 'What a bad season puts at economic risk here' for the story spine — and the anchor that explains why ASAL counties are livestock stories; GESI top-divergence card gives the equity context without a 15-row interrupt.
  spec: Horizontal bars by commodity, crops green / livestock brown, constant I$, county-reactive, ≤400px tall; beside it a compact card of the 3–5 GESI indicators where the county ranks in the top/bottom quartile (value, Kenya value, rank chip), linking to the full table in the annex.
  data: Local: exposure_vop.parquet (unused), gesi_v2.parquet (already queried). Strings exist (b1.vop*).
* ASAP crop-calendar strip (restore) [§3, under the monthly-climatology view]
  purpose: Bridge rain timing to harvest timing — explains the harvest lag that the technical annex's season-window regression depends on, and tells non-technical users when a failed season shows up in food.
  spec: One horizontal strip per crop: Jan–Dec ribbon with planting/growing/harvest segments coloured, MAM/OND season bands as background tint matching the climatology chart's colours; honest empty state for the 9 ASAL counties using the existing calendarNoData string pointing to NDVI.
  data: Local: seasonal_calendar.parquet (unused, 38/47 counties). Strings exist (b2.calendar*).
* Plain-language driver-state chip strip [§1 (first content under hero)]
  purpose: Replace the jargon status line for the non-technical entry: what phase are we in, what does that usually mean, what's forecast — in one glance.
  spec: Three chips: current phase (colour-coded, 'El Niño — moderate'), plain meaning line ('short rains have usually been wetter'), CPC forecast chip ('El Niño likely to continue — NOAA CPC, 100%'), as-of date; numeric indices (ONI/DMI values) in a hover title or small 'details' foldout. All values computed from existing cells (currentState, stateProbs), no typed numbers.
  data: Local: driver_indices.parquet, enso_state_probabilities.parquet (both already loaded).
* Temperature / SPEI variable toggle on county climate series [technical annex (or a local toggle on §3.1's dig-deeper)]
  purpose: Depth for the technical user ('dig deep and find the underlying data') at near-zero data cost — heat and drought-index views of the same county story.
  spec: Variable radio (Rainfall / Temperature / SPEI) on the seasonal by-year chart; same driver-phase colouring; anomaly view reuses the 1991–2020 baseline logic; y-label/units switch per variable.
  data: Local: chirps_county.parquet already contains TAVG/SPEI variables (only PTOT plotted today) — verify variable codes, no pipeline.
* KMD live warnings (CAP feed) panel [§6 'What to watch']
  purpose: Make 'What's coming' actionable with the only allowed forward-looking Kenya source besides CPC — current geolocated KMD alerts for the selected county.
  spec: List of active CAP alerts (title, severity chip, area, onset/expiry, link to meteo.go.ke), filtered to alerts intersecting the selected county where geometry allows, else national list; explicit 'Source: Kenya Meteorological Department' attribution; empty state 'No active KMD warnings'.
  data: meteo.go.ke/api/cap/rss.xml exists now but is a live third-origin fetch — likely CORS-blocked client-side; needs a small pipeline that snapshots the feed to a dated JSON at build/refresh time (constraint-compliant: KMD source). Honest 'as of <date>' stamp required.


====================================================================================================
PERSONA: Data-visualization critic (Tufte/Few school; Observable Plot + choropleth cartography)
====================================================================================================
VERDICT: The data behind this notebook is unusually honest, but its visual rhetoric fails both audiences: the non-technical reader gets ~19 disconnected, mostly-default-styled charts, only two maps in twelve pages (neither locating the selected county), and no single figure where the driver→rainfall thesis is visible without mental arithmetic; the technical reader gets honest labels undercut by craft defects (y-axes literally labelled "v" and "t", years rendered "1,985", "0.2k" ticks). The deepest problem is that the same climate driver wears three different colours across the document (El Niño red in §3.1, orange in §6.1, blue in §7.2) while red/green/orange are simultaneously reused for outcomes, so the reader can never learn the colour language. The single change that matters most: adopt an anomaly-first visual grammar — driver PHASE lives only in dedicated ribbons/chips (red/blue), rainfall OUTCOME lives only in a BrBG brown↔teal diverging scale — and express it in a rebuilt §3.1 plus a phase-grouped small-multiple anomaly map grid. That one grid is the artifact that makes the notebook's core claim visible at a glance and directly delivers the owner's requested timeseries maps.

STRUCTURE PROPOSAL: Keep ONE linear story spine for the non-technical reader — County picker + at-a-glance card (pinned) → §1 Now → §2 My county (maps first: locator, production event strip, VoP) → §3 Why (season-outcome chart + anomaly map grid + county-zoom maps) → §4 Impacts → §5 Compare → §6 Outlook — and use tabs only at two scales: (a) within-section Quarto tabsets for view variants (map / chart / table / absolute-vs-anomaly), which is where tabs genuinely help, and (b) one global 'Technical annex' destination that absorbs §3.2, all of §7, the methods prose, and per-figure 'About this plot' details, cross-linked from small '→ annex' pointers under each figure. A fully tabbed top-level navigation would fragment the narrative scroll that story-notebooks depend on and double the maintenance surface; section-level tabsets plus a single annex give the two-audience split at the lowest navigation cost, and the pinned glance card keeps county context alive in every section.

[CRITICAL] global — One driver, three colours — no coherent colour language
  PROBLEM: The same physical driver changes hue between figures while the same hues are reused for unrelated outcomes. ENSO/El Niño is red in §3.1 bars, ORANGE in §6.1 (default categorical), BLUE in §7.2; Western-V is teal/brown in §3.1 MAM panels but red in §6.1/§7.2; blue means La Niña (§3.1), IOD (§6.1), the selected county (§2.1), crops (§1.1) and scatter dots (§7.3); red means El Niño, Drier (§6.2), OND season (monthly climatology), conflict (§4.2) and Uganda (§4.6). A reader cannot build a colour vocabulary — every figure resets it.
  EVIDENCE: notebook.qmd L439/L1574 (El Niño #d73027, La Niña #4575b4), L826–833 (§6.1 no colour range → Plot default: Niño orange, IOD blue, WNP red per PDF p.8 legend), L983–984 (§7.2 legend PDF p.9: ENSO blue, IOD orange, WNP red), L874 (Drier #d73027 = El Niño's hex), L1626 (OND season also #d73027).
  REC (M): Create one shared OJS PALETTE cell and route every color:{} through it. Rule: DRIVER PHASE colours (El Niño #d73027 / La Niña #4575b4 / neutral grey; same warm/cool logic for IOD and WNP but each confined to its own labelled ribbon lane or chip — position disambiguates, not new hues) may ONLY appear on chips, ribbons, and dots that literally denote phase, never as fills of outcome marks. RAINFALL OUTCOME uses BrBG (drier=brown #a6611a, wetter=teal #018571) — the hydrological standard, colourblind-safe, and free of collision with ENSO red/blue. IMPACT good/bad uses Atlas yellowGreen/orangeRed (NDVI already close). Recolour §6.1 and §7.2 driver lines to the phase-family hues (Niño red-family, IOD purple or blue-outline, WNP dark slate) consistently.

[CRITICAL] global — A county notebook with almost no maps and no county locator
  PROBLEM: Two maps in 12 pages: a national trade-arrow map (§4.6) and the outlook choropleth (§6.2). Nothing ever shows WHERE Marsabit is, its shape, its subcounties, or the spatial pattern of any rainfall/NDVI/production variable. County selection is a bare dropdown buried in §6.2's control panel; the previously-built click-to-select Key-Facts choropleth picker is a confirmed port regression. For county policymakers — people who think in maps — this is the notebook's biggest experiential failure and it blocks the owner's explicit timeseries-map request.
  EVIDENCE: PDF pp.1–12: only maps are p.6 (trade arrows) and p.8 (outlook). §1 header shows 'Selected county: Marsabit' as plain text; the only county <select> control appears inside §6.2's floating panel (p.8). Handover README: 'Key Facts choropleth spatial picker (click-to-select)… DONE (2026-07-16)' — absent from current qmd.
  REC (M): Restore the choropleth click-picker (previously built, per handover README 2026-07-16) as the county selector at the top; add a persistent mini locator map in the county-at-a-glance card; add the CHIRPS seasonal-anomaly small-multiple map grid (spec in viz recommendations) using data already served in chirps_county.parquet + the shared GAUL24 topojsons.

[CRITICAL] §3.1 — The core driver→rainfall figure hides its own message
  PROBLEM: §3.1 is the figure the whole notebook rides on, and it makes the reader do the work: (a) default view is ABSOLUTE mm, so 'El Niño years are wetter' requires mentally subtracting a ~150 mm baseline across 45 bars; (b) phase is encoded in bar FILL, spending the strongest channel (colour) on the categorical driver instead of the outcome; (c) the IOD — which the notebook's own text says carries MORE signal than ENSO (r≈0.67 vs 0.45) — is relegated to 9px ▲/▼ glyphs in #333 floating at bar tops, which at PDF scale are illegible specks and collide with tall bars; (d) with absolute bars there is no zero-crossing, so phase-colour vs height shows no visible covariation.
  EVIDENCE: L1568 `anomaly = rainView.startsWith("Anomaly")` with radio default 'Absolute (mm)' (PDF p.3 shows absolute view checked); L1586–1589 glyphs `fontSize: 9, dy: -6, fill: "#333"`; PDF p.3: ▲/▼ barely resolvable above ~40 bars; Quick insight text p.3: 'the IOD carries more signal (r≈0.67 vs ENSO ≈0.45)'.
  REC (M): Rebuild as the 'season outcome' figure: anomaly view DEFAULT (vs 1991–2020), bars diverging from 0 and FILLED by outcome (BrBG brown/teal, doubling the height encoding — honest redundancy); driver phase moves to two thin categorical ribbon lanes under the x-axis (row 1 ENSO, row 2 IOD for OND; row 1 WNP for MAM) in phase colours; add three summary chips above (mean anomaly per phase, e.g. 'El Niño years: +64 mm (n=12)' — deterministic OJS aggregation of served data, no LLM numerics). Keep absolute mm as the toggle, not the default.

[MAJOR] §2.2 — Blank ≠ zero is violated by the bar encoding
  PROBLEM: The project's hard rule is that a missing KNBS county-year is an admin gap, never zero — the prose says so (§1.1: 'blank, not zero') — but the bar charts contradict it visually: in §2.2 Marsabit maize, 2022 simply has no bar, indistinguishable from a zero harvest (which, in a drought notebook, is exactly the misreading a viewer will make: '2022 triple-dip La Niña → total crop failure'). Same ambiguity in the §1.1 produce bars and lineplot view.
  EVIDENCE: PDF p.2 §2.2: bars at '19,'20,'21,'23,'24 — nothing at '22; no visual distinction from zero. §1.1 caption (p.1): 'a county–crop the report does not list shows blank, not zero' — stated in text, absent from the encoding.
  REC (S): Encode missingness explicitly: at each missing year draw a light hatched placeholder band (or ∅ tick at the axis) with tip 'not reported by KNBS'; in lineplot views break the line (already done for prices via z:'seg' — reuse that pattern) and mark gaps. One shared `missingMark(years)` helper applied to every KNBS-fed chart.

[MAJOR] §6.2 — Outlook choropleth: false certainty plus a red/green colourblind trap
  PROBLEM: Every county is painted a solid modal-tercile colour from only n=8 analogue years — a county where 4/8 analogues were wetter looks exactly as WETTER-certain as one where 8/8 were. The PDF shows a near-uniformly solid-green Kenya, overstating a probabilistic analogue as a deterministic verdict. The Drier=#d73027 / Wetter=#1a9850 pairing is the classic deuteranopia failure AND reuses El Niño's exact red for a rainfall outcome, colliding with the phase language.
  EVIDENCE: L874 `range: ["#d73027", "#e0e0e0", "#1a9850"]`, L879 flat `fill: label[d.properties.modal]`; PDF p.8: all-green map; card p.9: '6 of 8 analogue years were wetter'.
  REC (M): Recolour to BrBG (brown drier / grey near-normal / teal wetter) and encode confidence as two lightness steps per hue: 'leaning' (modal share ≤5/8) pale, 'likely' (≥6/8) saturated — a 2×3 legend grid; hatch counties with no 1991–2020 baseline. Mirror the split in the verdict card as an 8-pip row (6 teal / 1 brown / 1 grey) instead of prose-only '6 of 8'. Hover tooltip already carries share + n — keep it.

[MAJOR] §6.1 — Three unlike-scaled series on one axis; Western-V reads as perpetually extreme
  PROBLEM: §6.1 overlays Niño 3.4 (°C), DMI (index) and WNP (z) on a single y-axis labelled only 'index / z'. The WNP line rides between ~1.5 and 3.0 for the entire 2015–2026 window while the others oscillate around 0 — visually declaring the Western-V permanently 3σ extreme, which is a baseline/units artifact, not a claim the notebook intends. Line colours are Plot defaults, contradicting the phase palette (see colour finding).
  EVIDENCE: L826–833: single `y: {label: "index / z"}` with three stroke series; PDF p.8: red WNP line hovers 1.5–3.0 across the whole decade while IOD/Niño straddle 0.
  REC (S): Facet into three stacked small multiples sharing the x-axis, each with its own y-scale, unit label, and a shaded ±1σ (or phase-threshold) band, with phase-coloured dot/ribbon markers where each index crosses its event threshold. If a shared axis must stay, standardise all three to z vs 1991–2020 and say so on the axis — never mix °C, raw index and z unlabelled.

[MAJOR] §2.1 — GESI strips overplotted at 220px; indicator labels truncated mid-phrase
  PROBLEM: Each distribution strip crams 47 dots (r=2.4 → ~4.4px spacing on a 208px inner width) plus four quartile bands, a median tick, a Kenya diamond and the county dot into 220×17px — in the PDF the strips collapse into near-solid dark bars where only the blue county dot survives. Worse, indicator labels are cut mid-phrase: 'Proportion of population that used improved', 'Proportion of households by primary reliance on', 'Proportion of households that report lacking food or' — unreadable and credibility-damaging in a statistics product.
  EVIDENCE: L313 `W = 220, H = 17`, L324 `r="2.4"`; PDF p.2: strips render as dark bands; truncated label strings visible in table rows p.2.
  REC (M): Fix the label source first (full text, wrapped to two lines; the truncation appears to be in the served label strings, not CSS). Redesign the strip: widen to ~320px, drop the quartile bands, draw a thin grey min–max range line with 25/50/75 ticks, keep only Kenya diamond + county dot (halo'd), and jitter the 47 dots vertically ±4px (beeswarm-lite) so density is readable. Row height 22px.

[MAJOR] §4.1 — IPC drawn as a continuous line invents fractional phases and ignores the IPC standard palette
  PROBLEM: IPC phase is an ordinal classification (1–5), but Plot.lineY linearly interpolates between observations, drawing slopes through impossible values like 'phase 2.7' (visible diagonal transitions, PDF p.4). The single #c0392b red line also wastes the one dataset with a globally standardised colour scheme (IPC greens→yellows→reds) that Kenyan food-security readers already know, and the phase-3 dashed rule is unlabelled.
  EVIDENCE: L478 `Plot.lineY(ipcCty, …stroke: "#c0392b")`, L479 unlabelled `Plot.ruleY([3])`; PDF p.4: sloped transitions between phases 2, 3 and 4.
  REC (S): Switch to `curve: "step-after"` (classification holds until reclassified), or better: a horizon-style band strip filled with the official IPC phase colours (1 #CDFACD…5 #640000) with the line on top; label the rule 'Phase 3 — Crisis'. Y-axis integer ticks 1–5 only, labelled with phase names.

[MAJOR] global — Axis craft defects across at least seven charts
  PROBLEM: Years render with thousands separators — '1,985 1,990…' (§5.1), '2,002 2,004…' (§5.2), '1,970 1,980…' (§7.4); §3.2's y-axis is literally labelled 'v' and §7.4's is 't' (variable names leaked into print); §1.1 x-ticks read '0k 0.2k 0.4k … 1k tonnes'; §4.2 prints all 30 year labels 1997–2026 in a colliding strip; §2.2 has 11 y-ticks at 100-tonne intervals; §4.6 import bars show 8-digit '50,000,000' kg ticks. Individually small, collectively they read as an unfinished product — fatal for the technical audience's trust.
  EVIDENCE: PDF p.7 '1,985…'; p.7 §5.2 '2,002…'; p.11 '1,970…' and '↑ t'; p.3 §3.2 '↑ v' (L428–442 defines y:"v" with no label); p.1 '0k 0.2k…1k tonnes (2024)'; p.4 §4.2 30 crowded ticks; p.7 '50,000,000'.
  REC (S): One sweep: `x: {tickFormat: "d"}` (or the existing "'85" formatter) on every numeric-year axis; real y labels ('Niño 3.4 seasonal mean (°C)', 'production (t)'); §1.1 plain '0 200 400 … 1,000 t'; §4.2 ticks every 5 years; §2.2 ~5 ticks; imports in kt ('50k t') with a converted axis. ~15 lines of changes total.

[MAJOR] §4.6 — Trade arrow map: unlegended widths, 15-year aggregate, ~60% dead space
  PROBLEM: Arrow width encodes 'total traded quantity over the record' (2010–2024 summed — mixing drought and normal years into one static flow, erasing exactly the ENSO-relevant variation) with NO width legend, so magnitude is unreadable; centroid-to-centroid bezier arrows imply routes that don't exist (caption admits it); and the Mercator frame spans Zambia to Madagascar, shrinking Kenya to ~15% of the plot while half the map is empty ocean and irrelevant countries.
  EVIDENCE: L703–714: `projection: {type:"mercator", domain…}`, `strokeWidth` from links with no legend mark; caption p.6: 'Arrow width = total traded quantity over the record… drawn between country centres, not exact border crossings'; PDF p.6 frame includes Madagascar and Zambia.
  REC (M): Crop the projection domain to the EAC neighbourhood (KEN+ETH+UGA+TZA+SOM+SSD, inset 12); add a 3-step arrow-width legend in kt; either facet the map by El Niño/La Niña/neutral composite years (ties trade to the story) or keep one map as a locator and move magnitude to the (already present) per-year partner bars, which carry the time dimension the map destroys.

[MAJOR] §4.3 — Price axis contradicts its caption; nominal KES over 16 years
  PROBLEM: The y-axis is hard-labelled 'KES per kg' while the section caption says 'the axis shows KES per the unit the product is traded in' — for any non-kg product one of them is wrong on screen. And 2009–2026 nominal KES conflates Kenya's substantial inflation with scarcity: the 2022–23 'spike' the reader attributes to drought is partly just price level, weakening the notebook's causal storytelling.
  EVIDENCE: PDF p.4: axis '↑ KES per kg' vs caption 'the axis shows KES per the unit the product is traded in'; nominal series 2009–2026 with 20→90 KES drift.
  REC (M): Drive the axis label from the product's unit metadata (dynamic string, already have the product selector); add a caption sentence naming inflation as a confound now, and (pipeline permitting) a 'real (CPI-deflated)' toggle using KNBS CPI via the deterministic pipeline — never hand-typed. Consider a %-of-5-yr-median view which needs no external data.

[MAJOR] §1 — Missing VoP exposure chart removes the value denominator (port regression)
  PROBLEM: §1.1 shows tonnes of 31 crops — but tonnes of cowpeas vs camels are incommensurable, so the reader never sees what a bad season puts at stake in comparable terms. The previously-built Atlas value-of-production chart (MapSPAM + GLW4, constant I$) is a confirmed port regression; exposure_vop.parquet sits unused in the data dir. This is also the missing 'exposure vs ENSO' half of the owner's remit.
  EVIDENCE: Handover README: 'B1 now leads with the Atlas value-of-production chart… all named port gaps closed' — absent from current notebook.qmd (no exposure_vop reference); PDF §1.1 shows only KNBS tonnes.
  REC (S): Restore the VoP chart in §1 (horizontal bar, crops+livestock in constant I$, Atlas yellowGreen family) and feed its total into the county-at-a-glance card's 'value at stake' tile. Effort is small — the chart existed before and the parquet is served.

[MINOR] §5.1–5.2 — County-compare spaghetti reuses driver hues with no reference band or direct labels
  PROBLEM: Marsabit/Nakuru/Turkana get default blue/orange/red — the exact hues §6.1 uses for the three ocean drivers two pages later. Legend-to-line matching over 45 wiggly years is slow; no 1991–2020 normal band gives the lines context; comma-years (covered above) compound it.
  EVIDENCE: L778–783/L799–804: `color: {legend: true}` defaults; PDF p.7 legends 'Marsabit • Nakuru • Turkana' in blue/orange/red.
  REC (S): Direct end-of-line labels (Plot.text at last point, halo'd) instead of a legend; a neutral categorical ramp reserved for 'entities' (e.g., d3.schemeObservable10 minus red/blue) distinct from phase colours; light grey band for the selected-season 1991–2020 IQR across counties.

[MINOR] §4.5 — NDVI chart is the best encoding in the notebook — but the line is 0.4px and named droughts are unannotated
  PROBLEM: The two-tone above/below-normal difference area is exactly right. But the 0.4px black line disappears in print, and the caption names 2011, 2017, 2021–22 as the majors while the chart makes the reader find them.
  EVIDENCE: L654 `strokeWidth: 0.4`; caption p.5: 'Watch the deep, sustained deficits — 2011, 2017 and 2021–22'; PDF p.6 chart has no year annotations.
  REC (S): strokeWidth 0.8–1; add three slim annotation flags (Plot.text + ruleX) at 2011, 2017, 2021–22 labelled from nbText; align the orange/green to the Atlas orangeRed/yellowGreen tokens.

[MINOR] §6.2 — Controls panel occludes/splits the outlook map in print/PDF
  PROBLEM: In the PDF the county/season/driver control box sits ON the choropleth, splitting Kenya in two — southern counties render below the panel. Whatever the sticky/floating layout does on screen, the print/PDF artifact is a broken map, and the PDF is a primary sharing format for this audience.
  EVIDENCE: PDF p.8: map interrupted mid-figure by the COUNTY/CLIMATE SEASON/OCEAN DRIVER panel, with a green map fragment continuing beneath it.
  REC (S): Place the control row statically ABOVE the figure in normal flow; if a floating panel is kept on screen, add `@media print {position: static}` so exports never overlap the map.

[MINOR] §7.1 — Scatter colour is redundant with its own x-axis
  PROBLEM: §7.1 colours dots by ENSO phase — but ENSO anomaly IS the x-axis, so colour re-states position and the second variable of interest (IOD event state) gets no channel. Harmless, but it spends the panel's one free encoding on nothing.
  EVIDENCE: L951–955: `fill: "phase"` on x="enso"; heading p.9 'ENSO vs IOD, OND season — r = 0.59, n = 75'.
  REC (S): Colour (or shape) dots by IOD event sign instead — coupling then shows in two channels at once (position correlation + colour sorting into quadrants), which is the section's actual message. Keep the live r/n annotation; regression band is good.

--- VIZ/DATA RECOMMENDATIONS ---
* Unified palette token module [global (appendix helper cell)]
  purpose: Give both audiences one learnable colour language separating DRIVER PHASE from RAINFALL OUTCOME from IMPACT — fixes the notebook's worst global defect.
  spec: One OJS cell `PALETTE = {phase: {elnino:'#d73027', lanina:'#4575b4', neutral:'#bbbbbb', iodPos:'#7b3294-family or red-outline', wnpHigh:'#4d4d8f'}, outcome: {drier:'#a6611a', near:'#e0e0e0', wetter:'#018571'} /* BrBG */, impact: {good:'yellowGreen', bad:'orangeRed' /* Atlas tokens */}}` + a documented rule in Methods: phase colours appear only on chips/ribbons/dots denoting phase; outcome marks always BrBG; every Plot color:{} references PALETTE. Recolour §3.1, §6.1, §6.2, §7.2 accordingly.
  data: None — code-only refactor.
* §3.1 'Season outcome' redesign (anomaly bars + phase ribbons) [§3.1]
  purpose: Make the driver→rainfall relationship visible without mental subtraction — the notebook's core remit, for both audiences.
  spec: Default = anomaly vs 1991–2020: Plot.barY diverging around 0, bars FILLED by outcome sign (BrBG), zero rule emphasized. Below the x-axis two 10px categorical ribbon lanes (Plot.cell): ENSO phase and IOD phase for OND (WNP lane for MAM), in PALETTE.phase colours with lane labels at left — replaces both the phase bar-fill and the illegible 9px ▲/▼ glyphs. Above the chart three summary chips: mean anomaly ± n per phase, computed in OJS from the served rows (deterministic). Absolute-mm stays as toggle. Height ~420px incl. lanes, width 900.
  data: chirps_county.parquet + driver phase columns — all already served locally.
* Phase-grouped CHIRPS seasonal-anomaly map small multiples (national) [new §3.3 (story spine)]
  purpose: The single artifact that proves the analogue logic visually for non-technical readers ('all El Niño short-rains look like THIS') and doubles as the owner's requested timeseries maps at national scale.
  spec: Faceted choropleth grid of Kenya (GAUL24 a1, white 0.4px strokes), one facet per year 1981–2025, county fill = seasonal rainfall as % of that county's 1991–2020 normal. Diverging BrBG, pivot 100%, domain CLAMPED symmetric at [40%, 160%] (equivalently ±60pp) and FIXED across all facets and both seasons — one shared horizontal legend at bottom ('seasonal rainfall vs 1991–2020 normal'); counties missing a baseline hatched. Default layout: rows GROUPED BY PHASE (El Niño row(s), Neutral, La Niña — within-row sorted by driver magnitude), facet header = 'YY + phase dot in PALETTE.phase; toggle to chronological 9×5. Each Kenya cell ~100×130px → grid ≤1000×780 (within Atlas limits). Season toggle reuses the existing OND/MAM lens; hover = county, mm, % of normal; click a facet → sets focus year for the county-zoom variant; selected county outlined #111 in every facet. Download button per Atlas convention.
  data: EXISTS locally: chirps_county.parquet (county×season×year PTOT + climatology) + data/shared GAUL24 a1 topojson. No pipeline run needed.
* County-zoom seasonal timeseries maps with subcounty overlay + station dots (the owner's explicit ask) [new §3.4 or §2]
  purpose: Show within-county rainfall structure over time for the selected county — the owner's literal request ('timeseries maps of county with sub counties overlain for CHIRPS v3 seasonal rainfall… and weather station data').
  spec: Selected county zoomed (fitExtent + 8% inset), GAUL24 a2 subcounty polygons filled by subcounty seasonal anomaly on the SAME anchored BrBG scale as the national grid; neighbouring counties ghosted #f0f0ec for context. Facet by the last 12 seasons (4×3, ~240px cells) with a year slider/brush for the full record. Overlay NOAA GHCN-Daily/ISD stations as circles (white halo, fill = station season-total anomaly on the same scale, label = station name + mm); where the county has no open station, print 'no open station in this county' honestly — the sparse network is framed as CHIRPS point-validation, not coverage. Subcounty strokes white 0.5px at zoom scale only (at national-grid scale a2 lines are noise — keep a1 there).
  data: a2 topojson EXISTS (data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson). Subcounty rainfall NEEDS PIPELINE: either D409 CHIRPS v3 zonal rerun at admin2 (preferred, consistent with a1 series) or WFP VAM HDX dekadal rain on OCHA admin2 rolled to season (machine-readable stopgap). Station data NEEDS a small deterministic fetch: NOAA GHCN-Daily (~10 counties) + ISD (~28 stations), open and keyless.
* County-at-a-glance header card + restored click-picker locator [top of notebook (after hero), pinned]
  purpose: Orient the non-technical reader in one glance the moment a county is chosen; fixes the no-locator failure and the buried dropdown.
  spec: Full-width band, five tiles ≤130px tall: (1) mini locator map — Kenya a1 outline, selected county filled Atlas green; clicking opens the restored full choropleth click-to-select picker (port regression); (2) 'Driver now' — ENSO phase chip (PALETTE.phase) + ONI value + IOD value, from the served current-state data; (3) 'Outlook' — LIKELY WETTER/DRIER in outcome colour + an 8-pip analogue row (e.g., 6 teal/1 brown/1 grey) replacing prose-only '6 of 8'; (4) 'Last season' — e.g., 'OND 2025: +38% of normal' chip in BrBG; (5) 'Value at stake' — total VoP in constant I$ + top-3 commodities micro-bar from exposure_vop.parquet. All values computed in OJS from served parquets (no LLM-typed numerics); EN/FR via nbText; card itself downloadable.
  data: EXISTS locally: current-state indices, analogue outlook data, chirps_county.parquet, exposure_vop.parquet (currently unused). Picker code previously existed — port back.
* Outlook map v2 with certainty encoding [§6.2]
  purpose: Stop the n=8 analogue read as deterministic certainty; make §6.2 colourblind-safe and consistent with the outcome palette.
  spec: Same geography; hue = modal tercile in BrBG (brown/grey/teal); lightness = confidence in two steps: 'leaning' (modal share ≤5/8) pale, 'likely' (≥6/8) saturated → 2×3 legend grid labelled in plain words; dot-hatch counties with no baseline or n<6; selected county outlined #111; controls in static flow above the figure (print-safe). Verdict card gains the matching 8-pip row and keeps analogue-year chips.
  data: EXISTS: current outlook analogue table already carries counts/share per county (hover uses it).
* County production × phase event strip (2019–2024) [§2 (replacing the stray AFA maize bars)]
  purpose: Deliver 'county production vs ENSO' honestly despite the 6-year series — event-anchored storytelling across the 2020–23 triple-dip La Niña and 2023 El Niño, with zero fake statistics.
  spec: For the selected county: rows = top-5 crops by county tonnage; columns = 2019–2024; cell = dot sized by tonnage and filled by % departure from that crop's own served-years median (BrBG); missing county-years = hatched ∅ 'not reported' (blank≠zero made visible). Beneath: OND+MAM phase ribbon lanes and season-anomaly chips for the same years; two annotation flags '2020–23 triple-dip La Niña' and '2023 El Niño recovery'. Caption states explicitly: series too short for correlation — this is an event alignment, not a statistic.
  data: EXISTS locally: KNBS NAPR county parquets (2019–2024, 31 crops), chirps_county.parquet, driver phases.
* Restore Atlas VoP exposure chart [§1]
  purpose: Give §1 a comparable 'what is at stake' denominator across crops and livestock for both audiences.
  spec: Horizontal Plot.barX of constant-I$ VoP, crops + livestock interleaved and tagged, Atlas yellowGreen family, % -of-county-total axis option; feeds tile (5) of the glance card. Reinstate from the pre-regression implementation.
  data: EXISTS locally: exposure_vop.parquet (MapSPAM + GLW4), currently unused.


====================================================================================================
PERSONA: Agricultural economist / food-security analyst (FEWS NET / WFP VAM school)
====================================================================================================
VERDICT: The owner's claim is confirmed: nowhere in the main story is county production visually or analytically joined to a driver signal. §1.1 is a production inventory with zero climate context, §2.2's maize bars silently span the triple-dip La Niña collapse and 2023 El Niño recovery without a single annotation, and the only production-vs-driver figure in the whole notebook is a national, non-significant FAOSTAT scatter exiled to §7 with a caveat admitting the county story "awaits a longer series". Meanwhile §4 ("How Does A Bad Season Reach People?") never marks which seasons were bad — IPC, prices, NDVI and conflict all lack driver-phase context, and prices are raw nominal KES over 26 years. The ingredients for an honest fix are already on disk: the ASAP crop calendar, the VoP exposure layer, goat prices for pastoral terms of trade, and 24 years of NDVI for legitimate phase composites. The single most important change is a phase-annotated production-and-impacts spine: driver-phase shading applied across the county production timeline and every §4 impact chart, anchored by the crop calendar — event-anchored storytelling, not fake statistics.

STRUCTURE PROPOSAL: Adopt the owner's tabbed dual-track, organised around one linear county story plus a technical annex. Main track (simple story): 1. Where things stand now (driver state + outlook pointer, lean) → 2. My county (VoP stakes → produce multi-view with phase-shaded timeline → curated GESI vulnerability card) → 3. What drives my county's seasons (rainfall-by-phase + crop calendar + NDVI phase composite) → 4. How a bad season reaches people (all panels phase-ribboned; add goat:maize ToT) → 5. Outlook & watchlist (analogue map, calendar-aligned county card, watchlist table). Move to annex tabs: county comparison (§5), driver-interaction analysis (§7.1–7.2), the national FAOSTAT regression (§7.3–7.4, keeping its non-significance labelling verbatim), the full GESI table, the rainfall-percentile-vs-production dot plot, and Methods. The key move is merging §1.1 into the county profile so stakes, production and vulnerability read as one answer to 'what does a bad season put at risk here'.

[CRITICAL] §1.1, §2.2, §7.3 — Production is never joined to driver phase — owner's claim confirmed
  PROBLEM: County production exists in three places and none has climate context. §1.1 defaults to a single-year bar chart (2024 tonnage: maize, dry beans, sorghum...) with no phase colouring, no rainfall, no anomaly. §2.2 shows Marsabit maize collapsing in 2021–22 and rebounding in 2023–24 as plain monochrome bars — the triple-dip La Niña and El Niño recovery are IN the chart and never named. The only production-vs-driver figure is §7.3's national FAOSTAT scatter (r=0.20, n=64, not significant), explicitly banished 'for analysts'. Rainfall gets phase colouring in §3; production never does. For a food-security reader the notebook's central promise is simply not delivered.
  EVIDENCE: PDF p.1: §1.1 bars show only '(2024)' tonnage, no climate encodings. PDF p.2: §2.2 'Marsabit — dry maize production (AFA, 2020–24)' — uniform blue bars, trough at '21–'22, peak at '24, no annotation. PDF p.11: §7.3 'r = 0.20, n = 64, not significant (p>0.05)' with quick-insight text 'a longer county production series is needed to show it properly.'
  REC (M): Build the phase-annotated county production timeline as the centerpiece of the county profile: NAPR/AFA bars 2019–24 per crop with background season ribbons coloured by the governing driver phase (ENSO/IOD for OND, Western-V for MAM), the season window chosen per county from seasonal_calendar.parquet, plus named event annotations ('2020–23 triple-dip La Niña', '2023 El Niño recovery'). n=6 years forbids correlation claims — so make it event-anchored narrative, which the constraint explicitly allows. Keep missing county-years visibly blank (blank ≠ zero).

[CRITICAL] §4 — 'How Does A Bad Season Reach People?' never shows which seasons were bad
  PROBLEM: Every impact series — IPC phase (§4.1), ACLED fatalities (§4.2), market prices (§4.3), ReliefWeb reports (§4.4), NDVI (§4.5), imports (§4.6) — is drawn on a bare time axis with no driver-phase context. The reader must mentally carry §3's phase colouring across four pages. The §4.5 caption even instructs 'Watch the deep, sustained deficits — 2011, 2017 and 2021–22 are Kenya's major droughts' — asking the reader to do the join the chart should do.
  EVIDENCE: PDF pp.4–7: §4.1 IPC step-line, §4.2 conflict bars, §4.3 KES price lines, §4.5 NDVI area — all with plain white backgrounds; §4.5 intro text quoted above explicitly delegates the phase join to the reader's memory.
  REC (S): Build one shared OJS annotation helper that draws OND/MAM driver-phase ribbons (El Niño red / La Niña blue, matching §3.1's legend) behind any time-axis chart, and apply it to all six §4 panels. This one small helper converts §4 from a set of disconnected monitors into the causal chain the section title promises. Data is already loaded (enso_drivers_seasonal.parquet).

[CRITICAL] §2 (county profile) — Exposure ('what is at stake') is absent — exposure_vop.parquet unused
  PROBLEM: A county food-security officer's first question is what the county's economy rides on — crop or livestock — because that determines the whole risk pathway (rainfed harvest vs pasture/ToT). The Atlas VoP layer (MapSPAM+GLW4, constant I$, 47 counties × 34 commodities) sits on disk unused, and nbText.json still carries its complete text (b1.vopTitle 'what agriculture is worth here — value of production by commodity', vopIntro, vopCaption) — this is a port regression, not a design choice. Without it, Marsabit's profile opens with a GESI table and a maize chart, when Marsabit's VoP is overwhelmingly cattle+goats+sheep.
  EVIDENCE: grep of notebook.qmd: zero references to exposure_vop or vopTitle; exposure_vop.meta.json 'used_by: notebook.qmd Block 1 (value-of-production bar + livestock-share insight)' describes a chart that no longer exists; nbText.json b1 still has vopTitle/vopIntro/vopCaption in en+fr.
  REC (S): Restore the VoP bar chart at the top of the county profile with a livestock-share callout, and use livestock share to route the narrative: high-livestock counties foreground the NDVI/terms-of-trade pathway, cropping counties foreground the production-vs-season-window pathway. Zero new data or text needed — reconnect exposure_vop.parquet to the existing nbText keys.

[MAJOR] §4.3 — Raw nominal KES prices cannot support the section's own claim
  PROBLEM: The §4.3 intro asserts 'Price spikes often follow failed seasons' but the chart shows 26 years of nominal KES per kg (Marsabit maize ~20 KES in 2009 → ~90 KES peak in 2023), where inflation, currency drift and scarcity are indistinguishable. A FEWS/VAM analyst cannot read food-access stress off this chart; a non-technical reader will misread trend as crisis.
  EVIDENCE: PDF p.4: §4.3 y-axis 'KES per kg' 0–90 over 2010–2026, steadily rising; intro text quoted; market_prices.meta.json confirms value_kes nominal monthly retail, 2000–2026.
  REC (M): Add a deterministic anomaly toggle: (a) 12-month % change and (b) z-score vs that market's month-of-year climatology (both computable in the OJS/DuckDB layer, no LLM numbers), with driver-phase ribbons behind. Keep raw KES as the default-off expert view. Then the 2022–23 El Niño-drought price surge becomes legible as an anomaly, not just a rising line.

[MAJOR] §4 (new panel) — The pastoral pathway is missing — goat:maize terms of trade is computable today
  PROBLEM: The default county is Marsabit, a pastoralist county, yet §4 offers no pastoral livelihood indicator. The classic FEWS/WFP measure — kg of maize one goat buys — collapses in droughts (goat prices fall as herders destock, cereal prices spike) and is the single best 'bad season reaching people' signal in the ASALs. I verified the data is already served: market_prices.parquet contains 'Goats (Local Quality)' (4,864 rows, 20 counties, 2000–2026) alongside maize in 42 counties.
  EVIDENCE: DuckDB query on market_prices.parquet: 'Goats (Local Quality) n=4864, 2000–2026, 20 counties'; 'Cattle (Male, 2-3 years old...) 3 counties' (goats are the workhorse). Notebook grep: zero references to goats or terms of trade.
  REC (M): Add a goat:maize terms-of-trade panel for the ~20 counties with goat series (honest empty state elsewhere, matching the existing pricesNoData pattern): monthly ToT line with driver-phase ribbons and the NDVI deficit aligned above — the full pasture → livestock value → food access chain. Deterministic ratio of two observed series; no new pipeline.

[MAJOR] §3 / §6 — Crop calendar port regression — season windows float free of the farm year
  PROBLEM: seasonal_calendar.parquet (JRC ASAP, 38/47 counties, planting/harvest month windows by season) is unused, and nbText.json b2 still holds its text ('When does my county plant and harvest?'). Without it, the OND/MAM season lens is an abstraction: nothing tells the reader that a forecast El Niño OND lands on the short-rains planting window, and nothing anchors which season's rainfall should be paired with which production year — the exact join finding 1 needs.
  EVIDENCE: grep notebook.qmd: zero hits for seasonal_calendar; seasonal_calendar.meta.json 'used_by: notebook.qmd Block 2 (per-county planting/harvest strip...)' describes a removed feature; nbText b2.calendarHeading/calendarIntro exist unreferenced.
  REC (M): Restore the per-county planting/harvest strip under §3, and reuse the same windows twice: (a) to attribute each NAPR production year to its growing season(s) for the phase-shaded timeline, and (b) in §6.2 to state outlook–calendar alignment ('the forecast phase covers your Oct planting window'). Keep the ASAL gap honest as the meta already frames it: 9 counties have no rainfed crop season — for them the rains and NDVI are the calendar.

[MAJOR] §4.5 / new — NDVI is shown as a monitor when it could carry the county-level driver statistic
  PROBLEM: The county crop series (2019–24) is too short for statistics, but NDVI 2002–present spans ~5 El Niño and ~8 La Niña OND events — enough for legitimate per-county phase composites. §4.5 currently shows only the raw dekadal trace, leaving the notebook's strongest defensible county-level climate-impact quantification unbuilt while §7 apologises that the national regression is too coarse.
  EVIDENCE: PDF p.5–6: §4.5 single raw trace 2002–2026; ndvi_county.parquet min(year)=2002 verified; brief-confirmed event count (5 El Niño / 8 La Niña OND events since 2002).
  REC (M): Add an NDVI-by-phase composite: per county, median dekadal NDVI %-of-normal trajectory by ENSO phase with IQR band (or seasonal-mean anomaly dots grouped by phase, each dot a labelled year). This is production-proxy vs driver at county grain with honest n — the analytical join the owner asked for, delivered where the data can actually support it. Ensemble the composite from per-event traces (statistic per event first, then aggregate).

[MAJOR] §2.1 — GESI is a context-free 14-row dump in prime position, with truncated labels
  PROBLEM: The county profile opens with all GESI indicators before any climate or stakes content, unframed for the notebook's question. Several indicator labels render cut off — 'Proportion of population that used improved', 'Proportion of households by primary reliance on' — which reads as a data defect and erodes trust. Ironically the one indicator that belongs in a food-security story ('Proportion of households that report lacking food' — Marsabit 58.1 vs Kenya 28.8, rank 2/45) is buried mid-table.
  EVIDENCE: PDF p.2: §2.1 table rows quoted above, including the two truncated labels and 'Proportion of households that report lacking food or' (also clipped) showing 58.1 / 28.8 / rank 2/45.
  REC (M): Curate 4–6 vulnerability-to-climate indicators (food-lacking %, disability, teenage pregnancy, birth registration) into a compact 'who a bad season puts at risk' card placed AFTER the VoP stakes chart, with one sentence tying each to shock vulnerability; move the full table to the technical annex. Fix the truncated indicator strings at the source (they look like column-width clipping in extraction, e.g. '...used improved [water source]').

[MAJOR] §6.2 — Outlook stops at 'LIKELY WETTER' — no decision product for a food-security officer
  PROBLEM: The analogue card says 'LIKELY WETTER than normal — 6 of 8 analogue years were wetter' and ends. There is no consequence line (what wetter OND means for this county's planting window, pasture, or flood risk in a livestock economy), no cross-county watchlist, and no link to current conditions (latest NDVI deficit, current IPC phase). An officer must leave the notebook to answer 'so what, and where first?'.
  EVIDENCE: PDF p.9: card text 'LIKELY WETTER than normal / 6 of 8 analogue years were wetter' with analogue-year chips; nothing below it but the About expander and the §7 heading.
  REC (L): Two additions, both within the KMD/NOAA-CPC constraint: (a) extend the county card with calendar alignment and current-state lines ('forecast phase covers Oct–Dec short-rains planting; current NDVI 82% of normal; IPC Phase 3'); (b) add a national watchlist table ranking counties by analogue direction × latest NDVI deficit × current IPC phase × livestock VoP share — every input is an existing parquet plus the already-approved CPC probabilities. Optionally surface live KMD CAP alerts (meteo.go.ke/api/cap/rss.xml — an approved KMD source) as a current-hazards line.

[MINOR] §1 / §2 structure — Production inventory (§1.1) is separated from the county profile (§2)
  PROBLEM: The §2 intro even apologises for it: '(What the county grows is up top, under "What does my county produce?")'. Stakes (VoP), production, and vulnerability belong together as one county profile; splitting them forces back-scrolling and is why §2.2's maize chart feels 'stray'.
  EVIDENCE: nbText.json b1.intro quoted parenthetical; PDF pp.1–2 show produce views in §1.1 and the county header only arriving in §2.
  REC (M): Merge §1.1 into the county profile: order as VoP stakes → produce multi-view (default to the trend view, ranked by VoP) → curated GESI card. §1 then becomes a lean 'where things stand now' status strip (driver state + outlook pointer).

[MINOR] §2.2 — Maize chart label inconsistency: title says 'AFA, 2020–24', bars show 2019–24, caption credits KNBS NAPR
  PROBLEM: The heading reads 'dry maize production (AFA, 2020–24)' while the x-axis runs '19–'24 and the caption says the source is the KNBS NAPR with AFA folded in (233/234 county-years matched). Three conflicting provenance/coverage signals on one small chart undermine the notebook's otherwise excellent provenance discipline.
  EVIDENCE: PDF p.2 heading vs axis ticks '19…'24; nbText b1.maizeCaption: 'County maize production over time (KNBS NAPR). The Agriculture & Food Authority (AFA) reports the same figures (folded into the NAPR — 233/234 county-years matched exactly)'.
  REC (S): Correct the title to the caption's truth ('maize production, KNBS NAPR + AFA, 2019–24') — or better, retire this chart into the phase-shaded production timeline of finding 1, which supersedes it.

[MINOR] §4.1 — IPC panel underuses the data: worst-area line only, current-situation only
  PROBLEM: The chart plots max_phase (worst area) as a step line, which for Marsabit sits at 3 for most of a decade — visually flat and alarming at once, hiding within-county variation. The parquet also carries mean_phase, scenario_name and n_units, none surfaced.
  EVIDENCE: PDF p.4: §4.1 single red step line titled '(worst area, Current Situation)'; ipc_county.parquet schema verified: max_phase, mean_phase, scenario_name, n_units.
  REC (S): Show max_phase and mean_phase together (band between them = within-county spread), keep 'Current Situation' as the default scenario with a toggle, and apply the finding-2 phase ribbons so the 2017 and 2021–23 IPC deteriorations line up with their driver seasons.

--- VIZ/DATA RECOMMENDATIONS ---
* Phase-shaded county production timeline [§2 county profile (replaces §2.2)]
  purpose: The missing centerpiece: shows a county's harvests against the driver seasons that shaped them — the owner's core remit — for both audiences via event-anchored annotation instead of illegitimate n=6 statistics.
  spec: Grouped/faceted bars of production_t per crop per year (2019–24) with vertical background ribbons per growing season coloured by driver phase (ENSO/IOD for OND, Western-V for MAM; season window from the ASAP calendar, county-specific); named annotations for the triple-dip La Niña and 2023 El Niño; missing county-years rendered as hatched 'not reported' (blank ≠ zero); % -of-county-median toggle. Lives at the top of the merged county profile.
  data: knbs_napr_county_production.parquet + afa_rice.parquet + enso_drivers_seasonal.parquet + seasonal_calendar.parquet — all local, no pipeline work
* Driver-phase ribbon annotation helper (applied to all §4 panels) [§4 (all), also §5 compare lines]
  purpose: Makes 'How does a bad season reach people' literally visible: every impact series (IPC, conflict, prices, NDVI, imports) gains the same OND/MAM phase shading so 2011/2017/2021–23 read as one causal chain for the non-technical reader.
  spec: One reusable OJS function returning Plot.rect marks (y-free, x = season date ranges, fill = phase colours matching §3.1 legend, low opacity), composed into each existing chart's marks array; legend rendered once per section.
  data: enso_drivers_seasonal.parquet — already loaded
* NDVI-by-phase county composite [new §3.x or upgrade of §4.5]
  purpose: The strongest statistically legitimate county-level driver-impact quantification available (5 El Niño / 8 La Niña OND events since 2002) — answers the technical user's 'show me the signal at my county' that §7's national regression cannot.
  spec: Per county: median dekadal NDVI %-of-normal trajectory across the OND (and MAM/Western-V) season by phase, with IQR band per phase, computed per event first then aggregated (ensembling last); alternate view = seasonal-mean anomaly beeswarm by phase, each dot a labelled year. Height ≤500px, download button.
  data: ndvi_county.parquet + enso_drivers_seasonal.parquet — all local
* Goat:maize terms-of-trade panel [§4 new panel after §4.3]
  purpose: The FEWS/WFP pastoral food-access indicator for the ASAL counties (including default county Marsabit) — how many kg of maize one goat buys, collapsing in droughts; connects NDVI deficit to household purchasing power.
  spec: Monthly ToT line (goat price ÷ maize price per kg, per market or county-median), phase ribbons behind, latest-value callout vs long-term median; small NDVI-deficit strip aligned above; rendered only for the ~20 counties with a goat series, honest pricesNoData-style empty state elsewhere.
  data: market_prices.parquet ('Goats (Local Quality)' 4,864 rows / 20 counties / 2000–2026 verified) — local, deterministic ratio only
* Restored VoP stakes chart with livestock-share routing [top of §2 county profile]
  purpose: Answers 'what is at stake here' first, and tells both audiences which pathway matters for this county (crop vs pastoral).
  spec: Horizontal bars of vop_intld15 by commodity, hue by kind (Crop/Livestock), livestock-share % callout headline; the share value also drives conditional narrative text steering pastoral counties to the NDVI/ToT panels.
  data: exposure_vop.parquet — local and already documented; nbText b1.vop* keys exist in en+fr (pure port restoration)
* Crop-calendar strip + outlook alignment line [§3 (strip) + §6.2 (alignment)]
  purpose: Anchors OND/MAM to the farm year for the simple story, and gives §6 its decision hook: does the forecast phase cover my planting window?
  spec: Gantt-style strip per county: rows = crop × season, segments = planting (light) / harvest (dark) months; in §6.2 the county card gains one generated sentence combining CPC forecast phase with the county's next planting window; ASAL counties show the honest 'no rainfed crop season — pasture follows the rains' framing from the meta.
  data: seasonal_calendar.parquet (38/47 counties) + enso_state_probabilities.parquet — all local
* Price anomaly toggle for §4.3 [§4.3]
  purpose: Separates scarcity from inflation so 'price spikes follow failed seasons' becomes demonstrable rather than asserted.
  spec: Radio toggle: raw KES / 12-month % change / z-score vs market month-of-year climatology (computed in DuckDB, deterministic); phase ribbons; gap-segmented lines retained; dots on observations as now.
  data: market_prices.parquet — local
* County watchlist table (outlook × conditions × stakes) [§6 after the outlook map]
  purpose: The decision product a county/national food-security officer actually uses: where to look first this season.
  spec: Sortable table of 47 counties: analogue-outlook tercile (existing §6.2 logic), latest NDVI % of normal, current IPC max_phase, livestock VoP share, ToT 6-month trend where available; small inline bars per column; selected county pinned; paired mini-choropleth (GAUL24 topojson) coloured by a simple rank sum.
  data: all local (enso_outlook_base, ndvi_county, ipc_county, exposure_vop, market_prices) + approved NOAA CPC probabilities; no forecast-source conflict
* Season-window rainfall percentile vs production anomaly (per-year dot plot) [annex (near §7)]
  purpose: For the technical user: the honest county production–climate join — pairs each 2019–24 harvest with its growing-season rainfall percentile without fitting anything.
  spec: Per county × crop: 6 labelled year-dots, x = CHIRPS season-window rainfall percentile vs 1991–2020, y = production % of county 2019–24 median; quadrant guides at 33/67th percentile and 100%; explicitly captioned 'six seasons — a pattern check, not a statistic'. Lives in the technical annex.
  data: chirps_county.parquet + knbs_napr_county_production.parquet + seasonal_calendar.parquet — all local


====================================================================================================
PERSONA: Atlas data engineer (D409 pipeline + AAA data ecosystem: DuckDB-WASM, parquet, S3 hub, GAUL boundaries)
====================================================================================================
VERDICT: From the data-engineering side, this notebook is in the unusual position of already possessing most of what its owner says is missing: county production, driver indices, exposure value-of-production, crop calendars, and 8 of 9 CHIRPS variables are all sitting in the served directory — 8 parquets (plus 85% of the largest file) are shipped and never queried, while the owner's core ask (production vs drivers, county-level) fails for want of a single join between two clients that are already loaded. The payload is simultaneously bloated and underused: 6.3 MB served, of which chirps_county's unqueried variables alone are ~3.9 MB dead weight; a slimmed v2 with every owner ask added (subcounty CHIRPS ~300 KB measured-by-simulation, stations ~0.5 MB, CAP snapshot ~10 KB, Kenya-only a2 topojson ~200 KB) lands around 3.5–4 MB — smaller than today. The remote facts are now verified: no adm2 product exists on the Atlas hub (404), so the subcounty ask requires the D409 zonal rerun (trivial output size), and the KMD CAP feed is live but CORS-blocked for browser fetch, so it must be pipeline-staged. The single change that matters most is the production-vs-driver county join (event-anchored, no fake statistics) — it costs no new data and converts the notebook from 'drivers next to production' into what the owner actually commissioned. Close behind, for a public v2, is retiring the D409-only provenance of the very driver file the whole analysis runs on, when its git-reproducible twin is already in the directory unused.

STRUCTURE PROPOSAL: Support the owner's tabbed instinct, but implement it as data-tiering rather than page-splitting: a Story tier (current §1–§6, slimmed) that touches only small pre-filtered parquets, and a Technical Annex tier (current §7 + §8 + a new served-data catalog) where every dataset gets a row auto-generated from its .meta.json (source, licence, method, coverage, download link) — the annex becomes the public face of DATA.md instead of the current private-path references. Keep the county as the persistent spine across tabs (one shared selection, choropleth picker at top), and gate heavy assets by tab so the subcounty/station/annex parquets only load when their tab is opened — this is what keeps the payload budget flat while the feature set grows. Section order within Story: Now-state → My county (profile + exposure VoP) → Rains & drivers (with calendar strip + subcounty map) → Impacts → Outlook (with KMD CAP strip).

[CRITICAL] global (§1.1, §3, §7) — Remit-defeating gap: county production is never joined to driver data
  PROBLEM: The notebook serves everything needed to relate county production to ENSO/IOD/Western-V — knbs_napr_county_production (31 crops, 2019–24, spanning the 2020–23 triple-dip La Niña and 2023/24 El Niño), afa_rice, and driver_indices — but no cell joins them. Production lives in §1.1/§2.2 with no driver context; drivers live in §3/§6/§7 with only a weak NATIONAL FAOSTAT regression (§7.3, r=0.20 n.s.). The owner's stated core purpose ('show historical production… and associate this with oceanic drivers') is structurally unmet, and it is a join problem, not a data-acquisition problem.
  EVIDENCE: grep audit: knbs_napr_county_production.parquet and driver_indices.parquet each referenced once, in disjoint client cells (qmd lines 1855, 1360) with no cross-query; §7.3 caption itself says 'a longer county production series is needed to show it properly' while the county series sits one section away.
  REC (M): Build an event-anchored county panel: NAPR production bars per crop-year coloured by the season driver phase (reuse rainPhaseByYear from §3, line ~1518 of notebook.qmd), with the 2021–22 La Niña failure and 2023/24 El Niño recovery annotated. No correlation statistics — 5–6 county-years cannot support them (constraint honoured); phase-colouring + annotation is honest. Zero new data; one new OJS cell against the existing dbNapr + dbDrivers clients.

[CRITICAL] §3/§5 payload — 72% of the data payload is dead weight: chirps_county ships 9 variables, queries 1
  PROBLEM: chirps_county.parquet is 4.52 MB of a 6.29 MB total served-parquet payload (18 files). It contains 255,024 rows across 9 variables (PTOT, TMIN/TMAX/TAVG, SPEI-01/03/06/12/24; verified by DuckDB), but the qmd queries only variable='PTOT' (lines 1546, 1993) — 28,128 rows, 11% of the file. Single row group, FileAttachment whole-file load: every visitor downloads ~4 MB they never see.
  EVIDENCE: DuckDB on the served file: 9 distinct variables, PTOT=28,128 of 255,024 rows; grep shows only two PTOT WHERE-clauses and no TMIN/TMAX/TAVG/SPEI reference anywhere in notebook.qmd; re-export measurements from the actual file: 196.3 KB / 646.3 KB.
  REC (S): Either (a) use the data — add a variable toggle (Rainfall / Temperature / SPEI-3) to §3.1 and §5.1; SPEI directly strengthens the drought story and costs 0 new bytes; or (b) slim the file — measured re-exports: PTOT-only = 196 KB, PTOT+SPEI-03+TAVG = 646 KB (ZSTD, sorted by gaul1_code). Recommend (b) with the trio, keeping the toggle: −3.9 MB, ~60% total payload cut, feature added not removed.

[CRITICAL] §3 (new) — Owner ask (a): CHIRPS v3 subcounty seasonal rainfall — no adm2 data exists anywhere yet; D409 rerun is the right path and it is tiny
  PROBLEM: Nothing subcounty exists today: the Atlas hub has NO adm2 CHIRPS product (verified: variable=adm2_obs.parquet returns 404 on both processing=admin-periods and admin-monthly; the adm1 control URL returns 200), and the notebook loads only county-level CHIRPS. The alternatives are worse: WFP VAM HDX dekadal rainfall is on OCHA COD p-codes (crosswalk risk to GAUL24) and is not CHIRPS v3 — it fails the owner's explicit 'CHIRPS v3' requirement; client-side COG/raster rendering is heavy, off-architecture for a DuckDB-WASM notebook, and should be rejected.
  EVIDENCE: curl probes: adm2 URLs 404, adm1 200; topojson inspection: objects=['atlas_gaul24_a2_africa'], 6,670 geometries, 291 KEN, sample props include gaul2_code=103528, admin2_name='Baringo Central'; simulated 38,880-row admin2 parquet = 297 KB.
  REC (L): Rerun the D409 zonal extraction at GAUL24 admin2, Kenya only, PTOT, periods OND/MAM/annual, 1981–2026: 291 subcounties × 46 years × 3 periods = 40,158 rows. A same-shape simulated parquet measures 297 KB (ZSTD) — trivial. Join key is exact: the shared topojson (atlas_gaul24_a2_africa_simple-lowres.topojson) carries 291 KEN features with gaul2_code, admin2_name, gaul1_code, admin1_name (inspected). Do NOT ship the whole 3.34 MB Africa a2 topojson — cut a Kenya-only a2 extract (~150–250 KB). Write the new parquet sorted by gaul1_code with row-group stats (future-proofs S3 hosting per the iso3-pruning lesson).

[MAJOR] §1/§2 + §8 — Port regressions: exposure_vop and seasonal_calendar are served, acknowledged in §8, and never rendered
  PROBLEM: exposure_vop.parquet (12.7 KB, 1,156 rows: county, gaul1_code, commodity, kind, vop_intld15 — MapSPAM+GLW4 value of production) and seasonal_calendar.parquet (5 KB, 571 rows: county crop-calendar stages with month/dekad bounds, 38/47 counties) have zero qmd references. Worse, §8.1 acknowledges 'IFPRI (MapSPAM) & FAO (GLW4) — exposure surfaces' and 'JRC ASAP — crop-calendar seasonal timing' as if they power the page — an integrity mismatch between acknowledgements and rendered content. These are the Atlas's own exposure data; the Atlas notebook currently shows none of it.
  EVIDENCE: grep: 0 references for exposure_vop.parquet and seasonal_calendar.parquet in notebook.qmd; PDF p.11 §8.1 lists both providers; schemas verified (exposure_vop: 1,156 rows; seasonal_calendar: 571 rows).
  REC (M): Restore both: (1) exposure VoP treemap/bar in §2 ('what is at stake in my county' — livestock-vs-crop split directly supports the §1.1 ASAL narrative); (2) crop-calendar strip directly under §3.1 so readers see WHICH crop stages sit inside OND/MAM (honest 38/47 gap labelled: ASAL counties lack calendars). Both parquets have .meta.json; effort is chart cells only.

[MAJOR] §3/§6/§7 provenance — The served driver dataset is the non-reproducible one; the reproducible one sits unused
  PROBLEM: driver_indices.parquet (used by §3, §6, §7) is flagged D409-only in DATA.md ('produced by the D409 pipeline, staged here') — not reproducible from the repo. Meanwhile enso_drivers_monthly.parquet and enso_drivers_seasonal.parquet, built by the git-full self-fetching _sources/enso_drivers_build.py, are served-unused (0 qmd references). The notebook's central scientific inputs are the least reproducible files in the directory while their reproducible twins ride along as dead bytes. driver_indices also has data-quality quirks: rows back to 1854 with NULL year/month columns.
  EVIDENCE: DATA.md §4: 'driver_indices.parquet … is D409-only'; grep: enso_drivers_monthly/seasonal 0 refs, driver_indices 1 ref (line 1360); DuckDB sample of driver_indices row 1: date=1854-01-01, year=NULL, month=NULL.
  REC (M): Consolidate: extend enso_drivers_build.py to emit the Niño3.4/DMI/WNP columns the notebook needs (it already fetches RONI/SOI/DMI keyless), point dbDrivers at the git-full output, and retire driver_indices — or at minimum copy the D409 derivation script into _sources/. This converts the notebook's core driver provenance from D409-only to git-full in one move.

[MAJOR] §6 (new) — Owner ask (c): KMD CAP live warnings — runtime browser fetch is CORS-blocked; stage a snapshot
  PROBLEM: The obvious 'live' implementation — client-side fetch of meteo.go.ke/api/cap/rss.xml from the notebook — will fail: a probe with Origin: https://adaptationatlas.cgiar.org returns HTTP 200 with NO Access-Control-Allow-Origin header (though 'vary: origin' shows a CORS allowlist exists server-side, the Atlas origin is not on it). Any runtime-fetch design ships broken.
  EVIDENCE: curl -H 'Origin: https://adaptationatlas.cgiar.org' https://meteo.go.ke/api/cap/rss.xml → HTTP/2 200, content-type application/xml, content-length 4012, headers include 'vary: origin' but no access-control-allow-origin.
  REC (M): Two-track: (1) NOW — pipeline-staged snapshot: a git-full _sources script fetches the CAP RSS + linked alert XMLs at build time into cap_warnings.parquet (~5–10 KB: alert id, sent, event, severity, areas, expires, fetched_at), rendered as a warnings strip in §6 with an explicit 'as of {fetched_at}' label; the feed is small (4 KB) and sparse (last-modified 2026-05-07 — alerts exist only when issued, so an empty state must be designed). (2) IN PARALLEL — ask the KMD/WMO ClimWeb admins (Ani Ghosh route) to allowlist the Atlas origin; if granted, upgrade to runtime fetch with the snapshot as fallback. KMD is the approved forecast source, so this fully honours the Kenya-Met-only constraint and finally puts an actual KMD product on the page.

[MAJOR] §3/§5 (new) — Owner ask (b): weather stations — feasible via GHCN-Daily/GSOD, but only as sparse validation points, and the notebook must say so
  PROBLEM: No station data is served. The only open, keyless sources are NOAA GHCN-Daily (~10 Kenyan counties with usable stations) and NOAA ISD/GSOD (~28 stations); TAHMO/KMD/ENACTS dense networks are access-gated. A station layer therefore cannot provide county coverage and must not be presented as if it could.
  EVIDENCE: Prior source scan (recorded): GHCN-Daily ~10 counties, ISD ~28 stations open+keyless, dense nets gated; no station parquet or FileAttachment in notebook.qmd.
  REC (L): Pipeline (git-full): fetch GHCN-Daily KE* stations + GSOD Kenya → QC (drop flagged obs) → completeness gate (season valid if ≥85% of days present) → aggregate to seasonal/monthly. Emit stations_key.parquet (~40 rows: id, name, lat, lon, elev, gaul1_code via point-in-polygon against GAUL24 a1 — never name-match) and station_seasonal.parquet (~40 stations × ≤45 yrs × 13 periods × PRCP/TMAX/TMIN ≈ 30–60k rows, est. 300–600 KB ZSTD). Use: (1) station dots on the county/subcounty map; (2) a station-vs-CHIRPS seasonal scatter as CHIRPS validation in the technical annex. Coverage statement verbatim in the annex: 'open long-record stations exist in ~10 of 47 counties; the dense national networks are access-gated'.

[MAJOR] §8 / DATA.md — Reproducibility debt blocking a public v2: 7 D409-only datasets and private doc references in public Methods
  PROBLEM: (1) Seven datasets are D409-only (CHIRPS ×2, FAOSTAT ×2, ipc_county, market_prices, acled_conflict, reliefweb_county, driver_indices) — neither acquisition nor transform is in the repo. (2) §8 Methods (PDF p.10) cites '_master/DATA_CATALOG.md (D409 project store)' and 'METHODOLOGY_extraction_QA.md' as provenance — private OneDrive paths rendered as authoritative references in a public notebook; a public reader hits a dead end exactly where provenance is promised. (3) ACLED's licence is 'attribution, not fully open' and the parquet is redistributed as a public git blob — the served data is aggregated weekly county counts (verified schema: county, week, event_type, events, fatalities), which is likely permissible as a derived aggregate, but this needs an explicit licence check and attribution string, not silence. (4) Zero datasets submitted to the CGIAR Climate Data Hub.
  EVIDENCE: DATA.md state summary: 'D409-only = CHIRPS, FAOSTAT, IPC, market prices, ACLED, ReliefWeb, driver_indices'; PDF p.10 shows both private filenames in §8 body; acled_conflict.parquet schema inspected (16,674 aggregated rows, no raw event coordinates).
  REC (L): Before public v2: copy the D409 acquisition scripts into _sources/ (driver_indices first — trivially replaced per the consolidation finding; CHIRPS second — it is the largest dataset); replace the two private doc references in nbText methods with links to DATA.md and the per-figure .meta.json files (which are public on the branch); add ACLED's required attribution line and record the licence determination in acled_conflict.meta.json; submit the novel NAPR/GESI extractions to the Hub.

[MAJOR] global — No map for county selection — the picker port regression
  PROBLEM: County selection everywhere is a bare dropdown; the only Kenya map in 12 pages is the §6.2 analogue choropleth. A reader picking 'Marsabit' is never shown where Marsabit is, and the previously-built click-to-select choropleth picker was dropped in the port. For a county-centric notebook this is the single most visible data-UX regression, and the boundary data to fix it (a1 topojson, 381 KB) is already loaded for §6.2.
  EVIDENCE: PDF pp.1–2: 'Selected county: Marsabit' rendered as text/dropdown, no locator map; qmd line 1424 already attaches atlas_gaul24_a1_africa_simple-vlowres.topojson; port-regression list confirms the picker previously existed.
  REC (M): Restore the choropleth county picker at the top of the notebook (click county → sets the shared `county` input; dropdown kept as accessible alternative). Zero new data; reuse kenyaA1 (qmd line 1428). When the subcounty work lands, the same map gains the admin2 overlay for the selected county.

[MINOR] §2.2 — Provenance mislabel: heading says 'AFA, 2020–24', data are KNBS NAPR 2019–24
  PROBLEM: The §2.2 heading reads 'dry maize production (AFA, 2020–24)' (nbText sections.b1.maizeTitle) while its own caption says the data are KNBS NAPR with AFA folded in, and the rendered chart shows bars from '19. For a notebook whose §8 promises per-figure provenance, the one heading that names a source names the wrong one and the wrong span.
  EVIDENCE: nbText.json sections.b1.maizeTitle.en = 'dry maize production (AFA, 2020–24)' vs maizeCaption.en = 'County maize production over time (KNBS NAPR)…'; PDF p.2 chart shows a '19 bar; grep: afa_production.parquet 0 refs — the maize series comes from dbNapr.
  REC (S): Retitle 'maize production (KNBS NAPR, 2019–24)' in nbText.json (EN+FR); keep the AFA note in the caption where the 233/234 match statistic already explains the relationship.

[MINOR] DATA.md — DATA.md contradicts itself on .meta.json coverage and carries a stale NAPR meta
  PROBLEM: Seven per-dataset sections still say 'No .meta.json yet' (GESI, AFA, drivers, outlook, CHIRPS, ACLED, ReliefWeb) while the state summary says all 26 were written 2026-08-07 — and the directory listing confirms every parquet has a sibling .meta.json. Separately, DATA.md itself flags knbs_napr_county_production.meta.json as stale ('says 12 crops, actual 31'). A public reader auditing provenance meets contradictory claims.
  EVIDENCE: DATA.md lines 50–51, 60, 75, 83, 97, 113, 123 vs line 145–147 ('all 26 … 2026-08-07'); ls shows 26 .meta.json files present; DATA.md line 37: 'stale: says 12 crops, actual 31'.
  REC (S): Delete the seven stale 'No .meta.json yet' notes; re-run _sources/meta_build.py to refresh the NAPR meta (crop count) and diff-check all 26 against actual schemas as a pre-release gate.

[MINOR] data hygiene — Directory housekeeping: superseded/raw/build-input parquets mixed into the served dir, plus __pycache__ pollution
  PROBLEM: The served directory mixes runtime data with build inputs and superseded versions: gesi.parquet (superseded by gesi_v2, DATA.md says so), faostat.parquet (raw input to faostat_detrended), afa_production.parquet (raw input to afa_rice), knbs_production_national.parquet (validation-gate series, unused). None break anything (combined 0.12 MB) but they blur the 'what does the notebook actually consume' contract. data/KE-enso-explorer/_sources/__pycache__/ is untracked and polluting git status. Also: chirps_county contains a 48th admin1, 'Ilemi Triangle', absent from county_key — harmless under current name-filtered queries, but any future national roll-up from this file silently includes disputed territory; and CHIRPS queries join by normalized admin1_name string (normExpr/countyNorm) when both sides carry gaul1_code.
  EVIDENCE: grep: 0 refs for all four; DATA.md line 50 'superseded original'; git status shows data/KE-enso-explorer/_sources/__pycache__/ untracked; DuckDB: 48 distinct admin1 in chirps_county, set-difference vs county_key = ['Ilemi Triangle']; qmd line 1546 uses normExpr(admin1_name)=countyNorm(county).
  REC (S): Drop gesi.parquet from the branch; move faostat.parquet and afa_production.parquet under _sources/ (or mark role: build-input in meta); SERVE knbs_production_national in the technical annex (national additivity check — it is 4 KB and strengthens the NAPR credibility story). Add __pycache__/ to .gitignore. Document Ilemi in chirps_county.meta.json. Switch CHIRPS county filters from name-matching to gaul1_code equality.

[MINOR] §4.6 / runtime — Runtime CDN dependency: topojson-client imported from esm.sh
  PROBLEM: qmd line 1833 does await import('https://esm.sh/topojson-client@3.1.0'). Both maps (§4.6 flow map, §6.2 choropleth) die if esm.sh is slow, blocked (institutional networks), or down — a single external point of failure for the only cartography on the page, and the failure mode is a stuck spinner.
  EVIDENCE: notebook.qmd line 1833: topojsonClient = await import("https://esm.sh/topojson-client@3.1.0"); used by kenyaA1 (line 1428) and africaFeatures (line 1835).
  REC (S): Vendor topojson-client (11 KB min) into the repo/helpers and import locally, or pre-convert the topojsons to GeoJSON at build time and drop the client-side dependency entirely (the a1/a0 files are small enough that GeoJSON conversion adds little).

--- VIZ/DATA RECOMMENDATIONS ---
* Subcounty seasonal rainfall timeseries map (CHIRPS v3, admin2) [§3 (new 3.3)]
  purpose: The owner's explicit ask: show where within the selected county the season failed, year by year — for both audiences (simple map for readers, tercile logic for analysts).
  spec: Selected-county choropleth with the county's admin2 polygons (Kenya-only GAUL24 a2 extract, gaul2_code join), coloured by seasonal PTOT as tercile-vs-1991–2020-normal (drier/near/wetter — same language as §6.2); year scrubber (Inputs.range) plus a small-multiple grid mode for key episodes (2020–23 La Niña seasons, 2023 OND El Niño); OND/MAM toggle; download button; height ≤800px.
  data: NEW pipeline: D409 zonal rerun at GAUL24 admin2, Kenya, PTOT, OND/MAM/annual, 1981–2026 → 40,158 rows ≈ 300 KB (measured on a same-shape simulated parquet). Hub has no adm2 product (404 verified). Boundaries: cut Kenya-only a2 topojson from data/shared/atlas_gaul24_a2_africa_simple-lowres.topojson (291 KEN features with gaul2_code/admin2_name verified) ≈ 200 KB — do not ship the 3.34 MB Africa file.
* County production × driver-phase event panel [§2 or §3]
  purpose: Directly answers the remit: did MY county's harvests move with ENSO/IOD/Western-V? Event-anchored, honest about the short series — for both audiences.
  spec: NAPR production bars (crop selector, 2019–24) with each year's bar coloured by the OND ENSO phase / MAM Western-V phase (reuse §3's rainPhaseByYear), annotated bands for the triple-dip La Niña and 2023/24 El Niño; explicit caption: '6 seasons — patterns shown, no statistics claimed'. No trend lines, no r values (constraint: no county-level correlation claims).
  data: EXISTS: knbs_napr_county_production.parquet + afa_rice.parquet + driver_indices.parquet, all already served and clients loaded; join is one new OJS cell.
* Exposure value-of-production chart (restore) [§2]
  purpose: 'What is at stake in my county' — the Atlas's own exposure framing; grounds the ASAL livestock-vs-crops narrative in §1.1 with numbers.
  spec: Horizontal bar or treemap of vop_intld15 by commodity for the selected county, crop/livestock split colour-coded, %-of-county-total axis option; sits in §2 county profile.
  data: EXISTS unused: exposure_vop.parquet (12.7 KB, 1,156 rows, gaul1_code join, .meta.json present).
* Crop-calendar strip (JRC ASAP, restore) [§3]
  purpose: Lets a non-technical reader see why OND vs MAM matters: which crop stages sit inside each rainy season in their county.
  spec: Compact Gantt-style strip (crops × months, stage-coloured segments) rendered directly under §3.1's rainfall panel, sharing its month axis mentally; 'no calendar available' state for the 9 ASAL counties, honestly labelled.
  data: EXISTS unused: seasonal_calendar.parquet (5 KB, 571 rows, 38/47 counties, m_start/m_end + dekads).
* Weather-station overlay + CHIRPS validation scatter [§3 map + technical annex]
  purpose: The owner's station ask, delivered honestly: point context on the map for everyone; gridded-vs-gauge validation for the technical reader.
  spec: (1) Station dots (GHCN-Daily + GSOD) on the county/subcounty map, tooltip = name, elevation, record span, season total; (2) annex scatter of station seasonal PRCP vs CHIRPS PTOT for the containing admin unit, 1:1 line, per-station r; coverage statement fixed in the caption: ~10 of 47 counties have an open long-record station; dense networks (TAHMO/KMD/ENACTS) are access-gated.
  data: NEW pipeline (git-full, keyless): GHCN-Daily KE* + GSOD Kenya → QC flags dropped → ≥85% season-completeness gate → stations_key.parquet (~40 rows, gaul1_code via point-in-polygon, never name-match) + station_seasonal.parquet (~30–60k rows ≈ 300–600 KB ZSTD).
* KMD CAP warnings strip [§6]
  purpose: Puts an actual Kenya Met Department product on the page (the approved forecast source) — 'is there an official warning right now?' for the non-technical reader.
  spec: Horizontal strip of active/recent CAP alerts (event, severity colour, area, sent/expires) at the top of §6, with 'as of {fetched_at}' label and a designed empty state ('no active KMD warnings at last build'); links to meteo.go.ke.
  data: NEW pipeline-staged snapshot: build-time fetch of meteo.go.ke/api/cap/rss.xml + alert XMLs → cap_warnings.parquet (~5–10 KB). Runtime browser fetch is CORS-blocked (verified: no Access-Control-Allow-Origin for the Atlas origin); request ClimWeb allowlisting in parallel and upgrade to live fetch if granted.
* Temperature / SPEI toggle on rainfall panels [§3.1, §5.1]
  purpose: Analysts get drought-index depth (SPEI-3 is the season-scale drought measure); costs zero new bytes because the data already ships.
  spec: Radio toggle (Rainfall mm / SPEI-3 / Temperature °C) on §3.1 and §5.1, same driver-phase colouring; SPEI rendered as diverging bars around 0.
  data: EXISTS inside chirps_county.parquet (TMIN/TMAX/TAVG + 5 SPEI windows verified, currently 100% unqueried). Pair with the slimming re-export (PTOT+SPEI-03+TAVG = 646 KB measured) so the toggle ships alongside a 3.9 MB payload cut.
* Choropleth county picker (restore) [top of notebook / §1]
  purpose: Every audience: pick and see the county in one gesture; anchors all downstream sections spatially.
  spec: Small Kenya a1 choropleth at top (click-to-select, hover name, selected county outlined), synced two-way with the existing dropdown; later hosts the admin2 overlay of the selected county.
  data: EXISTS: atlas_gaul24_a1 topojson already attached at qmd line 1424 for §6.2; no new data.
* Served-data catalog table (technical annex) [§8 / annex]
  purpose: The technical reader's 'find the underlying data' entry point — replaces the private _master/DATA_CATALOG.md reference with something a public reader can actually open.
  spec: Auto-generated table (one row per served parquet) from the .meta.json sidecars: dataset, source+URL, licence, method, coverage, rows, size, download link; built at render time so it can never drift from the directory contents.
  data: EXISTS: 26 .meta.json files (all present, verified) — needs only a build-time aggregation cell; fix the stale NAPR meta (12→31 crops) first.


====================================================================================================
PERSONA: Science communicator / story editor
====================================================================================================
VERDICT: Today the notebook is a rigorously honest data inventory wearing question-mark headings — it serves neither audience well. The non-technical reader hits a 600-word extraction caption and five unexplained acronyms before any story starts, is never shown a map or told what is at stake, and never meets the droughts and floods Kenyans actually remember; the technical reader gets excellent material (validation gates, honest non-significance, the analogue card) but scattered across body captions instead of a proper annex. The single most important change is the restructure into a 4-beat story (stakes → ocean-steers-rains → what past named events did → what's coming) with the methodology text swept into a technical annex — because the county series is too short for statistics, named-event anchoring plus the restored map/VoP opener is the only honest way to deliver the owner's remit of connecting exposure to ENSO for lay readers. The good news: the best pattern already exists in §6.2's county card ('LIKELY WETTER — 6 of 8 analogue years'); the job is to make the whole notebook speak like that card.

STRUCTURE PROPOSAL: Three tabs. Tab 1 'The story' — a strictly linear 4-beat arc (What's at stake in my county? → How does the ocean steer my rains? → What did past El Niño/La Niña years do here? → What is the coming season likely to bring?), each beat opening with a dynamic county-aware headline sentence, closing with one 'What this can and cannot tell you' box, captions cut to one sentence + Data line. Tab 2 'Explore the data' — the produce multi-view, county comparison, price and trade explorers: tools, not story beats. Tab 3 'Technical annexes' — the supplemental analysis (§7), the full extraction methodology, per-table sources, and exact statistics (r/n/p). All section and figure numbers auto-generated, never typed in nbText strings; 'Block N' cross-references replaced by resolved anchors; FR translation deferred until this structure lands, then gated by a no-null-fr lint.

[CRITICAL] global — No story arc — the notebook is an inventory, not a narrative
  PROBLEM: The current order (state-now → produce → GESI → drivers → impacts → compare → outlook) buries the causal spine. The proof that the ocean moves rainfall (§3) arrives AFTER two sections of production tables and a GESI table the reader has no reason to care about yet. §1 ('Where Do Things Stand Right Now?') and §6 ('Where Are We Now, What's Coming?') ask the same question twice. §2 opens by apologising for its own structure: '(What the county grows is up top, under "What does my county produce?".)'. §4's intro is a table of contents, not a message: 'Food insecurity (IPC), conflict, and market prices, for the selected county.' No section earns a 'so what' one-liner.
  EVIDENCE: b1.intro: '(What the county grows is up top…)'; b4.intro: 'Food insecurity (IPC), conflict, and market prices, for the selected county.'; PDF pp.1–2: produce controls and a 600-word methods caption appear before any climate evidence.
  REC (L): Rebuild as a 4-beat arc with a headline message each section must earn: (1) 'What's at stake in my county?' — map picker + value of production + produce + GESI highlights; headline: 'In {county}, I${X} of crops and livestock ride on two rain seasons.' (2) 'How does the ocean steer my county's rains?' — driver-phase rainfall bars + crop calendar; headline: 'El Niño years and La Niña years look different in {county}'s rain gauge — season by season.' (3) 'What did past El Niño and La Niña years do here?' — event-anchored NDVI/IPC/prices/reports; headline: 'In 2020–23, five failed seasons drove pasture to record lows and food insecurity to Crisis.' (4) 'What is the coming season likely to bring?' — recent driver state + analogue map + county card; headline: 'The Pacific is in {phase}; in the {n} most similar past years, {county}'s short rains were {tercile} {k} of {n} times.' Merge §1/§6 duplication (current-state strip moves into beat 4), dissolve §2 into beat 1, move §5 Compare and the produce multi-view into an Explore tab.

[CRITICAL] §1.1 — A methods paper is embedded in a figure caption on page 1
  PROBLEM: keyfacts.produceCaption is ~600 words of PDF-extraction internals — 'orientation-aware (landscape annexes are rotated 90° on the page — de-rotated by word position)', 'column-binned by coordinate', 'dual-engine agreement', 'a duplicated / x-shifted text layer that defeats the second engine' — rendered under the very first chart a non-technical reader meets. produceMethodology adds another dense block. This is admirable rigor in the wrong room: it signals 'this notebook is not for you' to the county-official audience within the first scroll.
  EVIDENCE: keyfacts.produceCaption (nbText.json line 155, ~600 words); PDF p.1 shows it filling half the page under Figure 1.1.
  REC (M): Caption becomes one sentence + Data line: 'What {county} grows and herds, from Kenya's official county returns (KNBS NAPR, 2019–2024; blanks mean not-reported, never zero). · Data: KNBS NAPR · How we extracted and validated every number →' linking to a Technical Annex page that receives the full extraction text verbatim (it is genuinely excellent annex material). Keep the blank≠zero warning in the body — it is the one caveat lay readers need.

[CRITICAL] §3–§4 — The events Kenyans remember are never named
  PROBLEM: The notebook's strongest honest storytelling device — 1997–98 El Niño floods, 2010–11 Horn of Africa drought, 2016–17 La Niña drought, the 2020–23 triple-dip (five failed seasons), the 2023 El Niño recovery — appears nowhere in any figure. The only trace is a clause in ndviIntro ('2011, 2017 and 2021–22 are Kenya's major droughts'). The rainfall bars (§3.1), IPC line (§4.1), price series (§4.3), ReliefWeb bars (§4.4) and NDVI (§4.5) all visibly contain these events, but the reader must connect them unaided. Since the county production series (2019–24) is too short for statistics, named-event anchoring is the ONLY legitimate way this notebook can deliver the owner's remit of showing production/exposure vs ENSO for the non-technical audience — and it's missing.
  EVIDENCE: PDF pp.3–6: rainfall bars, IPC, prices, ReliefWeb and NDVI charts carry no event annotation; b4.ndviIntro is the sole mention of drought years, in passing prose.
  REC (M): Create a curated events.json (≈7 events: name EN/FR, date span, driver phase, one-line story) and drive a shared annotation layer from it: shaded vertical bands + labels on every time-series figure, phase-tinted (El Niño warm, La Niña cool), tooltip giving the one-liner. This is metadata, not data — no LLM-number constraint applies. Also name the analogue-year chips in the §6.2 county card ('1997 — the El Niño floods year', '2023 — El Niño recovery').

[CRITICAL] §1 — The story's call-to-action fails: 'Pick a county to begin' with no map, no stakes
  PROBLEM: The intro ends 'Pick a county to begin', but page 1 shows no map and no visible picker — county selection lives in a floating dropdown panel. nbText.json still carries the full text for the missing entry point: keyfacts.mapHeading ('Pick a county — Kenya at a glance'), mapIntro ('Click any county to explore it…'), mapCaption (VoP/NDVI/IPC/rain metrics), plus b1.vopTitle/vopIntro/vopCaption for the value-of-production chart — all orphaned keys for the ported-out features. Without the map and the VoP stakes chart, the WHO/WHERE/what's-at-stake beat of the story does not exist.
  EVIDENCE: PDF p.1: no map anywhere; nbText.json keyfacts.mapHeading/mapIntro/mapCaption and b1.vop* keys exist but render nowhere in the 12-page PDF; brief confirms these are port regressions.
  REC (M): Restore the Key-Facts choropleth click-to-select picker (exposure_vop.parquet + shared GAUL24 topojson are already local) as the opening figure, defaulting to the VoP metric so 'what's at stake' is the first thing coloured. Pair it with a compact county card (VoP, latest NDVI %, latest IPC phase). The orphaned nbText keys can be reused nearly as-is.

[MAJOR] §1, §3, §7 — 'Quick insight' boxes are static national lectures, not insights
  PROBLEM: All insight boxes are static md renders of nbText strings (notebook.qmd lines 299, 458). keyfacts.insight is a near-verbatim repeat of the intro ('Kenya's rains are steered by ocean patterns… Pick a county and follow…'). b2.insight1 is an r-value lecture ('r≈0.67 vs ENSO ≈0.45… Niño 3.4 ↔ Western-V r≈0.08') identical for every county. §4 — the section where a county official most needs a takeaway — has no insight box at all. Meanwhile the notebook already computes county-specific values in OJS (driver-phase rainfall means, latest NDVI, IPC peaks, the analogue card).
  EVIDENCE: notebook.qmd:299 md`${_lang(nbText.sections.keyfacts.insight)}` and :458 (b2.insight1) — static, county-blind; PDF p.8 county card shows the dynamic pattern already works.
  REC (M): Replace static boxes with a dynamic county-insight component computed from already-loaded data: §2-beat: 'In {county}, El Niño short rains averaged {x} mm — {p}% more than La Niña years'; §3-beat: 'During the 2020–23 drought, {county}'s pasture fell to {min}% of normal and food insecurity reached Phase {k}'. The §6.2 county card ('LIKELY WETTER than normal — 6 of 8 analogue years were wetter') is the best plain-language moment in the whole notebook — use it as the template. Move the r-values to the technical annex.

[MAJOR] global — Jargon audit: ~30 terms a county official won't know, starting on line one
  PROBLEM: The first dynamic line reads 'Niño 3.4 (ONI, 3-mo mean) 0.88 °C (El Niño), IOD (DMI) -0.44… Season lens: OND' — five unexplained acronyms before the story starts. The hero subtitle uses 'Western-V' cold. Full audit of nbText.json: ENSO, IOD, DMI, Western-V, WNP, Niño 3.4, ONI, RONI, OND, MAM, anomaly, z-score, detrended, trend-inflated, tercile, analogue (composite), dekadal, NDVI, MODIS, IPC 'worst area, Current Situation', r/n/p, 'partly-independent control', 'coupled Indo-Pacific system', 'season-window driver', VoP, I$, MapSPAM, GLW4, ASAL, XBT, FDW, GAUL, pixel-weighted, dual-engine, additivity, 'administrative data', 'stationary anomalies'.
  EVIDENCE: notebook.qmd:217 (current-state md string); hero.subtitle 'the Western-V steer Kenya's rains'; b2.insight1 'Niño 3.4 ↔ Western-V r≈0.08'; b5.recentCaption 'the IOD DMI (HadISST coalesced with ERSST…)'.
  REC (M): Three tiers. BODY REWRITE: current-state line → 'The Pacific is in El Niño (0.88 °C warmer than normal); the Indian Ocean Dipole is slightly negative.' OND/MAM → 'short rains (Oct–Dec)'/'long rains (Mar–May)' at first use per section; anomaly → 'difference from normal'; detrended → 'growth trend removed, leaving year-to-year swings'; tercile → 'drier / near-normal / wetter thirds'; analogue → 'similar past years'; dekadal → '10-day'; 'partly-independent' → 'moves largely on its own'; 'Season lens' → 'Season shown'; r/n/p in story text → 'a strong/moderate/weak link'. TOOLTIP GLOSSARY (hover/expand): ENSO, IOD, Western-V, NDVI, IPC phases 1–5, VoP/I$, ASAL, z-score. ANNEX-ONLY: ONI, RONI, DMI, WNP, ERSSTv5, HadISST, MODIS, MapSPAM, GLW4, XBT, FDW, GAUL, pixel-weighted, dual-engine, additivity, exact r/n/p values.

[MAJOR] global — Caveats read as scattered apologies instead of one honest pattern
  PROBLEM: Honesty is this notebook's superpower, but it is delivered as ⚠️ interruptions and defensive parentheticals sprinkled everywhere: ⚠️ 'National, not county' (b3.intro), ⚠️ 'National, not your county' (b4.tradeIntro), ⚠️ 'Low confidence for MAM' (b5.outlookMamCaveat), ⚠️ 'A blank / absent county-year…' (produceMethodology), 'may be incomplete', 'macadamia is currently held back', 'Caveat: national maize is long-rains-dominated…'. The cumulative effect is apologetic rather than authoritative, and the crucial caveats drown among the minor ones.
  EVIDENCE: Four separate ⚠️ blocks across b3.intro, b4.tradeIntro, b5.outlookMamCaveat, keyfacts.produceMethodology; PDF pp.5–8 show warning triangles as recurring visual texture.
  REC (S): Adopt one repeating component — 'What this can and cannot tell you' — one per story section, exactly three lines: CAN (what the reader may safely conclude), CANNOT (the one thing they must not conclude, e.g. 'this is an analogue of similar past years, not a rainfall forecast'), MORE (link to annex detail). Everything else (held-back tables, edition rebases, coverage counts) moves to the annex. Statistical-honesty labels (non-significant regression, exploratory conflict link) stay verbatim — relocate, never soften.

[MAJOR] global — Hard-coded numbering is broken and internal 'Block' jargon leaks to readers
  PROBLEM: Section/figure numbers are hand-typed inside nbText strings and have already drifted: b3.title and b4.title are BOTH '4.' ('4. How Does Climate Move Kenya's Harvests?' / '4. How Does A Bad Season Reach People?'); keys numbered 7.1/7.2 live inside section b2; 'Figure 7.3' is hard-coded in b3.scatterCaption. Worse, internal block names leak into user-facing prose: 'see the NDVI chart in Block 4' (b1.vopIntro), 'why Block 3 relates production to the season-window driver' (b2.calendarIntro), 'Detrending (Block 3)' (methods.text) — readers see sections 1–9, never 'Blocks', so every such cross-reference is a dead pointer.
  EVIDENCE: nbText.json b3.title='4. How Does Climate Move Kenya's Harvests?' vs b4.title='4. How Does A Bad Season Reach People?'; b2.interactHeading='7.1 How the drivers interact'; b1.vopIntro 'see the NDVI chart in Block 4'.
  REC (S): Strip all section/figure numbers from nbText strings and let Quarto/a numbering helper assign them; replace 'Block N' references with resolved anchor links ('see the vegetation chart in [What did past events do here?](#impacts)'). This is also a prerequisite for the restructure and halves future FR churn.

[MAJOR] global — Bilingual promise is 0% delivered — and the rewrite will decide its cost
  PROBLEM: All 126 fr keys in nbText.json are null (126/126 grep-verified); the _lang plumbing exists but only 'In this notebook / Dans ce notebook' is translated (hard-coded in qmd:178). The current text is maximally expensive to translate: a 600-word caption, r-values and section numbers baked into sentences, duplicated intro/insight text.
  EVIDENCE: grep -c '"fr": null' → 126 of 126 fr keys; notebook.qmd:178 is the only FR string in the codebase.
  REC (L): Sequence deliberately: (1) freeze FR until the EN restructure lands — translating today's text wastes a full pass; (2) the restructure itself is the cost-reducer: short captions, numbers/values interpolated by code not typed in prose, one honest-box pattern reused; (3) then a single professional FR pass; (4) add a publish-gate lint that fails on any null fr key so the notebook can never again ship half-bilingual.

[MAJOR] §6.2 — 'What to watch' links CPC and FEWS NET but never Kenya Met
  PROBLEM: b5.watch sends readers to the NOAA CPC ENSO advisory and the 'FEWS NET East Africa seasonal monitor' — yet the notebook's own provenance stance (methods: 'seasonal-forecast provenance follows the Kenya Meteorological Department') and the project's hard constraint make KMD the authority for forward-looking rainfall. Pointing readers to a third-party seasonal monitor as the forward-looking destination sits at odds with the Kenya-Met-only forecast rule; and the Kenyan reader's actual authority, meteo.go.ke, is never linked anywhere in the body.
  EVIDENCE: b5.watch: 'see the NOAA CPC ENSO advisory and FEWS NET East Africa seasonal monitor'; methods.acknowledgements: 'seasonal-forecast provenance follows the Kenya Meteorological Department' — but no meteo.go.ke link exists in nbText.json.
  REC (S): Rewrite 'What to watch' to lead with KMD: link the KMD seasonal outlook page and the live KMD CAP warnings feed (meteo.go.ke — ClimWeb; CAP feed exists now) as 'the official Kenyan forecast and live weather warnings'; keep the NOAA CPC ENSO advisory as the approved global driver-index source; drop or clearly demote the FEWS NET seasonal-monitor link (fine as historical/monitoring context, not as the forecast pointer).

[MAJOR] §2.2 — Maize chart contradicts its own caption and duplicates §1.1
  PROBLEM: The §2.2 heading says 'dry maize production (AFA, 2020–24)' while its caption says the data is KNBS NAPR ('County maize production over time (KNBS NAPR)'), and b1.maizeSources describes a source toggle ('toggle the source to compare') that does not appear in the render. The chart also duplicates the trend view already available in the §1.1 produce panel. For a story editor this is the classic stray artifact: it erodes trust (which agency IS this?) and adds a beat with no message.
  EVIDENCE: b1.maizeTitle='dry maize production (AFA, 2020–24)' vs b1.maizeCaption='…(KNBS NAPR). The Agriculture & Food Authority (AFA) reports the same figures…'; PDF p.2 §2.2 shows no source toggle.
  REC (S): Delete the standalone chart; fold the KNBS-vs-AFA comparison note into the produce panel's annex text where the 233/234 county-year match is already explained. If a maize trend must stay in the county profile, fix the title to match the caption's source and restore (or remove the text for) the toggle.

[MAJOR] global — Section titles ask overlapping or wrong questions; intro is jargon-loaded
  PROBLEM: §1 and §6 both ask 'where are we now'; §2 ('What Does My County Look Like?') is so vague it houses a GESI table and a stray maize chart; §3 leads with three acronyms ('ENSO, The IOD And The Western-V:…'); §1's title promises 'right now' but delivers structural produce data. The intro's second sentence contains 'partly-independent Western-V Pacific pattern' — jargon before the reader is invested.
  EVIDENCE: keyfacts.title='1. Where Do Things Stand Right Now?' vs b5.title='6. Where Are We Now, What's Coming?'; intro (nbText line 18) 'partly-independent Western-V Pacific pattern (long rains)'.
  REC (S): Final titles: 1. 'What's at stake in my county?' 2. 'How does the ocean steer my county's rains?' 3. 'What did past El Niño and La Niña years do here?' 4. 'What is the coming season likely to bring?' Explore tab: 'Explore and compare the data'. Annexes: 'How strong are these links, really?' and 'Data & methods'. 40-word intro rewrite: 'Kenya's rains are steered from the ocean. Pick your county to see what a season puts at stake, what past El Niño and La Niña years did to its rains, pastures, prices and hunger — and what the coming season is likely to bring.'

[MINOR] §5 — Compare section interrupts the impacts → outlook flow
  PROBLEM: §5 (multi-county rainfall + NDVI lines) is a useful analyst tool but sits between 'how a bad season reaches people' and 'what's coming', breaking narrative momentum with an exploratory detour that has no message of its own.
  EVIDENCE: PDF p.7: §5 sits between the trade imports chart and §6's outlook; compare.intro offers no takeaway ('a fast way to contrast…').
  REC (S): Move Compare to the Explore tab, alongside the produce multi-view and the price/trade explorers. In the story flow, replace it with the dynamic insight sentence that compares the selected county to the national picture.

[MINOR] §3.2 — Driver index chart heading is a fragment
  PROBLEM: The rendered heading '3.2 ENSO (Niño 3.4) — OND (points = ENSO phase)' is built from the fragment key b2.driverTitle='(points = ENSO phase)' — it reads as a parenthetical without a sentence, and gives the lay reader no reason to look at the chart.
  EVIDENCE: PDF p.3 heading '3.2 ENSO (Niño 3.4) — OND (points = ENSO phase)'; nbText b2.driverTitle.
  REC (S): Retitle to a message: 'The ocean signal itself — was each year El Niño, La Niña or neutral?' and move the encoding note ('points coloured by phase') into the caption where it belongs.

--- VIZ/DATA RECOMMENDATIONS ---
* Named-event annotation layer [§3.1, §4.1, §4.3, §4.4, §4.5, §5, §6.2]
  purpose: Lets both audiences see the 1997–98 El Niño floods, 2010–11 and 2016–17 droughts, the 2020–23 triple-dip and the 2023 El Niño in every time series — the honest substitute for county-level correlation statistics.
  spec: Curated events.json (~7 events: name EN/FR, date span, driver phase, one-line story) feeding a shared OJS helper: phase-tinted vertical bands (El Niño warm tint, La Niña cool tint) with top-edge labels on line charts (NDVI, IPC, prices, compare), outline+label treatment on the yearly rainfall bars, and named chips on the §6.2 analogue card ('1997 — the El Niño floods year'). Tooltip shows the one-line story.
  data: New tiny static JSON — curated metadata, no pipeline, no LLM-number concern; all target charts already exist.
* Dynamic county insight cards [all story sections]
  purpose: Replaces static national 'Quick insight' lectures with a county-specific takeaway sentence per story beat, for the non-technical reader.
  spec: One reusable OJS component styled like the §6.2 county card: beat 2 computes 'In {county}, El Niño short rains averaged {x} mm — {p}% more than La Niña years' from the driver-phase rainfall data; beat 3 computes the NDVI trough and IPC peak inside the 2020–23 event window; beat 1 reports top commodity and VoP share. Values interpolated by code (satisfies the no-LLM-numbers rule).
  data: chirps_county.parquet, NDVI, IPC, produce parquets — all already loaded in OJS; no new data.
* Restored Key-Facts choropleth county picker (story opener) [§1 / story beat 1]
  purpose: Gives 'Pick a county to begin' something to pick, and makes 'what's at stake' the first thing the lay reader sees coloured.
  spec: Click-to-select Kenya choropleth, default metric = value of production (switchable to latest NDVI %, IPC phase, seasonal rainfall per the orphaned mapCaption text), selected county outlined, beside a compact stat card (VoP, latest NDVI %, latest IPC). Reuses the existing orphaned nbText keys.
  data: exposure_vop.parquet (local, unused — port regression) + shared GAUL24 topojson (local); code previously existed.
* Event timeline spine [top of story beat 3]
  purpose: A compact visual table of contents for the impacts beat — non-technical readers navigate by remembered events, not by dataset names.
  spec: Horizontal strip 1982–2026, one labelled marker per named event coloured by driver phase, hover shows the one-line story; optionally clicking an event highlights its band in the charts below (presentation-only interaction — keep it OUT of loader dep arrays per the known race).
  data: Same events.json as the annotation layer; nothing else.
* County produce × events mini-panel [story beat 3]
  purpose: Directly serves the owner's 'production vs ENSO' remit without fake statistics: shows the 2019–24 county production series against the two events it spans.
  spec: Small-multiple bars of the county's top 3 crops (KNBS NAPR 2019–24) with the 2020–23 La Niña band and 2023 El Niño band overlaid; caption states plainly that six years is too short for correlation, so read it as event history. Blank county-years rendered as gaps with a 'not reported ≠ zero' note.
  data: KNBS NAPR produce parquet (local) + events.json; no pipeline work.
* Restored JRC ASAP crop-calendar strip [story beat 2, under the rainfall bars]
  purpose: Bridges signal to field for lay readers — makes 'planting follows the rains, harvest comes months later' visible instead of asserted.
  spec: Per-county horizontal strip: planting and harvest windows as bars over a 12-month axis, with the short-rains (Oct–Dec) and long-rains (Mar–May) windows shaded to align visually with the rainfall chart above; keep the honest calendarNoData text for the 9 ASAL/urban counties (nbText key already written).
  data: seasonal_calendar.parquet (local, unused — port regression), 38/47 counties.
